# QA manual — Corrida completa del checklist del MVP

Registro de la corrida manual de `docs/qa-checklist.md` (secciones 1–13).

| | |
|---|---|
| **Fecha inicio** | 2026-09-08 |
| **Fecha cierre** | _(abierta — documento vivo, se actualiza ronda a ronda)_ |
| **Última ronda** | Ronda 7 manual (2026-09-11, QA-5 PDF) — D6 verificado en navegador; BUG-19 y BUG-20 nuevos; Mejora-01 y Mejora-02 propuestas |
| **Commit inicial** | `0dfb416` (`claude: repaso QA`) |
| **Tester** | Merlina |
| **Entorno** | **Producción** — https://www.clubatleticoroberts.com + API `club-atletico-api.onrender.com` + Neon (base actual, con datos de prueba/basura) |
| **Objetivo** | Validar que el producto funciona de punta a punta. La data sucia no importa en esta etapa; se limpia después (TRUNCATE + re-seed) como último paso antes del MVP. |

> Esta corrida NO se hace en staging a propósito: se decidió testear sobre la base real tal cual está.
> Ningún paso de este documento modifica la base con acciones destructivas (TRUNCATE / DELETE masivo / `--reset`).

> ⏸ **Corrida pausada al final de §3.6** (2026-09-08). Se detectó un bug bloqueante en el flujo de pago en efectivo (BUG-08) y varias inconsistencias en el motor de cuotas que conviene resolver antes de seguir probando los socios morosos (§3.2 en adelante con esas cuentas) y antes de avanzar a §4.

> ▶️ **Ronda 2 de re-testeo — 2026-09-09, contra el entorno real ya deployado (GitHub Actions
> + S3/CloudFront para frontend, Render para backend, Neon definitiva).** De los 8 hallazgos
> originales: **3 verificados arreglados** (BUG-03, BUG-06, BUG-08), **2 reabiertos** (BUG-01,
> BUG-02 — el fix no se comportó como se esperaba), **1 parcialmente arreglado** (BUG-05: el
> socio ya ve su comprobante, el admin todavía no ve nada), **1 reencuadrado como decisión de
> producto** (BUG-04: el cálculo estaba bien, se sumó un estado visual nuevo — falta decidir si
> se quiere otro criterio de conteo), **1 convertido en decisión de negocio** (BUG-07 → D5).
> Además se decidieron D1, D2 y D3 (estaban abiertas como preguntas) y se encontraron
> **5 hallazgos nuevos**: BUG-09 a BUG-13, incluido uno grave — **BUG-12**, el descuento de
> menores se pierde en el checkout real. Detalle completo en cada ficha y en "Cómo retomar
> la corrida (ronda 3)" al final del documento. La corrida sigue en curso: quedó pausada a
> mitad de §4.2 (escáner), con resultados de esa sub-sección sin capturar todavía.

> ▶️ **Ronda 3 de re-testeo — 2026-09-11, contra producción con las correcciones de la
> ronda 3 ya deployadas.** Se testearon el estado "mes de ingreso" (D1), los comprobantes
> del admin y, parcialmente, el QR. Resultado: **D1 quedó a medio camino** (el mensaje de
> `/socio/cuotas` correcto, pero el inicio seguía diciendo MOROSO, el calendario "Inactivo",
> y pagar la primera cuota no actualizaba nada — hacía falta un segundo pago), **BUG-07
> siguió sin mandar mail**, y aparecieron 2 hallazgos: el comprobante viejo persistiendo
> tras un reemplazo (**BUG-15**) y la confirmación de que BUG-11 y BUG-13 todavía se ven en
> pantalla. Las correcciones están en "Estado de la ronda 4" al pie, con tablas de casos
> numerados para retestear. **BUG-07 resultó tener una causa raíz distinta de todo lo que se
> había supuesto en las rondas 2 y 3** — vale la pena leer esa ficha.

> ▶️ **Ronda 4 manual — 2026-09-11 (QA-4).** _(Ojo con la numeración: esta misma corrida figura al pie como "Estado de la ronda 5 manual" — es la misma, no son dos.)_ Verificados: **BUG-02** ✅ (redirige tras cambio de contraseña obligatorio), **D1** ✅ (flujo mes de ingreso completo y correcto con socios 77000005 y 77000006), **D6** (decisión tomada: cuotas en `/mis-compras` con link a `/socio/cuotas`). Sección §4 QR/escáner verificada de punta a punta. Primera pasada completa de §5 reservas/quincho: flujo OK de extremo a extremo. Hallazgos nuevos: **BUG-16** (escáner no distingue mes de ingreso), **BUG-17** (escáner no indica menor de edad), **BUG-18** (socio no puede reemplazar comprobante de reserva en `/mis-compras`). Mejora propuesta para `/admin/reservas`. Quedan pendientes el retesteo de los casos A…E de la ronda 4 y las secciones §5 canchas, compras mixtas y socios morosos.

> ▶️ **Ronda 6 — 2026-09-11 (correcciones Claude Code).** Corregidos los tres hallazgos de la
> ronda 5 e implementada D6 (cuotas resumidas en `/mis-compras` + `ruta_destino` calculada en
> el backend para los links de las notificaciones). Además se re-verificaron **en código** los
> casos que la ronda 5 dejó abiertos —BUG-01, 04, 07, 11, 13 y 15—, incluido un diferencial de
> **484 casos** entre el motor de cuotas del frontend y el del backend, con **0 divergencias**.
> De paso apareció que el bloque de regresión de BUG-12 agregado en la ronda 4 a
> `qa_seguridad.py` **no podía correr** (llaves de plantilla sin desescapar); ya está
> corregido. **Se rehizo también `/admin/reservas`**: el admin ve ahora la misma grilla que
> el socio, con el nombre de quién tiene cada turno, sin tope de fechas y con bloqueos
> manuales (mantenimiento, reunión de comisión). Todo pendiente de push/deploy — detalle al
> pie, en "Estado de la ronda 6".

> ▶️ **Ronda 7 manual — 2026-09-11 (QA-5).** **D6 verificado en navegador** ✅ (`/mis-compras` muestra cuotas resumidas + reservas). §5 quincho: flujo completo de selección múltiple, pago y aprobación verificado. `/admin/reservas` rediseñado verificado parcialmente: vista de reservas y suspensión OK, pero **inhabilitar turno falla en producción** con CORS/401 (**BUG-19**). Hallazgo crítico: el admin cancela una reserva pagada sin que el socio reciba aviso ni se inicie flujo de devolución (**BUG-20**). Concurrencia de socios en reservas funciona correctamente. Dos mejoras propuestas: TTL para pre-reservas de carrito (Mejora-01) y rediseño del botón "Nueva reserva" (Mejora-02).

---

## Cómo usar este documento

1. Ir sección por sección en el orden sugerido (§1 primero: crea los usuarios de prueba).
2. Marcar cada ítem en la columna **Resultado**:
   - `OK` — pasa como se espera
   - `BUG-nn` — falla; anotar el hallazgo en "Índice de hallazgos" con ese número y detallarlo abajo con la plantilla
   - `N/A` — no aplica / no se pudo probar en esta corrida (explicar en Observaciones)
   - `—` — todavía sin probar
3. Cada hallazgo va con: sección + pasos para reproducir + esperado vs obtenido + severidad.
4. Al terminar, completar el veredicto al pie.

> 📌 **Este es el único documento de QA manual del proyecto.** Cada ronda se agrega acá
> —actualizando el índice de hallazgos, la ficha del bug que corresponda y sumando una
> sección "Estado de la ronda N" al pie— en vez de abrir un archivo nuevo. La idea es que
> la historia de cada bug se lea completa en un solo lugar: varios de estos tardaron dos o
> tres rondas en mostrar su causa real, y esa historia es justamente lo que evita volver a
> diagnosticarlos desde cero. Cuando se cierre la corrida completa, ahí sí conviene abrir un
> documento nuevo, más definitivo, con el balance final.

---

## Decisiones de negocio confirmadas durante esta corrida

| Tema | Decisión | Impacto en el test |
|---|---|---|
| ¿Los morosos pueden comprar / alquilar? | **SÍ.** Un socio moroso hace compras en la tienda y reservas de canchas/quincho con normalidad. **Sigue figurando moroso** — la deuda de cuota no se toca ni se bloquea el checkout por estado de cuenta. | §3, §5: no esperar ningún bloqueo por morosidad en el carrito ni en la pre-reserva. Si el checkout impide comprar/reservar a un moroso → es BUG. El QR de acceso a la puerta sí sigue denegando (eso es correcto). |

---

## Preguntas de negocio abiertas (no son bugs — conviene decidirlas antes del MVP)

| # | Pregunta | Por qué importa |
|---|---|---|
| D1 ✅🔧🔁 | El mes de alta de un socio nuevo figura como **"pagado"** sin pago real. **Decidida el 09-09, implementada en la ronda 3, corregida en la ronda 4.** Se necesita un estado distinto — "mes de ingreso": el socio se ve al día, pero ese mes **sigue debiéndose**. La ronda 3 manual encontró que la implementación había quedado a medio camino (inicio en MOROSO, calendario en "Inactivo", sin actualizarse al pagar); ver §7 de la ronda 3 y "Estado de la ronda 4". | Bloqueaba la Regresión #2 y el caso "transición moroso → al día" (§3.4). **Retestear con los casos D1-A…E** — hace falta un socio dado de alta en el mes en curso. |
| D2 ✅🔧 | El mensaje de "recuperar contraseña" es siempre neutro. **Decidida el 09-09, pendiente de implementar:** para este sistema (club chico, comunidad de confianza) se prefiere avisar explícitamente si el DNI/mail no está registrado o está mal escrito, priorizando ahorrarle tiempo al socio por sobre la protección anti-enumeración. | Cambia el mensaje de `/recuperar-password` (y probablemente el de `/registro` al chequear duplicados) para que distinga "no registrado" de "listo, revisá tu mail". |
| D3 ✅🔧 | El mail de bienvenida de alta manual no incluye la contraseña temporal. **Decidida el 09-09, pendiente de implementar:** el mail debe incluir el DNI y la contraseña que cargó el admin, para que el socio la use y la cambie. | Afecta la plantilla de `bienvenida_alta_manual` y probablemente su lógica de armado (hoy no le pasan el password en texto plano al mailer). |
| D4 ✅ | ~~Pago en efectivo: ¿el socio debería subir un comprobante en absoluto...?~~ **Resuelta y VERIFICADA el 09-09** (ver BUG-08): el efectivo no pide comprobante y lo aprueba el admin al cobrar. Probado con un pago real de 3 meses, funcionó de punta a punta. | Sugerencia adicional (no bloqueante): que el admin pueda subir una foto **opcional** del efectivo como registro interno. |
| D5 ✅🔧 | **Unificar el criterio de notificaciones.** Hoy es mitad y mitad: mail cuando se **genera** una orden, silencio (solo in-app) cuando se **aprueba**. La preferencia expresada: in-app para el socio en la mayoría de los eventos (no saturar el mail), y reservar el mail específicamente para cuando el pago **se aprueba** — no necesariamente cuando la orden se genera. | Relacionado con BUG-07/`compra_confirmada`. Definir si esto significa sacar el mail de generación, agregar uno de aprobación, o ambos — y actualizar `docs/qa-checklist.md` en consecuencia una vez decidido. |
| D6 ✅🔧 | **¿Las cuotas tienen que aparecer en `/mis-compras`?** **Decidida en la ronda 5 manual, implementada en la ronda 6:** SÍ aparecen, pero con poco detalle — una línea "Cuota social — N mes(es) — $X" y un link a `/socio/cuotas`, que sigue siendo la pantalla dueña del desglose mes por mes. Para una orden mixta, la parte de cuotas se resume al final del listado de ítems con el mismo link. Además, los links de las notificaciones in-app de cuota dejan de apuntar a `/mis-compras`. | Cambió `listar_mis_compras()` (sacó el `NOT EXISTS` que excluía cuota_social), `SocioCompras.jsx` y la ruta que el backend calcula para cada notificación (`ruta_destino`). **Retestear con los casos D6-A…D.** |

---

## Condiciones conocidas del entorno (NO son bugs — no re-diagnosticar)

| # | Situación | Por qué |
|---|---|---|
| E1 | El primer request tras inactividad cuelga ~40–60 s | Cold start de Render free tier. Falta UptimeRobot pegándole a `/health`. |
| E2 | El scheduler no corre de noche | Mismo motivo (proceso dormido). Por eso §13 se saltea en esta corrida. |
| E3 | Turnos `bloqueada` viejos sin liberar en la agenda / pagos `pendiente` de órdenes muertas | Hallazgos **D2** (11 pre-reservas huérfanas) y **D3** (~45 pagos zombis) del `/qa` previo. Conocidos, causa = E2. No anotar como bug nuevo. |
| E4 | Links de mails apuntan a `localhost:5173` | Bug conocido **#7**: `FRONTEND_URL` sin setear en Render. **Actualización de esta corrida: NO se reprodujo** — todos los links de mails vistos (mis-compras, recuperar-password, admin/verificaciones) apuntaron correctamente a `clubatleticoroberts.com`. Ver regresión #7 más abajo. |
| E5 | No aparece botón de Mercado Pago en el checkout | Correcto: `VITE_MERCADOPAGO_HABILITADO=false`. Confirmado en esta corrida: no apareció. |
| E6 | `tzdata` / `ZoneInfoNotFoundError` | Solo rompe en dev sobre Windows. Irrelevante para el test contra producción (Render corre Linux). |

---

## Índice de hallazgos

Estado al **2026-09-11** — actualizado con las correcciones de la **ronda 8** (Claude Code),
que responden a lo que encontró la corrida manual de la **ronda 7** (BUG-19, BUG-20, Mejora-01
y Mejora-02 — ver "Estado de la ronda 8" al pie). Las filas BUG-16 a BUG-18 vienen de la
**ronda 6** y responden a la corrida manual de la ronda 5. Las filas BUG-01 a BUG-15
vienen de la ronda 4 y su estado sigue vigente: la ronda 6 las re-verificó **en código**
(ver "Estado de la ronda 6" al pie), pero ninguna pasó todavía por el navegador.
Todo lo que dice "corregido" está commiteado y **pendiente de push/deploy**.
Buena parte ya está **verificada en ejecución** contra la branch de dev de Neon (el build
del frontend pasa, y BUG-10, BUG-12 y D1 se probaron de punta a punta con datos reales):
ver "Estado de la ronda 3" al pie, sección "Verificación en ejecución".

| # | Sección | Severidad | Resumen | Estado |
|---|---------|-----------|---------|--------|
| BUG-01 | 2.1 / 9 | Alta | Socio es enviado a `/admin` en vez de `/socio`. | 🔧 **corregido en dos tandas, a retestear (casos 01-A…E)** — el redirect por rol se arregló en la ronda 3 (`homePorRol()`: solo `admin_general` va a `/admin`). En la ronda 4 apareció la parte que faltaba: `RolResponseSimple` **no mandaba `es_activo`**, y el filtro del frontend compara contra ese campo — o sea comparaba contra `undefined` y no filtraba nada, así que una asignación de admin **desactivada** seguía aterrizando en `/admin` con el panel en 403. |
| BUG-02 | 2.3 | Alta | Tras cambiar la contraseña obligatoria del primer login, no redirige. | ✅ **verificado arreglado** (ronda 4 manual) (el build del frontend sí pasa) — el deploy de la ronda 2 **sí** había llegado (run de GitHub Actions en `success` para `bd25364`, 16:47 UTC): el fix estaba publicado y no alcanzaba. Ahora el redirect ya no depende de que `/usuarios/me` conteste primero. Sin Node local no pude probarlo. |
| BUG-03 | 2.4 | Alta (seguridad) | Link de recuperación reutilizable tras cambio exitoso. | ✅ **verificado arreglado** — reintentar el link da "Este link ya no sirve..." |
| BUG-04 | 3.2 | Media/Alta | Mes actual adeudado se mostraba como "futuro"; después, el contador del header no coincidía con el calendario. | 🔧 **corregido, a retestear (casos 04-A…C)** — backend, escáner y `/socio/cuotas` ya estaban bien desde la ronda 3. Lo que seguía mal era **el inicio del socio**: tenía su propia copia vieja del cálculo, sin el corte por día de vencimiento. Las tres copias del frontend se unificaron en `utils/cuotas.js`. |
| BUG-05 | 3.3 | Alta | "Ver comprobante": roto para el socio (ya cerrado) y **inexistente** para el admin. | 🔧 **corregido, falta probarlo en el navegador** (el endpoint nuevo responde y pide auth) — componente compartido de comprobante en `/admin/verificaciones` (tarjeta del Pago, en cualquier estado), en el modal de verificación y en el historial de `/admin/socios`, **con opción de reemplazarlo** (endpoint nuevo `POST /admin/pagos/{id_pago}/comprobante`). UI sin verificar (no hay Node local). |
| BUG-06 | 3.3 (transversal) | Alta | Compras de cuota social no aparecían en ningún lado. | ✅ **verificado arreglado** — el historial en `/socio/cuotas` ahora muestra pagado, rechazado y cancelado con motivo. |
| BUG-07 | 3.4 | Media | Faltaba el mail de aprobación de cuota. | 🔧 **causa raíz encontrada y corregida, a retestear (casos 7-A…E)** — no era Resend, ni el checklist, ni el logging: `SessionLocal` usa `autoflush=False` y el mail se decidía con una query que leía la base **antes** de que el `orden.estado='aprobada'` bajara. El mail no salía **nunca**, por ninguno de los cuatro caminos que aprueban. Ver la ficha. |
| BUG-08 | 3.6 | **Bloqueante** | Pago en efectivo sin salida posible. | ✅ **verificado arreglado** — probado con un pago real de 3 meses en efectivo, aprobado sin pedir comprobante, meses acreditados bien. |
| BUG-09 | 3.5 | Media | El cobro manual/ventanilla siempre quedaba etiquetado "Pagado por transferencia". | 🔧 **corregido, falta probarlo en el navegador** — el `Pago` no recibía `metodo_pago` y tomaba el default de la columna. Ahora el modal de cobro tiene selector Efectivo/Transferencia (default efectivo) y queda en el Pago, en `notas_admin` y en auditoría. ⚠️ Los registros viejos siguen diciendo "transferencia" — ver ficha. |
| BUG-10 | 3.7 | Media | El filtro "Menores" en `/admin/socios` no devolvía resultados. | ✅ **corregido y VERIFICADO EN EJECUCIÓN** — el listado real devuelve las 150 filas con `fecha_nacimiento`, y el filtro "Menores" pasa de 0 a **38 socios**. El filtro estaba bien: `UsuarioListResponse` no incluía `fecha_nacimiento`, así que la edad salía `undefined` para todos. Se agregó ese campo y `fecha_ingreso`. Arregla de arrastre el precio de menor mostrado en el listado. |
| BUG-11 | 3.7 | Media | Notificación in-app incorrecta ("te sacaron: socio") al editar la fecha de nacimiento. | 🔧 **corregido en dos tandas, a retestear (casos N2-A/B)** — la ronda 3 arregló el backend (el "estado después" ahora incluye los roles protegidos). La ronda 4 agregó lo que faltaba: el frontend mandaba el PUT de roles **en toda edición**, así que cada cambio de fecha de nacimiento dejaba un `CAMBIO_ROLES` en auditoría. Ahora solo se manda si cambiaron. |
| BUG-12 | 3.7 | **Bloqueante** | El descuento de menores se perdía en el checkout real: se cobraba y registraba precio de adulto. | ✅ **corregido y VERIFICADO EN EJECUCIÓN** — probado de punta a punta con un socio menor real de la base: pantalla $3.000, checkout $6.000, y el precio congelado en el DetalleOrden $3.000. El descuento estaba implementado 4 veces y **faltaba en `socio_carrito.py`**, que es el checkout real. Motor de precios unificado en `utils/precios.py`. 2 meses de un menor = **$6.000** (el bug daba $10.000). Con regresión automática nueva en `qa_seguridad.py`. ⚠️ Las órdenes ya creadas conservan el monto viejo. |
| BUG-14 🆕 | 2.4 | Media | `/auth/recuperar-password` devolvía **HTTP 500** cuando el envío del mail fallaba (reproducido en dev con `RESEND_API_KEY` vacía: `Illegal header value b'Bearer '`). Preexistente — lo encontré verificando D2, no lo introdujo D2. | ✅ **corregido y verificado** — ahora responde 200 con `estado: "error_envio"` y un mensaje accionable, y el log dice la causa real. |
| BUG-13 | 3.3 / 3.9 | Baja | "Cancelar Trámite" usaba el `confirm()` nativo del navegador. | 🔧 **corregido, a retestear** — `/socio/cuotas` ya usaba `<ConfirmDialog>` desde la ronda 3. En la ronda 4 se reemplazaron los que quedaban en el camino del socio y del admin: "Vaciar carrito" y "Aprobar Pago Completo" (con su `alert()` de errores, ahora in-app). |
| BUG-15 🆕 | 3.3 / 3.4 | Media | Tras reemplazar un comprobante desde el panel, la vista sigue mostrando el archivo **viejo** hasta que se aprueba la orden. | 🔧 **corregido, a retestear (casos N1-A/B)** — cada tarjeta de Pago y cada modal guardaba su propia copia de la URL, leída al montarse. Ahora la URL vive en la pantalla, indexada por `id_pago`, y la comparten los dos. |

Las 5 decisiones de negocio (D1, D2, D3, D5 — D4 ya estaba cerrada) quedaron **implementadas**. **D1 y D2 están verificadas en ejecución** contra la base de dev (alta manual real y los tres caminos del mensaje de recuperar contraseña); D3 y D5, verificadas a nivel de template y de notificación creada. Pendientes de deploy. El detalle de cada una está más abajo, en "Cómo retomar la corrida (ronda 3)" → "Qué se implementó de D1/D2/D3/D5".

| # | Sección | Severidad | Resumen | Estado |
|---|---------|-----------|---------|--------|
| BUG-16 | 4.2 | Baja | Escáner no distingue "mes de ingreso" de "al día" | 🔧 **corregido, a retestear (casos 16-A/B)** — `en_mes_ingreso` ya se calculaba en el motor de cuotas pero el escáner lo tiraba: `_calcular_estado_financiero()` devolvía solo `(moroso, meses)`. Ahora viaja en la respuesta, el cartel dice **"SOCIO NUEVO ✓"** (verde, igual que al día) y la tarjeta suma el chip "Mes de ingreso — 1ª cuota pendiente". |
| BUG-17 | 4.2 | Baja | Escáner no indica si el socio es menor de edad | 🔧 **corregido, a retestear (caso 17-A)** — `es_menor` + `edad` se agregaron a la respuesta del escáner, calculados con `utils.precios.es_menor()`, la MISMA regla que el descuento y el filtro "Menores" de `/admin/socios`. Sin fecha de nacimiento cargada no se asume nada. |
| BUG-18 | 5 | Media | Socio no puede reemplazar comprobante de reserva en `/mis-compras` | 🔧 **corregido, a retestear (casos 18-A/B)** — era solo frontend: el endpoint `POST /socio/cuotas/pagos/{id}/comprobante` ya servía para cualquier Pago del socio (cuota, alquiler o tienda), pero la tarjeta de `/mis-compras` solo ofrecía el uploader cuando **no** había comprobante. Ahora hay "Cambiar comprobante". |
| D6 | 3.2 / 5.3 | — | Cuotas en `/mis-compras` + links de las notificaciones | 🔧 **implementada, a retestear (casos D6-A…D)** — ver la ficha al pie. |

| BUG-19 🆕 | 5 / admin/reservas | Alta | `POST /admin/reservas/bloqueo` falla con CORS/401 en producción — endpoint nuevo de la ronda 6 sin probar en navegador | 🔧 **causa raíz encontrada y corregida, a retestear (casos 19-A…D)** — no era CORS ni auth: `reservas_instalaciones.id_producto` es **NOT NULL** y el endpoint insertaba la fila sin ese campo. El `IntegrityError` subía sin manejar y el 500 salía **por fuera** del `CORSMiddleware`, así que el navegador reportaba el síntoma equivocado. Reproducido y verificado contra el schema real. |
| BUG-20 🆕 | 5 / admin/reservas | Alta | Admin suspende reserva pagada sin aviso al socio ni flujo de devolución | 🔧 **corregido, a retestear (casos 20-A…D)** — la orden pasa a `rechazada` con motivo de club (`/mis-compras` la muestra como **"Cancelada por el club"**, ámbar, no rojo), se crea notificación in-app `reserva_cancelada`, y el modal del admin avisa **antes** de confirmar si el turno se había pagado por transferencia. El saldo a favor y el mail ya existían. |
| Mejora-01 | 5 / carrito | Baja | Turno en carrito no se libera al cerrar sesión | 🔧 **corregido, a retestear (caso M1-A)** — el TTL de 20 min ya existía pero solo corría desde el job de **4 horas**. Ahora también corre en el de **15 minutos**, así que el turno se libera solo a los ~20-35 min sin depender de que el socio vacíe el carrito. |
| Mejora-02 | 5 / admin/reservas | Media | Botón "Nueva reserva" no corresponde al sistema de turnos fijos | 🔧 **implementada, a retestear (casos M2-A…D)** — se eliminó el botón del header. Un turno libre ahora abre "Asignar a un socio" / "Inhabilitar turno". El precio sale del catálogo por el turno, no de una lista, y al socio le llega notificación in-app. |

Severidad: **Bloqueante** (impide lanzar) · **Alta** (rompe un flujo, hay workaround) · **Media** (molesto, no rompe) · **Baja** (cosmético / nice-to-have) · **Duda** (no sé si es el comportamiento esperado).

---

## Plantilla de hallazgo

```
### BUG-nn — <título corto>

- **Sección:** <nº y nombre del checklist>
- **Severidad:** Bloqueante | Alta | Media | Baja | Duda
- **Usuario / rol:** <con qué cuenta>
- **Dispositivo / navegador:** <desktop Chrome | Android gama baja | iPhone | ...>
- **Pasos para reproducir:**
  1.
  2.
  3.
- **Esperado:**
- **Obtenido:**
- **Evidencia:** <screenshot, request/response, id de orden/usuario, log>
- **Notas:**
```

---

## Usuarios de prueba usados en esta corrida

| Alias | DNI | Rol(es) | Estado | Cómo se creó | Notas |
|-------|-----|---------|--------|--------------|-------|
| admin | 42542525 | admin_general | activo | ya existía | |
| socio-aldia (1) | 77000001 | socio | al día → pagó cuotas durante el test; reservas de quincho en ronda 7 | `/registro` + aprobación | mail `tmtwifi0@gmail.com`. Pass final: `wewewewe2` (post-recupero) |
| socio-rechazado→alta-manual | 77000002 | socio | primero rechazado, luego recreado por alta manual | `/registro` (rechazado) → `/admin/socios` alta manual (mismo DNI reusado) | mail `merlina.cabrera@alu.ing.unlp.edu.ar` |
| socio-moroso-1mes | 42542572 | socio | moroso, 1 mes | existente en data | mail `cmerlina402@gmail.com` |
| socio-moroso-7meses | 37889456 | socio | moroso, 7 meses | existente en data | mail inexistente (a propósito) |
| socio-moroso-15meses | 36778899 | socio | moroso, 15 meses | existente en data | mail inexistente (a propósito) |
| socio-alta-manual-2 | 77000003 | socio | requiere_cambio_password | alta manual `/admin/socios` (09-09) | usado para re-testear BUG-02; mail inexistente a propósito |
| socio-menor (reutilizado) | 77000002 | socio | menor (nac. 2015, editado a mano) | mismo DNI 77000002 de antes, editado desde `/admin/socios` | usado para BUG-10, BUG-11, BUG-12 |
| socio-baja | — | — | — | **pendiente de crear** | no llegó a probarse todavía |
| staff | — | personal_administrativo | — | **pendiente de crear** | no llegó a probarse todavía (bloqueado indirectamente por BUG-01: crear este rol ahora mismo reproduciría el bug de routing, no un caso limpio de "staff") |
| portero | — | admin_temporal | — | **pendiente de crear** | mismo bloqueo que arriba — ver nota en §4.2 |

---

## 1. Alta de socio

### 1.1 Registro público
| Ítem | Resultado |
|------|-----------|
| Form `/registro` completo → "solicitud recibida" | OK |
| Llega mail `solicitud_recibida` al socio | OK |
| Llega mail `aviso_admin_nuevo_socio` al admin | OK |
| La solicitud aparece en `/admin/solicitudes` | OK |
| DNI ya existente → error claro, no crea nada | OK |
| Email ya existente → error claro | OK |
| Campos obligatorios vacíos → validación del form | OK |

**Observaciones:** Probado con DNI 77000001 y 77000002. Los tres casos de validación (DNI repetido, mail repetido, campos incompletos) se probaron explícitamente y dieron error claro en los tres.

### 1.2 Aprobación
| Ítem | Resultado |
|------|-----------|
| `/admin/solicitudes` → Aprobar | OK |
| Llega mail `cuenta_aprobada` con instrucciones | OK |
| El socio aparece activo en `/admin/socios` | OK (implícito — se pudo gestionar el socio desde ahí después) |
| Arranca **al día**, no moroso (bug #8) | OK — ver **D1** (pregunta de negocio: ese mes figura "pagado" sin pago real) |
| Loguea con provisoria `car` + últimos 5 del DNI | N/A — el registro público define su propia contraseña en el form (no usa una provisoria estándar). Este ítem probablemente aplica a una carga masiva inicial de socios existentes (no testeada en esta corrida), no al alta vía `/registro`. |

**Observaciones:** El socio detecta "primer ingreso" igual al loguear por primera vez y pide cambio de contraseña obligatorio — ver **BUG-02**.

### 1.3 Rechazo
| Ítem | Resultado |
|------|-----------|
| Rechazar con motivo | OK |
| Llega mail `solicitud_rechazada` | OK |
| Ese usuario no puede loguear | OK — mensaje "no está registrado en el sistema" (no dice "pendiente", es correcto) |

**Observaciones:** Probado con DNI 77000002. El mismo DNI se reutilizó después con éxito para un alta manual — no pareció haber conflicto por el intento previo rechazado.

### 1.4 Alta manual desde admin
| Ítem | Resultado |
|------|-----------|
| `/admin/socios` → crear socio nuevo | OK |
| Llega mail `bienvenida_alta_manual` | OK |
| Arranca al día, no moroso (bug #8) | OK |
| Fuerza cambio de contraseña en el primer login | OK (pide cambio) — ver **BUG-02** para lo que pasa después de cambiarla |
| Queda registrado en `/admin/auditoria` | OK — confirmado: "Creó un socio manualmente" con actor y timestamp |

**Observaciones:** Ver **D3** — el mail de bienvenida no incluye la contraseña temporal que cargó el admin; el socio no tiene forma de saber qué contraseña usar salvo que se lo comuniquen fuera del sistema.

**Hallazgos §1:** Ninguno directo (los relacionados — BUG-02 y D3 — se detallan/aplican en §1.4 y §2.3).

---

## 2. Login y primer ingreso

### 2.1 Login OK
| Ítem | Resultado |
|------|-----------|
| DNI + pass correcta → home del rol, sin doble redirect (regresión #4) | **BUG-01** — funcionó bien para 77000001/77000002 (van a `/socio`), pero 2 de 3 socios morosos probados fueron enviados a `/admin` en vez de `/socio` |
| Menú hamburguesa muestra solo opciones del rol | OK (confirmado con 77000001) |

### 2.2 Login fallido
| Ítem | Resultado |
|------|-----------|
| Pass incorrecta → error, no entra | OK (confirmado al intentar con la contraseña vieja tras un cambio) |
| DNI inexistente → mismo mensaje genérico que pass mala | — (no probado explícitamente) |
| Varios intentos → rate limiting con mensaje claro | OK — ~5 intentos y avisa "probá en unos minutos" |
| Tras esperar el bloqueo, se puede reintentar | — (no se esperó a confirmar) |

### 2.3 Primer ingreso / cambio obligatorio
| Ítem | Resultado |
|------|-----------|
| Login primer-login → redirige a `/cambiar-password-obligatorio` | OK |
| No se puede navegar a otra ruta hasta cambiar | — (no probado explícitamente) |
| Cambiar pass → redirige a `/socio` **sin re-login** (regresión #5, token nuevo) | **BUG-02** — no redirige a ningún lado, queda en la misma pantalla |
| Logout + login con la pass nueva → OK | OK (yendo manualmente a `/login`) |
| La provisoria vieja ya no funciona | OK — confirmado, la rechaza |

### 2.4 Recuperar password
| Ítem | Resultado |
|------|-----------|
| `/recuperar-password` → mensaje neutro | OK — ver **D2** |
| Llega mail `recuperar_password` con link | OK |
| El link abre el form; cambiar → login con la nueva OK | OK — y en este flujo **sí** redirige correctamente al login (a diferencia de 2.3) |
| Reusar el mismo link → error (token usado) | **BUG-03** — vuelve a dejar entrar y pide cambio de contraseña de nuevo |
| Link vencido → error claro | — no confirmado del todo: tras "cierto tiempo" el link seguía abriendo, pero no se llegó a probar si permitía efectivamente completar el cambio ni si ya habían pasado los 60 minutos declarados. Relacionado con BUG-03, mismo sospechoso: el token no se estaría invalidando bien. |
| Email inexistente → mismo mensaje neutro y NO llega mail | OK — verificado el 09-09. Ver **D2**: se decidió que este mensaje neutro cambie a uno específico. |
| Pedirlo 6 veces → el 6º avisa rate limit | OK — verificado el 09-09 |

**Hallazgos §2:** BUG-01, BUG-02, BUG-03.

---

## 3. Cuotas

### 3.1 Estado de cuenta — socio al día
| Ítem | Resultado |
|------|-----------|
| `/socio/cuotas`: "al día" + próximo vencimiento día 10 | OK |
| Calendario: pagos en verde, futuros sin marcar | OK — ver **D1** sobre el mes de alta mostrado como "pagado" |
| QR en `/socio` muestra "al día" | OK |

### 3.2 Estado de cuenta — socio moroso
| Ítem | Resultado |
|------|-----------|
| Deuda = meses adeudados × precio cuota actual | OK — confirmado con 7 meses × $5.000 = $35.000 |
| Calendario marca los meses adeudados | **BUG-04** — el mes actual (el que corresponde a "hoy") se muestra como "futuro" en vez de "adeudado", en los 3 morosos probados |
| QR muestra "moroso" | OK |

**Observaciones:** Probado con 3 cuentas: 1 mes (DNI 42542572), 7 meses (DNI 37889456), 15 meses (DNI 36778899). El bug del redirect a `/admin` (BUG-01) apareció con las primeras dos cuentas pero NO con la tercera — comportamiento inconsistente, no se identificó el patrón.

### 3.3 Pago por comprobante (socio)
| Ítem | Resultado |
|------|-----------|
| Elegir pagar 2 meses | OK |
| Monto = 2 × cuota (−40% si menor) | OK para socio mayor de edad ($10.000). Descuento de menor no probado en esta ronda. |
| Checkout → orden `pendiente_verificacion` | OK |
| **Regresión #2:** tras la orden, `/socio` y `/socio/cuotas` muestran "Pago en verificación" (azul), no "MOROSO" | — sigue sin poder probarse limpio: mientras el "mes de ingreso" se trate como pagado (ver **D1**, pendiente de implementar), no hay forma de aislar el caso de un socio genuinamente moroso con un pago recién generado. Retomar después de implementar D1. |
| QR sigue inhabilitado hasta aprobación (correcto) | — (no verificado explícitamente durante la espera) |
| Subir foto del comprobante → orden con adjunto | OK, se subió sin problema |
| Llega mail `orden_generada` | OK |
| La orden aparece en `/admin/verificaciones` | OK |

**Hallazgos §3.3:** BUG-05 (no se puede ver el comprobante propio), BUG-06 (no aparece en Mis Compras).

### 3.4 Aprobación / rechazo de pago (admin)
| Ítem | Resultado |
|------|-----------|
| `/admin/verificaciones` → la imagen del comprobante carga desde S3 | **BUG-05 (extendido)** — confirmado el 09-09: no carga NADA, ni siquiera un link roto. Ni `/admin/verificaciones` ni el modal de `/admin/socios` tienen ningún control para ver el comprobante. |
| Aprobar → se acreditan los meses, `mes_cubierto_hasta` avanza | OK |
| **Regresión #1:** pagué 2 meses → se acreditan **2** (no 1) | OK — confirmado: cobertura avanzó exactamente 2 meses. Reconfirmado también vía cobro manual en §3.5 (2 meses → 2 acreditados). |
| El socio pasa a "al día" / baja la deuda | OK (ya estaba al día, se mantuvo) |
| Llega mail `orden_aprobada_cuota` | Re-confirmado el 09-09: sigue sin llegar mail, y sin rastro en los logs de Render. Ya no se trata como bug — ver **D5** (decisión de negocio sobre cuándo debe salir el mail). |
| QR y calendario quedan coherentes | OK |
| Rechazar otra con motivo → mail `orden_rechazada`, meses NO se acreditan, bloqueo liberado | Parcial OK — probado sobre la orden de efectivo trabada (ver BUG-08): el mail y la notificación con motivo llegaron bien, los meses no se acreditaron, y el bloqueo del carrito se liberó correctamente tras el rechazo |

### 3.5 Cobro manual desde admin
| Ítem | Resultado |
|------|-----------|
| Admin registra pago de N meses desde el panel | OK — probado con DNI 77000001, 2 meses |
| **Regresión #1:** N registrados = N acreditados | OK — cobertura avanzó exactamente 2 meses (de "hasta feb 2027" a "hasta abr 2027 inclusive") |
| Queda en `/admin/auditoria` | OK — "Registrar pago manual" queda registrado con actor y timestamp |

**Hallazgos §3.5:** **BUG-09** — el registro queda etiquetado "Pagado por transferencia" en `/admin/socios` aunque haya sido un cobro manual/presencial, sin importar el método real. El socio recibe notificación in-app correcta ("Pago en ventanilla registrado"), pero con el mismo link roto a "Mis Compras" que ya se corrigió en otros lados (revisar si este también apunta a `/socio/cuotas`).

### 3.6 Método efectivo
| Ítem | Resultado |
|------|-----------|
| NO aparece botón de Mercado Pago (E5) | OK |
| Elegir "efectivo" → la orden llega **UNA sola vez** a `/admin/verificaciones` (regresión #3) | OK — verificado el 09-09: pago real de 3 meses ($15.000) en efectivo, aprobado sin pedir comprobante, meses acreditados. **BUG-08 confirmado arreglado.** Sugerencia (no bloqueante, parte de D4): que la carga de foto del efectivo sea opcional para el admin, como registro interno. |

**Hallazgos §3.4–3.6:** BUG-05 (extendido a admin), D5 (ex BUG-07), BUG-08 ✅ verificado.

> ✅ **Pausa levantada el 09-09.** Se retomó la corrida y se avanzó hasta la mitad de §4.2.

### 3.7 Descuento a menores
| Ítem | Resultado |
|------|-----------|
| El 40% se aplica en estado de cuenta, checkout y cálculo de deuda | **BUG-12 [BLOQUEANTE]** — se aplica bien SOLO en la vista inicial del carrito ($3.000 × 1 mes, correcto). En el checkout real (monto a transferir, orden guardada, vista de admin en verificaciones, notificación de aprobación) el monto vuelve a ser el precio completo sin descuento. |

**Observaciones §3.7:** Además del bug de precio: el filtro "Menores" en `/admin/socios` no encuentra a un socio menor recién editado (**BUG-10**). Y al editar la fecha de nacimiento de un socio para convertirlo en prueba de menor, le llegó una notificación in-app incorrecta de "te sacaron: socio" sin que el rol realmente haya cambiado (**BUG-11**).

### 3.8 Precio de cuota indexado
| Ítem | Resultado |
|------|-----------|
| Subir precio de cuota en config del club | — pendiente |
| Moroso: la deuda se recalcula al precio nuevo | — pendiente |
| Pago adelantado: quedó congelado al precio viejo, no se cobra diferencia | — pendiente |

### 3.9 Cancelación de trámite por el socio

_(sub-sección nueva, no estaba en el checklist original — se probó de paso)_

| Ítem | Resultado |
|------|-----------|
| Socio cancela un pago pendiente desde `/socio/cuotas` ("Cancelar Trámite") | OK — queda "Cancelado" en el historial del socio y "Cancelada por el socio" en `/admin/verificaciones` y `/admin/socios` |
| La confirmación es consistente con el resto de la UI (modal in-app) | **BUG-13** — dispara el `confirm()` nativo del navegador, no un modal in-app |

**Hallazgos §3:** BUG-04 (parcial/reencuadrado), BUG-05 (parcial), BUG-09, BUG-10, BUG-11, BUG-12 (bloqueante), BUG-13. Resueltos y verificados: BUG-06, BUG-08.

---

## 4. QR y escáner

### 4.1 QR del socio
| Ítem | Resultado |
|------|-----------|
| `/socio` muestra el QR completo y legible | OK |
| El QR refleja el estado real (al día / moroso) | OK — coherente en todos los socios probados hasta ahora. Falta el caso puntual de la transición moroso→al día en el instante de aprobación (bloqueado por D1, ver §3.4). |

### 4.2 Escáner en la puerta (`/admin/escaner`)

> 🔄 **En progreso — pausado a mitad de esta sub-sección.** Se empezó a escanear DNI 77000001, 77000002 y 77000003 desde el celu (admin general) contra cada estado de inicio del socio, pero los resultados no quedaron capturados en texto (probablemente por screenshots que no se transcribieron). Repetir esta prueba documentando el resultado en el próximo tramo.

| Ítem | Resultado |
|------|-----------|
| `portero` (admin_temporal) loguea → solo ve el escáner | — (bloqueado además por **BUG-01**: hasta que se corrija el routing por rol, no tiene sentido testear esto con el `portero` de prueba, porque no vamos a poder distinguir un bug de routing general de un problema puntual del rol `admin_temporal`) |
| QR de socio al día → verde / acceso OK, con nombre y foto | — en progreso, sin resultado capturado |
| QR de socio moroso → advertencia / rojo | — en progreso, sin resultado capturado |
| QR de socio dado de baja → rechazo | — |
| QR inválido / otro código → error claro, no rompe | — |
| Prueba con poca luz, en la puerta real, de noche | — |

### 4.3 Validación por DNI (fallback)
| Ítem | Resultado |
|------|-----------|
| Ingresar DNI a mano → mismo resultado que el QR | — |

**Hallazgos §4:**

---

## 5. Reservas de canchas / quincho

> Regla confirmada: **los morosos reservan normal y siguen morosos** (no hay bloqueo por estado de cuenta).

### 5.1 Pre-reserva del socio
| Ítem | Resultado |
|------|-----------|
| Elegir instalación, día y turno libre | — |
| Confirmar → reserva `bloqueada` + orden `pendiente_verificacion` | — |
| El turno pasa a naranja/ocupado para el resto al instante | — |
| Subir comprobante → orden con adjunto; mail `orden_generada` | — |
| Un socio moroso puede completar la pre-reserva sin bloqueo | — |

### 5.2 Doble reserva (condición de carrera)
| Ítem | Resultado |
|------|-----------|
| Dos socios, mismo turno, casi simultáneo → solo uno lo obtiene, sin orden fantasma | — |

### 5.3 Aprobación / rechazo (admin)
| Ítem | Resultado |
|------|-----------|
| Aprobar → reserva `confirmada`, verde; mail `orden_aprobada` | — |
| Rechazar → reserva `liberada`, turno libre, mail `orden_rechazada` | — |
| **Ronda nueva:** modal de turno pendiente → "Rechazar / Liberar turno" funciona (antes daba 404) | — |

### 5.4 Expiración sin pago
| Ítem | Resultado |
|------|-----------|
| Reserva `bloqueada` sin comprobante + pasa la hora de inicio → se libera | — |
| Free tier: al cargar el calendario la reserva vencida igual se libera (lazy) | — |
| Reserva con orden aprobada NO se libera por este camino | — |

### 5.5 Reserva manual del admin
| Ítem | Resultado |
|------|-----------|
| Crear reserva a mano (sin orden) → azul, no expira, no pisa una pre-reserva existente | — |

### 5.6 Cancelación del socio
| Ítem | Resultado |
|------|-----------|
| Socio cancela antes del turno → `cancelada_socio`, gris, turno liberado | — |

### 5.7 Lluvia / suspensión + saldo a favor
| Ítem | Resultado |
|------|-----------|
| Admin suspende reserva `confirmada` por lluvia → mail `reserva_suspendida` | — |
| Se acredita `saldo_a_favor` al socio por el monto correspondiente | — |
| **Ronda nueva:** el botón "Suspender por lluvia / mantenimiento" existe y funciona | — |
| Nuevo checkout → puede aplicar el saldo como descuento; total = total − saldo | — |
| El saldo restante queda bien registrado | — |
| Ajuste manual de saldo en `/admin/socios` exige motivo y queda en auditoría | — |

**Hallazgos §5:**

---

## 6. Perfil del socio

### 6.1 Editar datos
| Ítem | Resultado |
|------|-----------|
| Cambiar teléfono y dirección → guarda OK | — |
| Email y DNI NO editables por el socio | — |

### 6.2 Foto de perfil
| Ítem | Resultado |
|------|-----------|
| Subir foto → se ve en el perfil y en el escáner (regresión #9) | — |
| Subir desde galería y desde cámara (celu) | — |

### 6.3 Cambiar password desde el perfil
| Ítem | Resultado |
|------|-----------|
| Con pass actual correcta → cambia OK | — |
| Con pass actual incorrecta → error, no cambia | — |

**Hallazgos §6:**

---

## 7. Notificaciones

### 7.1 Badge de la campana
| Ítem | Resultado |
|------|-----------|
| **Regresión #6:** generar una notif desde otro navegador → el badge aparece solo, sin navegar, en <60 s | — |
| `/notificaciones` lista las notas | — |
| Marcar como leída → baja el contador | — |

### 7.2 Beca
| Ítem | Resultado |
|------|-----------|
| Admin asigna beca → genera notificación in-app, NO manda mail | — |

**Hallazgos §7:**

---

## 8. Panel admin

### 8.1 Dashboard `/admin`
| Ítem | Resultado |
|------|-----------|
| Las métricas cargan | — |
| Los accesos rápidos llevan a la página correcta | — |

### 8.2 CRUD socios `/admin/socios`
| Ítem | Resultado |
|------|-----------|
| Listar, filtrar y buscar funciona | — |
| Editar un socio → guarda | — |
| Dar de baja → mail `socio_dado_de_baja` | — |
| Reactivar → mail `socio_reactivado` | — |
| El admin no puede darse de baja a sí mismo | — |
| No se puede dar de baja al último admin activo | — |

### 8.3 Límites de "personal administrativo"
| Ítem | Resultado |
|------|-----------|
| `staff` puede ver socios y aprobar/rechazar órdenes | — |
| `staff` NO puede crear / editar / dar de baja socios (botón oculto o 403) | — |

### 8.4 Auditoría `/admin/auditoria`
| Ítem | Resultado |
|------|-----------|
| Toda acción crítica queda registrada | Parcial OK — confirmado para "creó socio manualmente", "aprobó solicitud" y "rechazó solicitud" (§1). El resto de las acciones administrativas no se revisó todavía. |
| La paginación funciona | — |
| El `admin_general` NO recibe mails de sus propias acciones | — |

**Hallazgos §8:**

---

## 9. Permisos y seguridad (transversal)

| Ítem | Resultado |
|------|-----------|
| `socio` navega a `/admin/*` → redirigido / "sin permiso" | Parcial — funcionó bien navegando manualmente a una URL de admin (`/admin/solicitudes` → redirige a `/socio`), pero **BUG-01**: el redirect automático post-login a veces manda a un socio a `/admin` en vez de a `/socio` |
| `socio` pega a endpoint admin con su token → 403 | — |
| `personal_administrativo` a endpoint de CRUD de socios → 403 | — |
| Request sin token a endpoint protegido → 401 | — |
| Token vencido → 401, la app fuerza re-login | — |
| Logout → el token deja de funcionar | — |
| URL de comprobante sin firmar en el navegador → acceso denegado (bucket privado) | Confirmado que deniega/no carga (ver **BUG-05**) — pero el problema es que se lo muestra como opción al propio dueño del comprobante sin una alternativa que sí funcione |
| **Ronda nueva:** forzar error de validación en un form admin → texto legible, nunca `[object Object]` | — |

**Hallazgos §9:** BUG-01 (parcial), BUG-05 (parcial).

---

## 10. Mobile (mobile-first)

Correr §2, §3, §4, §5 en dispositivos reales.

| Dispositivo | §2 | §3 | §4 | §5 | Notas |
|-------------|----|----|----|----|-------|
| Android gama baja | — | — | — | — | |
| iPhone | — | — | — | — | |

Por cada uno:
| Ítem | Android | iPhone |
|------|---------|--------|
| Menú hamburguesa abre/cierra bien | — | — |
| Formularios usables (teclado no tapa el botón) | — | — |
| Cámara del escáner QR enfoca y lee | — | — |
| Subida de foto desde galería y cámara | — | — |
| El QR del socio se ve completo en pantalla chica | — | — |

**Hallazgos §10:**

---

## 11. Zona horaria

Probar entre las 21 y 24 h de Argentina (en UTC ya es el día siguiente).

| Ítem | Resultado |
|------|-----------|
| Crear reserva / registrar pago → fecha guardada = día de Argentina, no UTC | — |
| Vencimiento "día 10": el socio queda moroso el día correcto, no el 9 a la noche | — |
| Socio que cumple años hoy → gana/pierde el descuento de menor en la fecha correcta | — |

**Observaciones (si se testeó de día, marcar N/A y aclarar):** Toda la corrida hasta ahora se hizo de día — sección no cubierta.

**Hallazgos §11:**

---

## 12. Mails

Verificar a medida que cada flujo los dispara. Por cada mail: (a) llega, (b) no cae en spam, (c) los links van a `https://www.clubatleticoroberts.com` (si van a `localhost` → E4 / bug #7), (d) se ve bien en el celular.

| Mail | Llega | Links OK | Mobile | Notas |
|------|-------|----------|--------|-------|
| `solicitud_recibida` | OK | OK | — | |
| `cuenta_aprobada` | OK | OK | — | |
| `solicitud_rechazada` | OK | OK | — | |
| `bienvenida_alta_manual` | OK | OK | — | No incluye la contraseña temporal — ver D3 |
| `orden_generada` | OK | OK | — | El link a "mis compras" no muestra la compra — ver BUG-06 |
| `orden_aprobada_cuota` | **BUG-07** | — | — | No llegó |
| `orden_aprobada` (reserva) | — | — | — | |
| `orden_rechazada` | OK | OK | — | Probado sobre el caso de efectivo (BUG-08); llegó con motivo |
| `orden_expirada` | — | — | — | |
| `recordatorio_comprobante` | — | — | — | |
| `cuota_vencida` | — | — | — | |
| `recuperar_password` | OK | OK | — | |
| `reserva_suspendida` | — | — | — | |
| `socio_dado_de_baja` | — | — | — | |
| `socio_reactivado` | — | — | — | |
| `aviso_admin_nuevo_socio` | OK | OK | — | |

**Hallazgos §12:** BUG-06, BUG-07, D3.

---

## 13. Jobs / scheduler

> **Saltada en esta corrida** (E2: Render dormido sin UptimeRobot). Correr cuando el scheduler esté vivo.

| Job | Resultado |
|-----|-----------|
| `expirar_ordenes_vencidas` | N/A |
| `expirar_reservas_sin_pago` | N/A |
| `recordatorio_comprobante_pendiente` | N/A |
| `notificar_cuotas_vencidas` | N/A |
| `cerrar_eventos_vencidos` | N/A |

**Hallazgos §13:**

---

## Regresiones — los 9 bugs ya corregidos (confirmar que siguen arreglados)

| # | Qué debe pasar ahora | Sección | Resultado |
|---|----------------------|---------|-----------|
| 1 | Pagar N meses acredita N | 3.4, 3.5 | OK |
| 2 | Con orden pendiente: "Pago en verificación", no "MOROSO" | 3.3 | — sigue sin poder probarse limpio; bloqueado por D1 (mes de ingreso) |
| 3 | Doble clic en confirmar NO duplica la orden | 3.6 | — no probado con doble clic explícito |
| 4 | Post-login cae en el home del rol, sin doble redirect | 2.1 | **Reaparición confirmada — BUG-01 reabierto.** Causa raíz identificada: cualquier rol adicional a `socio` manda a `/admin`. |
| 5 | Cambio de clave funciona y la sesión no se cae | 2.3 | **BUG-02 reabierto.** Se re-probó después del fix + deploy y el síntoma es idéntico: no redirige. |
| 6 | El badge de notificaciones aparece solo | 7.1 | — no probado |
| 7 | Links de mails al dominio real | 12 | **OK** — todos los links vistos apuntaron a `clubatleticoroberts.com`, ninguno a `localhost` |
| 8 | Alta (solicitud y manual) NO genera socio moroso | 1.2, 1.4 | OK — confirmado en los dos flujos |
| 9 | La foto de perfil carga bien | 6.2 | — no probado |

Ronda nueva (arreglos posteriores al 06-09):
| Ítem | Resultado |
|------|-----------|
| Escáner de eventos muestra nombre y registra asistencia | — |
| Modal de reserva → "Rechazar / Liberar turno" funciona | — |
| "Suspender por lluvia" libera turno + acredita saldo | — |
| Mensajes de error legibles, nunca `[object Object]` | — |
| Recuperar contraseña: el 6º intento avisa rate limit | — |

---

## Detalle de hallazgos

### BUG-01 — Socio con rol único es enviado a `/admin` tras login (inconsistente)

- **Sección:** 2.1 (Login OK) / 9 (Permisos)
- **Severidad:** Alta
- **Usuario / rol:** socio (rol único, sin admin) — DNI 42542572 y DNI 37889456 lo reprodujeron; DNI 36778899 NO (fue directo a `/socio`)
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Loguear con un socio moroso que tenga únicamente el rol `socio`.
  2. Observar a qué ruta redirige tras el login exitoso.
- **Esperado:** Redirige a `/socio` (home del rol).
- **Obtenido:** En 2 de 3 cuentas probadas, redirige a `/admin`, donde las tarjetas de métricas muestran "error al cargar" (no carga datos, pero sí se ve la interfaz del panel admin).
- **Evidencia:** Reproducido con DNI 42542572 y 37889456. NO reproducido con DNI 36778899 (fue directo a `/socio`, comportamiento correcto).
- **Notas:** Comportamiento inconsistente entre cuentas con el mismo rol — no se identificó qué diferencia a la cuenta que sí funcionó bien. Podría ser una condición de carrera en el chequeo de rol post-login, o un resabio de sesión/token cacheado de una prueba anterior en el mismo navegador. Aunque no carga datos sensibles, mostrar la shell del panel admin a una cuenta sin ese permiso es un problema — habría que revisar `homePorRol()` y la lógica de routing post-login. Relacionado con la observación de auditoría previa de Claude Code sobre el orden de roles en `homePorRol()`.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** el frontend calculaba los roles a partir de `roles_asignados` **crudo**,
  sin filtrar nada. El backend (`dependencies.require_roles`) sí descarta los roles con
  `rol.es_activo = false` y las asignaciones vencidas (`valido_hasta` en el pasado).
  Resultado: una cuenta con una asignación vieja de admin —desactivada o vencida— era
  tratada como admin por el frontend (`homePorRol` → `/admin`, `RequireRole` la dejaba
  pasar) y como socio por el backend (403 en todos los endpoints). De ahí el shell del
  panel con las métricas en "error al cargar".
  Eso también explica la **inconsistencia entre cuentas**: dependía de qué asignaciones
  viejas arrastrara cada DNI, no de una condición de carrera.
- **Qué se hizo:** `rolesDeUsuario()` en `components/RequireRole.jsx` ahora filtra por
  `es_activo` y `valido_hasta`, y pasó a ser la única fuente de verdad de roles del
  frontend. Se eliminaron las 4 copias sueltas que había (`MainLayout`, `Login`,
  `TecnicoEventos`, `TecnicoPlanteles`, `Landing`). En el backend se agregó `es_activo`
  a `RolResponseSimple` para que el dato llegue.
- **Efecto colateral bueno:** el menú hamburguesa deja de mostrar secciones que el backend
  después rechaza.
- **Al re-testear:** si vuelve a pasar, ya NO es este camino — mirá en `/admin/socios` qué
  roles tiene realmente esa cuenta, porque entonces los tiene de verdad y activos.

**🔴 Reabierto (2026-09-09) — causa raíz real, distinta a la del fix**

- **Lo que encontró la tester:** el fix corrigió el caso de roles viejos/vencidos, pero el
  síntoma sigue pasando con roles **vigentes y reales**. DNI 42542572 y 37889456 tienen,
  además de `socio`, otros roles activos (`personal_administrativo`, `personal_tecnico`,
  escáner). DNI 36778899 tiene únicamente `socio` — y ese sí cae en `/socio` correctamente.
- **Regla de negocio, definida explícitamente por la tester:** *"independientemente de la
  cantidad de roles que tenga asignado, todos los SOCIOS deben ir por defecto a `/socio`, el
  único que va para `/admin` es el ADMIN GENERAL."* Es decir, ni siquiera `personal_administrativo`
  o `admin_temporal` (portero) deben aterrizar en `/admin` — solo `admin_general`. El resto
  navega a sus herramientas específicas desde el menú, no como landing automático.
- **Qué falta corregir:** `homePorRol()` (y cualquier lugar que decida el destino post-login)
  tiene que dejar de preguntar "¿tiene algún rol admin-like?" y preguntar en cambio
  "¿tiene específicamente `admin_general`?" — si no, siempre `/socio`.
- **Al re-testear:** repetir login con DNI 42542572 y 37889456 (múltiples roles) y confirmar
  que ahora sí van a `/socio`. Probar también con un `admin_temporal` puro (portero) una vez
  que se cree ese usuario de prueba — según la regla nueva, también debería ir a `/socio`.

**Corrección aplicada (ronda 3) — a re-testear**

- **Regla implementada, tal cual se definió:** el único rol que aterriza en `/admin` es
  `admin_general`. Cualquier otra cuenta cae en `/socio`, sin importar cuántos roles más
  tenga. `personal_administrativo` y `personal_tecnico` ya no tienen landing propia: sus
  herramientas se abren desde el menú.
- **Qué se tocó:** `homePorRol()` en `components/RequireRole.jsx`. Antes preguntaba
  "¿tiene algún rol admin-like?" (y `personal_administrativo` entraba en esa bolsa, y
  `personal_tecnico` iba a `/gestion-planteles`); ahora pregunta por `admin_general` y nada
  más. En `Login.jsx` solo se actualizaron los comentarios y se renombró `esAdmin` →
  `puedeEntrarAlPanel`.
- **Loop de redirects evitado a propósito:** mandar a `/socio` a un `admin_temporal` puro
  (portero) o a un `invitado` de comercio sería mandarlo a una ruta que `RequireRole`
  rechaza — y al rechazarla, `RequireRole` vuelve a llamar a `homePorRol()`, que lo manda
  otra vez ahí: redirect infinito. Para que no pueda pasar, las listas de roles de cada
  área se exportan (`ROLES_AREA_SOCIO`, `ROLES_AREA_ESCANER`) y ahora `App.jsx` monta las
  rutas con **esas mismas** constantes, así el guard de la ruta y el cálculo del destino no
  pueden discrepar. Un portero/invitado puro sigue cayendo en `/admin/escaner`.
- ⚠️ **Diferencia con lo pedido, a confirmar:** se pidió que un `admin_temporal` puro
  también fuera a `/socio`. No se hizo, por lo de arriba: hoy `/socio` no admite ese rol y
  el resultado sería una pantalla que rebota. Si el club quiere que un portero vea la home
  de socio, hay que **agregar `admin_temporal` a `ROLES_AREA_SOCIO`** — pero eso le
  mostraría QR de acceso, cuotas y comercios a alguien que no es socio, así que es una
  decisión de producto, no un detalle de implementación. Decime cuál preferís.
- **Al re-testear:** login con DNI 42542572 y 37889456 (multi-rol) → tienen que caer en
  `/socio`. Confirmar además que desde el menú siguen llegando a `/admin/verificaciones` y
  a `/gestion-planteles` según su rol.

**Ronda 4 (2026-09-11) — faltaba un pedazo, y era del backend**

- El redirect por rol de la ronda 3 estaba bien: `homePorRol()` manda a `/admin` solo a
  `admin_general` y todo el resto a `/socio`, sin importar cuántos roles arrastre.
- Pero `rolesDeUsuario()` (en `components/RequireRole.jsx`) dice filtrar las asignaciones
  cuyo rol del catálogo está **desactivado**, igual que hace `require_roles()` en el
  backend. Y **`RolResponseSimple` nunca mandó `es_activo`**: la comparación del frontend
  era contra `undefined`, o sea no filtraba nada. Una cuenta con una asignación vieja de
  admin desactivada aterrizaba igual en `/admin` y se encontraba el panel lleno de 403,
  porque el backend sí la filtraba. Es exactamente el síntoma "inconsistente entre cuentas"
  del reporte original: dependía de qué asignaciones viejas tuviera cada DNI.
- **Corregido:** `es_activo` viaja en el schema.
- ⚠️ Este fix es de **backend**. Con el frontend nuevo y el backend viejo, el síntoma sigue.

| Caso | Qué tiene que pasar |
|---|---|
| 01-A | DNI 42542572 (multi-rol) → login cae en `/socio` |
| 01-B | DNI 37889456 (multi-rol) → login cae en `/socio` |
| 01-C | DNI 36778899 (solo socio) → login cae en `/socio` |
| 01-D | DNI 42542525 (admin general) → login cae en `/admin` |
| 01-E | Con un rol desactivado desde el catálogo, el socio sigue cayendo en `/socio` |

---

### BUG-02 — Cambio de contraseña obligatorio no redirige tras éxito

- **Sección:** 2.3 (Primer ingreso / cambio obligatorio)
- **Severidad:** Alta
- **Usuario / rol:** socio, primer login (`requiere_cambio_password = true`) — reproducido con DNI 77000001 (post-registro público) y DNI 77000002 (post-alta manual)
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Loguear por primera vez con una cuenta que tiene `requiere_cambio_password = true`.
  2. Completar el formulario de cambio de contraseña obligatorio con una contraseña nueva (distinta a la provisoria).
  3. Confirmar el cambio.
- **Esperado:** Redirige automáticamente a `/socio` (u otro home del rol), sin necesidad de loguear de nuevo (el backend ya devuelve un token nuevo).
- **Obtenido:** No pasa nada visible — la página se queda en `/cambiar-password-obligatorio`, sin mensaje de éxito ni redirect. Hay que navegar manualmente a `/login` e ingresar con la contraseña nueva para poder entrar.
- **Evidencia:** Reproducido 2/2 veces, con dos flujos de alta distintos (registro público y alta manual).
- **Notas:** El cambio de contraseña en sí funciona bien del lado del backend (la contraseña vieja deja de servir, la nueva sí funciona en `/login`), así que esto parece un bug puramente de frontend: falta el manejo post-éxito (mostrar confirmación + `navigate()` a home). Contrastar con §2.4 (recuperar contraseña), donde el flujo de cambio de password SÍ redirige correctamente al login — sugiere que el fix ya existe en un lugar y falta aplicarlo en este otro componente.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** carrera de orden, tal como se sospechaba: era del frontend. `aplicarToken()`
  disparaba la recarga del perfil pero **no la esperaba**, así que `navigate('/socio')` corría
  con el `user` viejo todavía en memoria — con `requiere_cambio_password = true`. `RutaPrivada`
  hacía exactamente lo que tiene que hacer: rebotarlo de vuelta a
  `/cambiar-password-obligatorio`. Visto desde afuera, el submit "no hacía nada".
- **Qué se hizo:** `aplicarToken()` (`context/AuthContext.jsx`) ahora es `async` y devuelve el
  perfil ya recargado; `CambiarPasswordObligatorio.jsx` lo espera antes de navegar. De paso
  navega a `homePorRol()` en vez de a `/socio` fijo, así un técnico o un administrativo con
  contraseña temporal también cae en su pantalla.

**🔴 Reabierto (2026-09-09) — el fix no resolvió el síntoma**

- **Re-test:** alta manual de un socio nuevo (DNI 77000003), primer login, cambio de
  contraseña obligatorio. Resultado: **idéntico al reporte original** — tras guardar, queda
  en `/cambiar-password-obligatorio`, hay que ir a mano a `/login`.
- **A investigar:** con la causa raíz que se había diagnosticado (`aplicarToken()` no
  esperado antes de navegar), el fix debería haber andado. Cosas a chequear antes de tocar
  código de nuevo:
  1. Confirmar que el deploy del frontend realmente incluye este cambio (revisar el commit
     desplegado en el build de CloudFront/S3 vs. el commit donde se hizo el fix).
  2. Si el deploy es correcto, revisar si `homePorRol()` está devolviendo algo que
     `navigate()` no sabe manejar para este rol en particular, o si `RutaPrivada` está
     evaluando el `user` de un contexto que todavía no se actualizó pese al `await`.
  3. Probar en una ventana de incógnito (para descartar caché de un build de frontend viejo
     servido por el navegador o por CloudFront).
- **Al re-testear:** después de confirmar el punto 1, repetir el flujo completo y mirar la
  consola del navegador por cualquier error silencioso durante el submit.

**Corrección aplicada (ronda 3) — a re-testear**

- **Punto 1 del pedido: el deploy SÍ había llegado.** Verificado con `gh run list`: el
  workflow "Deploy Frontend a S3" corrió y terminó en `success` para el commit `bd25364`
  ("8 bugs: qa-manual hasta #3.6"), que es el que contiene el fix de la ronda 2, el
  2026-09-09 a las 16:47 UTC — o sea horas antes del re-test. El workflow además invalida
  CloudFront con `--paths "/*"`. Así que no era un problema de deploy: **el fix estaba
  publicado y no alcanzaba**.
- **Por qué no alcanzaba:** el fix de la ronda 2 hizo que `aplicarToken()` esperara el
  refresh del perfil antes de navegar. Eso corrige el orden, pero deja el redirect
  **colgado de que `/usuarios/me` conteste bien y rápido**. Si esa request falla o tarda
  —token recién rotado, cold start de Render de 40–60 s (condición E1 de este mismo
  documento), un 401 de borde por la granularidad de `iat`, una respuesta cacheada—, el
  `user` en memoria sigue con `requiere_cambio_password = true`, `RutaPrivada` rebota a
  `/cambiar-password-obligatorio` y desde afuera se ve exactamente igual que antes: el
  botón "no hace nada".
- **Qué se hizo, invirtiendo la dependencia:** el 200 de `POST /usuarios/me/password` ya es
  la confirmación de que la contraseña cambió y de que el backend puso el flag en false
  (lo hace en la misma transacción). Con eso alcanza para desbloquear la navegación. La
  función nueva `confirmarCambioPassword()` (`context/AuthContext.jsx`) adopta el token
  nuevo y baja el flag en memoria de forma **sincrónica**, y recién después refresca el
  perfil en segundo plano sin que el redirect espere nada.
- **Además, para que un fallo no vuelva a ser invisible:** la pantalla ahora muestra
  "Contraseña actualizada. Entrando…" al confirmar, y si algo falla muestra un texto de
  error incluso cuando la excepción viene sin mensaje (antes un `err.message` vacío
  renderizaba un `<p>` vacío: cero feedback). Se agregó también la guarda de doble submit
  con `useRef`, como en el resto de los botones críticos.
- ⚠️ **Sin verificar por mí.** No tengo Node ni npm en este entorno, así que no pude
  levantar el frontend ni reproducir el flujo. El razonamiento y el cambio están, pero la
  confirmación tiene que ser tuya.
- **Al re-testear:** alta manual nueva → primer login → cambiar contraseña. Esperado: ver
  el cartel verde y caer en `/socio` sin tocar nada. Si volviera a fallar, ahora sí debería
  aparecer un mensaje de error en pantalla: copialo, porque señala exactamente el punto.

---

### BUG-03 — Link de recuperación de contraseña reutilizable tras cambio exitoso

- **Sección:** 2.4 (Recuperar password)
- **Severidad:** Alta (seguridad)
- **Usuario / rol:** socio — DNI 77000001
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Pedir recuperación de contraseña desde `/recuperar-password`.
  2. Abrir el link del mail y completar el cambio de contraseña exitosamente.
  3. Volver a abrir el mismo link (mismo token en la URL) por segunda vez.
- **Esperado:** El link ya usado debería mostrar un error (ej. "este link ya fue utilizado" / "token inválido").
- **Obtenido:** El link vuelve a abrir con normalidad y vuelve a pedir el cambio de contraseña, como si fuera la primera vez.
- **Evidencia:** Reproducido una vez, de forma inmediata después del primer uso exitoso.
- **Notas:** Relacionado, sin confirmar del todo: después de que pasara "cierto tiempo" (no se confirmó si superó los 60 minutos declarados como validez), el link seguía abriendo. No se llegó a probar si en ese momento tardío efectivamente dejaba completar el cambio o si ahí sí tiraba error de vencido. Revisar en el backend que, tras un cambio de contraseña exitoso, se invalide `token_recuperacion` (poniéndolo en `NULL`) y no solo se dependa de la expiración por tiempo.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** el backend **sí estaba bien** — `resetear_password` pone
  `token_recuperacion = NULL` apenas se usa. Lo que fallaba es que
  `RecuperarPassword.jsx` mostraba el formulario de contraseña nueva con solo ver un
  `?token=` en la URL, sin preguntarle nada al backend. Un link ya usado se veía idéntico
  a uno nuevo, y el error recién habría aparecido al enviar el formulario.
  **O sea: la contraseña no se podía cambiar dos veces; lo que engañaba era la pantalla.**
  Eso también explica el ítem "link vencido" que quedó en duda: el link "seguía abriendo"
  porque la pantalla nunca chequeaba nada.
- **Qué se hizo:** nuevo endpoint `GET /auth/reset-password/estado?token=…` que responde
  `{valido: true|false}` (misma respuesta para inexistente, vencido y ya usado — no filtra
  información). La pantalla lo consulta al montar y, si no es válido, muestra "Este link ya
  no sirve" con un botón para pedir uno nuevo. Si la consulta falla por red, no bloquea:
  deja intentar y que el POST decida.
- **De paso:** `resetear_password` ya no revienta si `token_recuperacion_expira` quedara en
  NULL (antes comparaba `None < datetime` → 500).
- **Al re-testear, probar los 3 casos:** link fresco (entra), link ya usado (rechaza),
  link de más de 1 hora (rechaza).

**✅ Verificado arreglado (2026-09-09)**

Se probaron los 3 casos: cambio de contraseña exitoso, y al reabrir el mismo link aparece
correctamente *"Este link ya no sirve: o ya lo usaste para cambiar tu contraseña, o pasó
más de una hora desde que lo pediste. Pedí uno nuevo, es gratis."* Cierra el hallazgo.

---

### BUG-04 — Calendario de cuotas muestra el mes actual adeudado como "futuro"

- **Sección:** 3.2 (Estado de cuenta — socio moroso)
- **Severidad:** Media/Alta
- **Usuario / rol:** socio moroso — reproducido con DNI 42542572 (1 mes), 37889456 (7 meses) y 36778899 (15 meses)
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Loguear con un socio moroso donde el mes en curso (el de "hoy") es uno de los meses adeudados.
  2. Ir a `/socio/cuotas` y revisar el calendario.
- **Esperado:** El mes en curso, si está adeudado, se muestra en rojo/"adeudado" igual que los demás meses debidos.
- **Obtenido:** El mes en curso (septiembre, al momento del test) se muestra como "futuro" en vez de "adeudado", aunque efectivamente es un mes que el socio debe.
- **Evidencia:** Reproducido en las 3 cuentas moroso probadas, con distintas cantidades de meses de deuda (1, 7, 15).
- **Notas:** Probablemente la lógica de pintado del calendario compara contra "hoy" con un `>` en vez de `>=`, tratando el mes actual como "todavía no vencido" en vez de "ya vencido". Podría confundir al socio sobre si el mes en curso ya cuenta como deuda o no.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz — ojo, distinta a la sospechada:** no era un `>` en vez de `>=`. El sistema
  estaba **contando bien**. Con día de vencimiento 10 y el test corrido el 8 de septiembre,
  la cuota de septiembre **todavía no había vencido**, así que no suma a la deuda — y el
  header y el calendario coincidían (7 meses adeudados en los dos lados).
  El problema es de lenguaje: pintar ese mes como **"Futuro"**, en gris, al lado de meses en
  rojo, hace pensar que el sistema no lo cuenta. Es un mes que vence en dos días, no un mes
  "por venir".
- **Qué se hizo:** estado nuevo `a_vencer` en `SocioCuotas.jsx` para el mes en curso sin
  cobertura y todavía no vencido. Se muestra en ámbar con el texto **"Vence el 10"**
  (el día real configurado), con su propio ícono, chip en el resumen y entrada en la leyenda.
  **La plata no cambia**: ese mes sigue sin sumar a la deuda hasta que vence, igual que antes
  y igual que el backend.
- **Si el club prefiere lo contrario** (que el mes en curso cuente como deuda desde el día 1),
  eso es una decisión de negocio y hay que cambiarlo en el backend también, no solo acá.

**🟡 Parcial (2026-09-09) — el estado visual está bien; apareció una inconsistencia en el conteo**

- **Lo bueno:** probado con DNI 42542572 y 37889456 — el mes en curso ahora dice
  correctamente "Vence el 10" en ámbar en el calendario, ya no dice "futuro". El fix visual
  funciona.
- **Lo que se destapó:** el **contador de arriba** ("N meses adeudados" y el monto en $) NO
  coincide con lo que muestra el propio calendario. Ejemplo concreto con DNI 37889456, probado
  el 9 de septiembre (un día antes del vencimiento del día 10): el header dice **"7 meses
  adeudados · $35.000"**, pero el calendario marca solo 6 meses en rojo ("adeudado") + septiembre
  en ámbar ("vence el 10", todavía no vencido). Según la propia lógica que ya está bien aplicada
  en el calendario, hoy debería decir **6 meses adeudados**, y recién mañana (11 de septiembre)
  pasar a 7. El número del header no está usando el mismo corte que el calendario.
- **A corregir:** unificar el cálculo — el mes en curso no debería sumar al contador total ni
  al monto en pesos hasta el día siguiente a su vencimiento, igual que ya no cuenta como
  "adeudado" en el calendario. Revisar dónde se calcula el número del header (probablemente
  `calcular_estado_financiero` o el endpoint que arma el resumen de `/socio/cuotas`) para que
  use el mismo corte de fecha que la lógica del calendario (que si ya usa `a_vencer`
  correctamente, es la fuente de verdad a igualar).
- **Al re-testear:** repetir con un moroso el día antes del vencimiento y el día después —
  el número de "meses adeudados" del header tiene que cambiar en el momento exacto en que el
  mes pasa de "vence el 10" a "adeudado" en el calendario, ni un día antes.

**Corrección aplicada (ronda 3) — a re-testear**

- **Confirmado el diagnóstico de la tester**, y con el número exacto: con cobertura hasta el
  10 de febrero y día de vencimiento 10, al 9 de septiembre el motor generaba 7 períodos
  (marzo…septiembre) y contaba los 7, aunque el de septiembre vence el 10 — o sea, al día
  siguiente. El calendario ya lo trataba bien; el contador no.
- **Qué se hizo — un solo corte, en los tres lugares:** un período cuenta como adeudado
  **solo si su fecha de vencimiento ya pasó** (`vencimiento < hoy`). El día del vencimiento
  todavía está en plazo.
  - Backend: `utils/cuotas_periodos.calcular_estado_financiero()` filtra los períodos no
    vencidos, y `moroso` pasó a derivarse de que quede al menos uno.
  - Contador del header: `calcularEstadoFinanciero()` en `SocioCuotas.jsx` y en
    `AdminSocios.jsx` (las dos copias del frontend) aplican el mismo filtro.
  - Calendario: la regla de `adeudado` en `estadoDeMes()` pasó de `fechaRep <= hoy` a
    `fechaRep < hoy`. Sin esto el calendario se adelantaba un día al contador: el día 10
    pintaba rojo mientras el header todavía decía 6.
- **También se arregló la función inversa.** `fecha_cubierta_para_meses_adeudados()` (la que
  usa la edición manual de deuda en `/admin/socios`) tenía que seguir siendo la inversa
  exacta, y con el corte nuevo quedaba corrida en uno.
- **Un quinto lugar que también tenía su propia copia:** `routers/qr_auth.py` tenía
  `_calcular_estado_financiero()`, una reimplementación a mano ("puerto 1:1 del frontend").
  Ese es el motor del **escáner de la puerta**. Si lo dejaba como estaba, la puerta habría
  marcado moroso a alguien que en su pantalla figuraba al día — justo la divergencia que
  este bug pide eliminar. Ahora delega en el motor compartido.
- **Verificado por mí (cálculo, no UI):**
  - El escenario de la ficha, con cobertura hasta el 10-feb y vencimiento 10: **6 meses** el
    8, el 9 y el 10 de septiembre; **7** a partir del 11. Es exactamente el corte que se
    pidió ("recién mañana pasar a 7").
  - Backend, contador del header y calendario dan **el mismo número** en 5.980
    combinaciones (días de vencimiento 1/5/10/15/28 × todos los días de agosto a octubre de
    2026 × deudas de 0 a 12 meses).
  - La inversa es exacta en **163.520** combinaciones (días de vencimiento 1 a 28 — el
    dominio real, que la base limita con un CHECK `BETWEEN 1 AND 28` — × todos los días de
    2026 × deudas de 0 a 15 meses).
- **Al re-testear:** un moroso el día del vencimiento y el día siguiente. El header y el
  calendario tienen que cambiar juntos, en el mismo momento.

**Ronda 4 (2026-09-11) — quedaba una copia más del motor: el inicio del socio**

La ronda 3 unificó el cálculo en backend, escáner y `/socio/cuotas`, y verificó 5.980
combinaciones. Lo que no entró en esa unificación fue **`SocioInicio.jsx`**, que tenía su
propia copia — sin el corte por día de vencimiento y sin la gracia de mes de ingreso. Por
eso la corrida manual vio, para el mismo socio y los mismos datos, `/socio/cuotas` diciendo
una cosa y el inicio diciendo MOROSO en rojo.

- **Corregido de raíz:** las **tres** copias del frontend (`SocioInicio`, `SocioCuotas`,
  `AdminSocios`) se borraron y se reemplazaron por un módulo único,
  `frontend/src/utils/cuotas.js`, espejo de `backend/utils/cuotas_periodos.py`. Quedan dos
  archivos con esta lógica en todo el proyecto, y los dos lo dicen en su encabezado.
- Verificado con vencimiento 10 y cobertura hasta el 10/02, backend y frontend por separado:

| Fecha | Meses adeudados |
|---|---|
| 9 de septiembre | 6 |
| 10 de septiembre (día del vencimiento) | 6 |
| 11 de septiembre | 7 |

| Caso | Qué tiene que pasar |
|---|---|
| 04-A | El número del header de `/socio/cuotas` coincide con la cantidad de meses en rojo del calendario |
| 04-B | Inicio, `/socio/cuotas` y `/admin/socios` dicen lo mismo para el mismo socio |
| 04-C | El mes en curso figura "Vence el 10" (ámbar) hasta el día 10 inclusive, y adeudado el 11 |

---

### BUG-05 — "Ver comprobante" del socio no carga la imagen

- **Sección:** 3.3 (Pago por comprobante)
- **Severidad:** Alta
- **Usuario / rol:** socio — DNI 77000001
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Subir un comprobante de pago desde el checkout de cuotas.
  2. Ir a `/socio/cuotas`, donde aparece "tenés un pago en proceso — comprobante en revisión — ver comprobante".
  3. Hacer clic en "ver comprobante".
- **Esperado:** Se abre/muestra la imagen que el socio subió.
- **Obtenido:** Abre una pestaña nueva a una URL directa del backend (`club-atletico-api.onrender.com/comprobantes/146/...jpeg`) que no carga ningún contenido (pantalla en blanco).
- **Evidencia:** URL de ejemplo: `https://club-atletico-api.onrender.com/comprobantes/146/1bb13b0d31c24897a741128aa67bfd7a.jpeg`
- **Notas:** Coincide con el comportamiento esperado de seguridad del checklist §9 ("URL de comprobante sin firmar → acceso denegado, bucket privado") — el problema es que ese mismo link roto es el que la UI le da al propio dueño del comprobante para revisarlo. Falta confirmar si `/admin/verificaciones` tiene el mismo problema al mostrarle la imagen al admin (no se confirmó explícitamente en esta corrida — ver ítem pendiente en §3.4). La solución probablemente pasa por servir la imagen vía un endpoint autenticado del backend (que haga de proxy) o generar una URL firmada de corta duración, en vez de exponer el link directo del bucket privado.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** en la base se guarda el **object key** de S3 (`comprobantes/146/abc.jpeg`),
  no una URL. Varias pantallas hacían `${API}${key}` a secas y armaban
  `https://club-atletico-api.onrender.com/comprobantes/146/abc.jpeg`, que no es una ruta del
  backend ni de nada. De ahí la pestaña en blanco.
- **Alcance real, más grande que lo reportado:** el mismo defecto afectaba a
  **`/admin/verificaciones`** (el admin tampoco veía la imagen — quedaba pendiente confirmarlo
  en §3.4: confirmado, fallaba) y al mail **`aviso_club_comprobante`** que se le manda al club.
- **Qué se hizo:** `PagoResponse.comprobante_url` (`schemas.py`) ahora firma el valor al
  serializar, vía el helper nuevo `utils/s3.resolver_url_archivo()` — así queda arreglado de
  una sola vez en todas las respuestas que embeben un Pago (orden pendiente del socio, bandeja
  del admin, mis-compras), sin depender de que cada router se acuerde. El helper es idempotente:
  no re-firma una URL ya resuelta ni toca las rutas locales legacy.
  En el frontend, las 3 pantallas usan ahora `utils/archivos.resolverUrlArchivo()`.
  El mail al club dejó de linkear el archivo (una Presigned URL vive 15 minutos, no sirve en un
  mail) y manda al panel, que además pide sesión.
- **Detalle que rompía en silencio:** la detección de PDF en el panel usaba
  `endsWith('.pdf')` sobre la URL; una Presigned URL termina en `?X-Amz-Signature=...`, así que
  un comprobante PDF se habría renderizado como `<img>` roto. Corregido.
- **Al re-testear:** subir un comprobante **JPG y uno PDF**, y verlos desde socio y desde admin.

**🟡 Parcial (2026-09-09) — arreglado para el socio, sigue faltando para el admin**

- **Lo bueno:** confirmado con DNI 77000001 (que ya tiene historial de 3 pagos) — desde
  `/socio/cuotas` → "Historial de Pagos" → "Ver comprobante" abre la imagen correctamente.
- **Lo que falta:** desde `/admin/verificaciones`, las tarjetas de pago (aunque estén en
  cualquier estado: verificado, rechazado) **no tienen ningún control para ver el
  comprobante** — no es que el link esté roto, directamente no existe un botón/link para
  eso en esa pantalla. Mismo resultado desde el modal de historial de pagos en
  `/admin/socios` (ícono `$`).
- **Pedido explícito de la tester:** que el admin pueda no solo **ver** el comprobante desde
  algún lado, sino también **reemplazarlo/editarlo** si hace falta.
- **A corregir:** esto no es "el mismo bug de nuevo" — es que los componentes de admin
  (`AdminVerificaciones.jsx` y el modal de historial en `AdminSocios.jsx`) nunca tuvieron una
  UI para mostrar el comprobante, más allá de que el backend ya devuelva la URL firmada
  correctamente (la corrección de `PagoResponse.comprobante_url` que se hizo para BUG-05
  soluciona el dato, pero si el componente no lo renderiza en ningún lado, no alcanza).
  Agregar un link/thumbnail "Ver comprobante" en ambas pantallas de admin, y evaluar si
  conviene sumar la opción de reemplazarlo.

**Corrección aplicada (ronda 3) — a re-testear**

- **Confirmado tal como lo describiste:** no era un link roto, era que el control no
  existía. El comprobante solo se renderizaba dentro del modal de verificación, y ese modal
  se abre desde el botón "Verificar", que está detrás de un `{!resuelta && ...}` — o sea,
  solo para órdenes pendientes. Una vez aprobada o rechazada la orden, no quedaba ninguna
  puerta de entrada al archivo.
- **Qué se hizo:** un componente compartido, `components/admin/ComprobantePago.jsx`, que
  muestra el comprobante (imagen con link a tamaño completo, o tarjeta con link para PDF) y
  trae el botón de **reemplazar**. Se usa en los tres lugares:
  - **`/admin/verificaciones`** — en la tarjeta del Pago, al desplegarla, disponible en
    **cualquier estado** (pendiente, verificado, rechazado). Va al nivel del Pago porque el
    comprobante cuelga del Pago, no de la Orden: en un carrito mixto las dos órdenes
    comparten archivo.
  - **el modal de verificación** — mismo componente, así se puede reemplazar un comprobante
    ilegible y aprobar, sin tener que rechazar la orden y hacerla generar de nuevo.
  - **`/admin/socios` → historial de pagos** (el ícono `$`), por orden.
- **Reemplazo (el pedido explícito):** endpoint nuevo `POST /admin/pagos/{id_pago}/comprobante`
  (`routers/admin_pagos.py`), para `admin_general` y `personal_administrativo`. A diferencia
  del camino del socio, **no exige** que el pago sea propio ni que esté `pendiente`: un pago
  ya verificado o rechazado también se puede corregir si se cargó un archivo ilegible o el
  de otro socio. A propósito **no** reinicia el reloj de expiración de las órdenes hermanas
  (ese reinicio existe para darle 48 h al admin desde que el *socio* sube la foto) y **no**
  manda el aviso de "llegó un comprobante" al club, que es justamente quien lo está
  subiendo. Queda registrado en `audit_log` como `REEMPLAZAR_COMPROBANTE_ADMIN`, con el
  archivo anterior.
- **De paso:** la validación de formato/tamaño se movió a `utils/comprobantes.py`, compartida
  entre el camino del socio y el del admin, para que no puedan aceptar cosas distintas. Y en
  el modal, el botón "Aprobar" ahora mira el comprobante **vigente** en pantalla, así que si
  el admin adjunta uno donde no había, el botón se habilita sin recargar.
- ⚠️ **Sin verificar por mí** en la parte de UI (no tengo Node para levantar el frontend). El
  endpoint sí lo verifiqué a nivel de que la app arranca y la ruta queda registrada.
- **Al re-testear:** con un **JPG y un PDF**, en una orden **pendiente** y en una ya
  **resuelta**, desde `/admin/verificaciones` y desde el historial de `/admin/socios`: ver,
  reemplazar, y volver a ver.

---

### BUG-06 — Las compras de cuota social no aparecen en "Mis Compras"

- **Sección:** 3.3 (transversal: también afecta 3.4, 3.6 y varios mails)
- **Severidad:** Alta
- **Usuario / rol:** socio — DNI 77000001
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Generar una orden de cuota (por transferencia o efectivo).
  2. Ir a "Mis Compras".
- **Esperado:** La orden de cuota aparece en el historial, en cualquiera de sus estados (pendiente, aprobada, rechazada).
- **Obtenido:** "Mis Compras" queda vacío — nunca lista órdenes de tipo cuota social, en ningún estado.
- **Evidencia:** Confirmado en los 3 intentos de pago de cuota realizados durante la sesión (transferencia aprobada, transferencia con comprobante ilegible, efectivo rechazado) — ninguno apareció.
- **Notas:** Este gap es más grave de lo que parece a primera vista porque **varios mails y notificaciones dirigen explícitamente al socio a revisar "Mis Compras"** para ver el estado de su pago de cuota (`orden_generada`, notificación de pago verificado, notificación de pago rechazado) — pero ahí nunca va a encontrar nada. Una vez que la notificación puntual se pierde/lee, el socio no tiene ningún lugar dentro del sistema para volver a ver qué pasó con un pago de cuota rechazado o su historial de pagos. Revisar si "Mis Compras" filtra por tipo de orden (ej. solo `compra`/`alquiler`, excluyendo `cuota_social`) y si es intencional o un descuido.

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** la exclusión **es intencional** y está documentada en el código
  (`socio_carrito.listar_mis_compras`): "Mis Compras" es tienda/alquileres a propósito, y el
  estado de las cuotas vive en `/socio/cuotas`. Hasta ahí, por diseño.
  **Pero el agujero que reportaste es real y era doble:**
  1. El historial de `/socio/cuotas` devolvía **solo las órdenes `aprobada`**. Un pago
     rechazado, expirado o cancelado no aparecía en **ninguna** pantalla del socio.
  2. Los mails `orden_generada` y `recordatorio_comprobante` mandaban **siempre** a
     `/mis-compras`, incluso para una orden de cuota pura → el socio seguía el link y
     encontraba la pantalla vacía. Eso es lo que hacía que pareciera un bug de filtro.
- **Qué se hizo:**
  - `GET /socio/cuotas/historial` ahora devuelve también `rechazada`, `expirada` y
    `cancelada_socio`, con el `estado` y el `motivo_rechazo`. Las `pendiente_verificacion`
    siguen fuera a propósito: esas ya se muestran arriba, en el banner de orden pendiente.
  - La UI del historial las distingue con badge, ícono y color, y muestra el motivo del
    rechazo debajo.
  - Los mails ahora eligen destino: si el carrito era **solo cuota**, el link va a
    `/socio/cuotas`; si tenía tienda/alquiler, sigue a `/mis-compras`.
- **Al re-testear:** rechazar una orden de cuota y confirmar que queda visible en el historial
  de `/socio/cuotas` con el motivo, y que el link del mail `orden_generada` de una cuota pura
  abre `/socio/cuotas` y no `/mis-compras`.

**✅ Verificado arreglado (2026-09-09)**

Confirmado con DNI 77000001: el historial de `/socio/cuotas` ahora muestra las 3 órdenes
probadas (pagado ×2, rechazado ×1 con motivo) y, más tarde en la misma sesión, también una
cancelada por el socio. Cierra el hallazgo.

---

### BUG-07 — Falta el mail `orden_aprobada_cuota`

- **Sección:** 3.4 (Aprobación / rechazo de pago — admin)
- **Severidad:** Media
- **Usuario / rol:** socio — DNI 77000001 / admin — DNI 42542525
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Aprobar desde `/admin/verificaciones` una orden de cuota pendiente.
  2. Revisar la bandeja de entrada del socio y del club.
- **Esperado:** Llega el mail `orden_aprobada_cuota` (según el checklist).
- **Obtenido:** No llegó ningún mail, ni al socio ni al club. El socio sí recibió la notificación in-app ("¡Pago verificado!").
- **Evidencia:** Confirmado una vez, con la orden #155 (2 cuotas por transferencia).
- **Notas:** Podría ser intencional (reemplazar el mail por notificación in-app para esta acción puntual) o un mail que quedó sin conectar en el flujo de aprobación de cuotas específicamente (a diferencia del rechazo, que sí mandó mail correctamente — ver la fila de abajo en la misma sección). Confirmar cuál es el comportamiento esperado.

**Revisión (2026-09-09) — NO es un bug de código; el checklist estaba desactualizado**

- **Qué pasó:** `orden_aprobada_cuota` (y `orden_aprobada_tienda`) **ya no se usan**. Se
  reemplazaron a propósito por un único mail **`compra_confirmada`** a nivel **Pago**, con el
  detalle completo del carrito — justamente para que alguien que paga cuota + tienda en un
  mismo carrito no reciba 2 mails sueltos, ninguno con el total real. El asunto es
  **"✅ Compra confirmada — Comprobante #N"**. El checklist quedó pidiendo el mail viejo.
  Ya se corrigió `docs/qa-checklist.md` (§3.4 y §12).
- **Lo que queda sin confirmar:** si en la prueba de la orden #155 llegó el `compra_confirmada`
  (con ese asunto) y no se lo reconoció, o si efectivamente no salió. Revisando el código, la
  cadena está bien conectada: `/admin/ordenes/{id}/aprobar` llama a
  `finalizar_pago_si_corresponde()`, que encola el mail al socio y el `aviso_club_pago` al club.
  **No pude reproducirlo** (sin backend ni base local), así que no lo doy por descartado.
- **Qué se hizo para que la próxima vez se diagnostique en un vistazo:** se agregaron logs en
  `utils/ordenes.finalizar_pago_si_corresponde()` en **cada salida temprana** y en el encolado.
  Antes, si el mail no salía, no quedaba rastro de por qué ni de si siquiera se había intentado.
- **Al re-testear:** aprobar una orden de cuota, buscar el asunto **"Compra confirmada"** (no
  "orden aprobada"), y si no llega, mirar los logs de Render buscando `compra_confirmada` — la
  línea dice si se encoló o cuál fue el motivo de la omisión.
- **Nota:** el mail sale **una sola vez por Pago y recién cuando se resolvieron todas sus
  órdenes**. Si un carrito tenía cuota + tienda y aprobás solo una, el mail no sale todavía —
  es correcto, no es el bug.

**🔁 Reencuadrado como decisión de negocio — D5 (2026-09-09)**

- **Re-test:** se aprobó otra orden de cuota (#159) y se revisaron los logs de Render en el
  momento — llegó la notificación in-app, pero **no hay ningún rastro de `compra_confirmada`
  en los logs**, ni de que se haya encolado ni de ningún motivo de omisión (a pesar de los
  logs nuevos agregados en `finalizar_pago_si_corresponde()`).
- **La respuesta, sin embargo, no fue "arreglalo"**: la preferencia expresada es no mandar
  mail en la generación y sí en la aprobación — ver **D5** en "Preguntas de negocio abiertas".
  Esto cambia el objetivo: no se trata de hacer que `compra_confirmada` llegue tal como está
  diseñado hoy, sino de rediseñar cuándo dispara cada canal (in-app vs. mail) según el
  criterio nuevo.
- **Al re-testear (una vez implementado D5):** confirmar que el mail de aprobación llega, y
  que la generación de la orden deja de mandar mail (o lo que finalmente se decida).

**Implementación de D5 (ronda 3) — y por qué no había rastro en los logs**

- **Encontré por qué los logs estaban vacíos, y no era que el código no pasara por ahí:**
  el proyecto **no tenía ninguna configuración de logging**. Uvicorn configura solo sus
  propios loggers (`uvicorn`, `uvicorn.error`, `uvicorn.access`) y deja el root logger sin
  handler, o sea en nivel WARNING. Todos los `logger.info(...)` de los módulos del proyecto
  se descartaban en silencio — incluidos, con toda la ironía, los que se habían agregado en
  la ronda 2 en cada salida temprana de `finalizar_pago_si_corresponde()` justamente para
  poder diagnosticar esto. Por eso viste "ningún rastro": esas líneas nunca se emitieron.
  (Los `warning`/`exception` sí se veían, pero solo por el handler de último recurso de
  Python.)
- **Arreglado:** `logging.basicConfig(level=LOG_LEVEL, ...)` al principio de `main.py`, con
  `LOG_LEVEL` configurable por env var y default `INFO`. Verificado acá: las líneas INFO de
  `utils.ordenes` ahora se emiten con timestamp y nombre de módulo. **Esto es lo que hace
  diagnosticable el mail de aprobación en la próxima corrida**, gane o pierda.
- **D5 implementado:** el mail de "orden generada" se eliminó y se reemplazó por una
  **notificación in-app** al crear la orden, con texto según el método de pago (efectivo →
  "acercate al club"; transferencia → "subí el comprobante"; MP → "completá el pago"), y
  apuntando a Gestión de Cuotas o a Mis Compras según lo que haya en el carrito. El mail
  queda reservado para la **aprobación** (`compra_confirmada`, asunto "✅ Compra confirmada
  — Comprobante #N"), que es lo que se pidió.
- **Detalle de implementación:** la notificación usa el tipo `sistema`, que ya está
  permitido por el CHECK `chk_notificacion_tipo`. Un tipo nuevo (ej. `orden_generada`)
  habría necesitado una migración solo para eso; si querés distinguirlo en el centro de
  notificaciones, lo agrego con su migración.
- `task_orden_generada` y su template quedaron sin uso. Los dejé en el repo con un comentario
  que lo dice, por si el club quiere volver atrás; si en la próxima revisión siguen sin
  usarse, conviene borrarlos.
- **Al re-testear:** generar una orden → **no** debe llegar mail, y sí una notificación
  in-app. Aprobarla → **debe** llegar el mail "Compra confirmada". Si no llega, ahora los
  logs de Render sí van a decir si se encoló o cuál fue el motivo de la omisión — buscá
  `compra_confirmada`.

**Ronda 4 (2026-09-11) — CAUSA RAÍZ. No era ninguna de las hipótesis anteriores**

El re-test de la ronda 3 volvió a dar lo mismo (orden #159 aprobada, notificación in-app sí,
mail no, logs sin rastro). Con el logging ya configurado, esa ausencia de rastro dejó de ser
explicable y obligó a mirar el mecanismo en vez del mail.

**Qué pasaba:** `SessionLocal` se crea con **`autoflush=False`** (`database.py`).
`finalizar_pago_si_corresponde()` decide si mandar el mail con una **query**: "¿le quedan
órdenes pendientes a este Pago?". El `orden.estado = "aprobada"` que acababa de hacer
`procesar_aprobacion_orden()` vivía **solo en memoria** — sin autoflush, ninguna query lo ve
hasta que alguien lo baja a la base. Así que esa query leía el estado anterior, veía la orden
recién aprobada todavía como `pendiente_verificacion`, concluía "falta resolver una hermana"
y se volvía sin encolar nada.

O sea: **el mail no salía nunca al aprobar**, ni siquiera en el caso simple de un Pago con
una sola orden, y por los **cuatro** caminos que aprueban (admin, webhook de Mercado Pago,
saldo a favor que cubre el total, scheduler). No era un caso de borde: era el camino feliz.
Por eso tampoco había rastro en los logs: la función salía por el `return` de "quedan
hermanas pendientes", que es el único que se veía como INFO.

- **Corregido:** `db.flush()` explícito antes de esa query.
- **Dos causas secundarias**, en el camino *expirada → reabierta → aprobada* (el que se da
  cuando una orden queda dando vueltas varios días, como pasó en estas corridas):
  - Al expirar, el scheduler dejaba el Pago en `'rechazado'` **y** `mail_confirmacion_enviado`
    en `True` para siempre. `reabrir_orden` ahora deshace las dos cosas.
  - La bandera del mail se levantaba *antes* de decidir si había algo que mandar; ahora se
    levanta recién al encolarlo.
  - Aprobar deja el Pago en `'verificado'` venga del estado que venga. Antes, un Pago
    reabierto quedaba con una orden aprobada adentro y el Pago figurando como rechazado en
    cualquier reporte.
- Se agregó además un lock de la fila del Pago: dos órdenes hermanas resolviéndose en
  requests simultáneas podían cruzarse y que **ninguna** mandara el mail.
- ⚠️ Fix de **backend**. Es el cambio más importante de esta ronda.

| Caso | Qué tiene que pasar |
|---|---|
| 7-A | Aprobar una orden de cuota por transferencia → llega el mail "✅ Compra confirmada" + notificación in-app |
| 7-B | Aprobar una orden de tienda → mismo mail, con el detalle de los ítems |
| 7-C | Pago mixto (cuota + tienda) aprobado en dos pasos → **un solo** mail, al resolver la última orden |
| 7-D | Orden expirada → reabrir → aprobar → llega el mail igual (antes no llegaba nunca) |
| 7-E | Doble clic en aprobar → un solo mail |

Si alguno falla, ahora los logs de Render sí dicen el motivo: buscar `compra_confirmada`.

---

### BUG-08 — [BLOQUEANTE] Pago en efectivo sin salida posible

- **Sección:** 3.6 (Método efectivo)
- **Severidad:** Bloqueante
- **Usuario / rol:** socio — DNI 77000001 / admin — DNI 42542525
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Como socio, agregar cuotas al carrito y elegir "efectivo" como método de pago.
  2. Confirmar la compra (no se pide comprobante en este paso).
  3. Ir a `/socio/cuotas`.
  4. Como admin, ir a `/admin/verificaciones` y buscar esa orden.
  5. Intentar aprobarla.
- **Esperado:** El pago en efectivo se puede aprobar sin necesidad de comprobante (dado que es una transacción presencial), o existe algún mecanismo para que el admin la marque como cobrada.
- **Obtenido:**
  - En `/socio/cuotas` aparece: *"Tenés un pago en proceso — Orden #156 — Total a transferir: $10.000 — Falta subir el comprobante de transferencia — [Subir Comprobante] [Cancelar Trámite]"* — lenguaje de transferencia bancaria para un pago que el socio eligió pagar en efectivo.
  - En `/admin/verificaciones`, la orden queda pendiente con método "💵 Efectivo", pero el sistema no permite aprobarla porque "el socio aún no ha subido comprobante" — y tampoco le da al admin ninguna opción para cargar un comprobante él mismo.
  - Resultado: la orden queda completamente trabada, sin ningún camino para aprobarla. La única salida encontrada fue rechazarla.
- **Evidencia:** Orden #156 / Pago #147, DNI 77000001, $10.000, método efectivo.
- **Notas:** Este es el hallazgo más urgente de la corrida — tal como está, **el método de pago en efectivo no se puede completar nunca**. Está directamente relacionado con **D4** (pregunta de negocio): probablemente el diseño correcto sea que el pago en efectivo nunca le pida comprobante al socio, y que sea el ADMIN quien, al cobrar en persona, marque la orden como "cobrada" (opcionalmente subiendo él mismo una foto para registro interno, no como requisito para aprobar).

**Corrección aplicada (2026-09-09) — a re-testear**

- **Causa raíz:** el backend **nunca** exigió comprobante para aprobar. El candado era una sola
  línea del frontend (`AdminVerificaciones.jsx`):
  `disabled={isSubmitting || (!esMercadoPago && !orden.pago?.comprobante_url)}`
  — o sea, "todo lo que no sea Mercado Pago necesita comprobante", y el efectivo quedaba
  encerrado ahí. Sumado al copy de transferencia del lado del socio, el flujo entero parecía
  roto de raíz cuando en realidad faltaba contemplar el caso.
- **Qué se hizo (responde también a D4):** el efectivo se trata como lo que es — se cobra en
  mano, no lleva comprobante:
  - **Admin:** el botón "Aprobar" se habilita para efectivo sin comprobante. Donde antes decía
    "El socio aún no ha subido un comprobante" ahora dice *"💵 Pago en efectivo — se cobra en
    el club, no lleva comprobante. Aprobá la orden cuando tengas la plata en mano."*
  - **Socio (`/socio/cuotas`):** el banner ahora es sensible al método. En efectivo dice
    "Total a pagar" (no "a transferir"), explica que se paga en la sede y que un administrativo
    registra el cobro, y **el botón "Subir Comprobante" no aparece**. Lo mismo en "Mis Compras".
  - **Reabrir una orden expirada** en efectivo ya no se rechaza por falta de comprobante
    (backend, `admin_ordenes.reabrir_orden`).
  - El job `recordatorio_comprobante_pendiente` ya no le escribe a socios con pagos en efectivo
    ni de Mercado Pago pidiéndoles un comprobante que nunca van a poder subir.
- **Lo que NO se hizo:** no se agregó una opción para que el admin suba él la foto del efectivo.
  Si el club la quiere como registro interno, es una decisión aparte — hoy la trazabilidad
  queda en `audit_log` (quién aprobó y cuándo), que para un cobro presencial alcanza.
- **Al re-testear:** el camino completo — socio elige efectivo → confirma → mira
  `/socio/cuotas` (copy correcto, sin botón de subir) → admin abre `/admin/verificaciones` →
  **aprueba** → se acreditan los meses y el socio pasa a al día.

**✅ Verificado arreglado (2026-09-09)**

Probado con un pago real de 3 meses ($15.000) en efectivo con DNI 77000001: orden generada,
mail a socio y club, admin aprueba **sin que se le pida comprobante**, meses acreditados
correctamente (cobertura avanzó de fin de octubre a fin de enero 2027 inclusive). Cierra el
hallazgo. Sugerencia de la tester para más adelante (no bloqueante, parte de D4): que el
admin pueda adjuntar una foto **opcional** del efectivo cobrado, como registro interno.

---

### BUG-09 — Cobro manual/ventanilla siempre etiquetado "Pagado por transferencia"

- **Sección:** 3.5 (Cobro manual desde admin)
- **Severidad:** Media
- **Usuario / rol:** admin — DNI 42542525 / socio — DNI 77000001
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Como admin, registrar un pago manual/ventanilla de N meses para un socio (sin pasar por
     el checkout del socio).
  2. Revisar cómo queda etiquetado en `/admin/socios` → historial de ese socio.
- **Esperado:** El método de pago que figura en el registro refleja cómo se cobró en
  realidad (efectivo entregado en persona, por ejemplo), o al menos un método neutro tipo
  "Pago manual / ventanilla".
- **Obtenido:** Queda etiquetado como *"Pagado por transferencia"*, incluso cuando el cobro
  fue presencial y no fue una transferencia bancaria.
- **Evidencia:** Orden #160, DNI 77000001, 2 meses, $10.000 — "Pago manual por ventanilla —
  2 mes(es). Tarifa aplicada: Cuota Social Base." pero con método "Pagado por transferencia".
- **Notas:** Probablemente el formulario de cobro manual no tiene un selector de método (o
  lo tiene pero hardcodea "transferencia" por default) y guarda ese valor fijo sin preguntar.
  Es cosmético/de trazabilidad — no bloquea el flujo — pero puede confundir en una auditoría
  contable a fin de mes si el club necesita distinguir efectivo de transferencia en sus
  registros internos.

**Corrección aplicada (ronda 3) — a re-testear**

- **Causa raíz:** `registrar_pago_manual` creaba el `Pago` sin pasar `metodo_pago`, así que
  la columna tomaba su `server_default`, que es `'transferencia'`. No es que el formulario
  hardcodeara el valor: no existía el campo en ninguna parte del recorrido.
- **Qué se hizo:** el modal de cobro en ventanilla ahora tiene un selector **Efectivo /
  Transferencia** (por defecto **Efectivo**, que es el caso típico del cobro presencial y
  justo el que quedaba mal etiquetado). El valor viaja en el payload
  (`RegistrarPagoManualPayload.metodo_pago`, validado con `Literal`), se guarda en el Pago, y
  queda también en `notas_admin` y en `audit_log`.
- **De paso, el nombre que se muestra:** el historial armaba la etiqueta con
  `metodo.replace('_',' ')`, que dejaba "mercado pago" en minúscula y suelto. Ahora hay un
  mapa (`etiquetaMetodoPago`) con "Cobrado en efectivo", "Pagado por transferencia", "Pagado
  por Mercado Pago" y "Saldado con saldo a favor".
- ⚠️ **Ojo con los registros viejos:** las órdenes de cobro manual ya existentes quedaron
  guardadas como `transferencia` en la base y **van a seguir mostrándose así** — el fix no
  las corrige hacia atrás. Si te importa para el cierre contable, se puede hacer un `UPDATE`
  puntual sobre los pagos cuyo `notas_admin` empieza con "Pago manual por ventanilla";
  decime y lo preparo.
- **Al re-testear:** cobrar N meses por ventanilla eligiendo Efectivo, y confirmar que el
  historial del socio dice "Cobrado en efectivo". Repetir con Transferencia.

---

### BUG-10 — El filtro "Menores" en `/admin/socios` no devuelve resultados

- **Sección:** 3.7 (Descuento a menores)
- **Severidad:** Media
- **Usuario / rol:** admin — DNI 42542525
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Editar un socio existente desde `/admin/socios` y ponerle una fecha de nacimiento que lo
     haga menor de edad (ej. año 2015).
  2. Guardar. Confirmar que se guardó bien (entrando de nuevo a ver el dato).
  3. Ir al listado de `/admin/socios` y aplicar el filtro "Menores".
- **Esperado:** El socio recién editado aparece en la lista filtrada.
- **Obtenido:** El filtro no devuelve ningún resultado, a pesar de que el socio menor existe
  y está bien guardado.
- **Evidencia:** DNI 77000002, editado con fecha de nacimiento 2015.
- **Notas:** Revisar si el filtro calcula la edad con la misma fórmula que el resto del
  sistema (podría estar comparando contra un campo distinto, o con un cálculo de edad que no
  coincide con el que usa el motor de cuotas para aplicar el descuento).

**Corrección aplicada (ronda 3) — a re-testear**

- **Causa raíz:** el filtro estaba bien; el dato no llegaba. `/admin/usuarios/` responde con
  el schema `UsuarioListResponse`, que **no incluía `fecha_nacimiento`**. El frontend hacía
  `calcularEdad(s.fecha_nacimiento)` sobre `undefined`, que devuelve `null`, y la condición
  `edad !== null && edad < 18` descartaba a todo el mundo. De ahí el "no devuelve ningún
  resultado" incluso con el socio bien guardado: el dato existía en la base y en el detalle,
  pero no en el listado.
- **Qué se hizo:** se agregaron `fecha_nacimiento` y `fecha_ingreso` a `UsuarioListResponse`.
- **Dos cosas más que arreglan de arrastre**, porque leían los mismos campos ausentes en el
  listado:
  - el precio con descuento de cada fila (`calcularPrecioFinal(precioCuota, socio.fecha_nacimiento)`)
    mostraba tarifa de adulto para los menores;
  - el cálculo de deuda de un socio **sin** `mes_cubierto_hasta` usa `fecha_ingreso` como
    fecha base, y tampoco la tenía.
- **Nota aparte, no la toqué:** el frontend tiene el 40% hardcodeado en
  `DESCUENTO_MENOR_PORCENTAJE` (`AdminSocios.jsx`), mientras el backend lo lee de
  `ConfiguracionGlobal.descuento_menor_pct`, que el Admin General puede editar desde el
  Catálogo de Productos. Hoy coinciden, así que no se ve; si alguien cambia ese porcentaje,
  la pantalla de socios va a mostrar un precio distinto al que cobra el sistema. Lo dejo
  anotado como riesgo, no como bug de esta ronda.
**✅ Verificado en ejecución (2026-09-09)**

`GET /admin/usuarios/` devuelve las 150 filas y **las 150 traen ahora `fecha_nacimiento` y
`fecha_ingreso`**. Aplicando el mismo filtro que usa el frontend, "Menores" devuelve **38
socios** — antes devolvía 0. (149 de los 150 socios tienen fecha de nacimiento cargada; el
que falta no es un menor, simplemente no tiene el dato.)

- **Al re-testear:** confirmar en pantalla, y de paso mirar que en la fila de un menor el
  precio de cuota salga con descuento.

---

### BUG-11 — Notificación in-app incorrecta "te sacaron: socio" al editar la fecha de nacimiento

- **Sección:** 3.7 (Descuento a menores) / transversal a notificaciones
- **Severidad:** Media
- **Usuario / rol:** socio — DNI 77000002
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Como admin, editar la fecha de nacimiento de un socio activo desde `/admin/socios`
     (sin tocar sus roles).
  2. Loguear (o refrescar sesión) con ese socio y revisar `/notificaciones`.
- **Esperado:** Ninguna notificación sobre roles, ya que no se tocó ningún rol.
- **Obtenido:** Llega una notificación in-app: *"Tus roles cambiaron — En el club te sacaron:
  socio."* — pero el socio conserva el rol `socio` con normalidad (sigue viendo el menú
  hamburguesa típico de socio).
- **Evidencia:** DNI 77000002, notificación recibida ~3 minutos después de editar el año de
  nacimiento a 2015.
- **Notas:** Probablemente algún trigger o lógica que recalcula elegibilidad de roles al
  guardar cualquier cambio en el usuario (quizás pensado para roles etarios como "jugador" en
  categorías juveniles) está interpretando el cambio de edad como una baja del rol `socio`
  cuando no debería tocarlo. Revisar qué dispara esa notificación puntual y bajo qué condición
  decide que un rol se "sacó".

**Corrección aplicada (ronda 3) — a re-testear**

- **Causa raíz:** el endpoint de roles **protege** `socio` y `admin_general` — están
  prohibidos en el payload (403 si llegan) y el `DELETE` de roles los excluye a propósito,
  así que sobreviven a cualquier cambio. Pero el "estado después" se calculaba **solo** a
  partir de los IDs que venían en el payload, donde esos roles nunca están. Resultado:
  `quitados = anteriores - nuevos` los daba por eliminados. Por eso alcanzaba con editar
  cualquier campo del socio —la fecha de nacimiento, por ejemplo— para disparar el aviso: el
  formulario manda la lista de roles seleccionables y `socio` no figura en ella.
- **El mismo error estaba envenenando el `audit_log`:** cada `CAMBIO_ROLES` registraba un
  "después" sin `socio` ni `admin_general`, o sea un historial que decía que a la persona le
  habían sacado su rol base.
- **Qué se hizo:** el estado final se calcula como *lo que llegó en el payload* **+** *los
  roles protegidos que el usuario ya tenía*. La notificación y el audit ahora describen lo
  que de verdad pasó.
- **Verificado por mí** (simulación de la lógica de diff, sin base):
  - editar la fecha de nacimiento sin tocar roles → antes: `quitados=['socio']`; ahora:
    `quitados=[]`, ninguna notificación;
  - agregarle `jugador` a un socio → antes: `quitados=['socio']`; ahora `quitados=[]` y
    `después=['jugador','socio']`;
  - sacarle `jugador` de verdad → sigue avisando `quitados=['jugador']`, que es correcto;
  - un `admin_general` editado → ya no aparece como si se hubiera quedado sin roles.
- **Al re-testear:** editar la fecha de nacimiento de DNI 77000002 y confirmar que **no**
  llega ninguna notificación de roles. Después sacarle un rol real (ej. `jugador`) y
  confirmar que sí avisa, y que nombra solo ese rol.

**Ronda 4 (2026-09-11) — el backend ya estaba bien; faltaba dejar de mandar el PUT**

El fix de la ronda 3 (el "estado después" de los roles incluye los protegidos que el usuario
conserva) es correcto. Lo que seguía en pie es la causa de fondo: **el frontend manda el PUT
de roles en toda edición de socio**, aunque nadie haya abierto el bloque de roles. Cada
cambio de fecha de nacimiento dejaba una entrada `CAMBIO_ROLES` en `/admin/auditoria` sin que
nadie hubiera cambiado un rol, y cualquier discrepancia en ese viaje redondo terminaba en una
notificación falsa al socio.

- **Corregido:** los roles solo se mandan si de verdad cambiaron respecto de lo que trajo el
  backend. De paso cubre un riesgo que estaba latente: si el fetch de roles del modal falla,
  la selección queda vacía y mandarla le borraba al socio **todos** sus roles no protegidos.

| Caso | Qué tiene que pasar |
|---|---|
| N2-A | Editar la fecha de nacimiento de un socio → **ninguna** notificación de roles y **ninguna** entrada `CAMBIO_ROLES` en `/admin/auditoria` |
| N2-B | Agregar o quitar un rol de verdad → sí notifica, y la auditoría lo registra |

---

### BUG-12 — [BLOQUEANTE] El descuento de menores se pierde en el checkout real

- **Sección:** 3.7 (Descuento a menores)
- **Severidad:** Bloqueante (impacto directo en ingresos reales)
- **Usuario / rol:** socio menor — DNI 77000002 (editado a menor de edad) / admin — DNI 42542525
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Loguear con un socio menor de edad.
  2. Ir a `/socio/cuotas` y agregar 2 meses al carrito.
  3. Confirmar el pago por transferencia y revisar el monto en cada paso: carrito, pantalla de
     transferencia, mail al club, vista de admin en `/admin/verificaciones`, notificación de
     aprobación al socio.
- **Esperado:** El 40% de descuento se refleja en TODOS esos pasos: $3.000/mes × 2 = $6.000
  en total, de punta a punta.
- **Obtenido:**
  - En el carrito (`/socio/cuotas`), el precio SÍ aparece descontado: "$3.000 × 1 mes".
  - Al pasar a la pantalla de transferencia, el monto salta a **$10.000** (precio completo
    para 2 meses, sin descuento).
  - El mail al club sobre el nuevo comprobante dice **$10.000**.
  - En `/admin/verificaciones`, el admin ve "Cuota Social Base x2 · $5.000 = $10.000" (precio
    de adulto).
  - La notificación de aprobación al socio también dice **$10.000**.
- **Evidencia:** Pago #152, DNI 77000002, orden de 2 meses.
- **Notas:** El descuento se calcula y se muestra correctamente en el paso inicial (probablemente
  en un cálculo del lado del frontend, solo para mostrar el resumen del carrito), pero se pierde
  apenas se arma la orden real — el precio que efectivamente queda guardado, cobrado y
  confirmado en cada pantalla posterior es el de adulto. Esto es un problema serio de
  integridad de cobro: un socio menor terminaría pagando (o el club registrando como cobrado)
  el doble de lo que corresponde. Revisar dónde se calcula el `precio_unitario` real de la
  orden en el backend (probablemente en el checkout/creación de orden) y confirmar que use el
  mismo descuento por edad que ya calcula bien la vista de "estado de cuenta"/carrito.

**Corrección aplicada (ronda 3) — a re-testear · era exactamente lo que sospechabas**

- **Causa raíz:** el descuento por menor de edad estaba implementado **cuatro veces**,
  copiado a mano en `routers/socio_cuotas.py`, `routers/admin_pagos.py` y
  `routers/admin_usuarios.py` — y **faltaba en `routers/socio_carrito.py`**, que es
  justamente por donde pasa el checkout real. `/socio/cuotas/estado` (lo que alimenta la
  vista del carrito) aplicaba el descuento; `POST /socio/carrito/checkout` congelaba
  `producto.precio_actual` crudo como `precio_unitario_historico`. De ahí el salto de $3.000
  a $10.000 justo al confirmar: el precio de adulto quedaba grabado en la orden, y todo lo
  que viene después —monto a transferir, mail al club, vista del admin, notificación de
  aprobación— lo leía de ahí. No era un problema de visualización en ningún punto.
- **Qué se hizo:** el motor de precios pasó a vivir en un módulo único,
  `utils/precios.py`, y los cuatro routers lo usan. La puerta de entrada es
  `precio_para_socio(producto, socio, db)`, que decide sola si corresponde descuento mirando
  la categoría del producto — quien arma un `DetalleOrden` no necesita saber qué categorías
  tienen descuento ni acordarse de preguntarlo. Eso es lo que impide que se repita: el bug
  no fue un cálculo mal hecho, fue un llamador nuevo que no se enteró de que existía la
  regla.
- **En el carrito**, el precio efectivo de cada ítem se resuelve **una sola vez** al validar
  el ítem y viaja junto con él. Antes cada bloque posterior (total global, subtotal por
  dominio, detalle de la orden, ítems de Mercado Pago, audit log) leía `precio_actual` por su
  cuenta — cinco lugares para desincronizarse. Ahora hay uno. El audit log además guarda
  `precio_lista` junto al cobrado, para que se pueda auditar el descuento aplicado.
- **Mercado Pago quedaba mal por el mismo motivo** (le pasaba el precio de lista al armar la
  preferencia), aunque hoy esté deshabilitado (condición E5).
- **Verificado por mí** (motor de precios, con cuota de $5.000 y descuento 40%): menor
  $3.000 · adulto $5.000 · socio sin fecha de nacimiento $5.000 (paga completa: no se puede
  afirmar que sea menor) · producto de tienda para un menor $5.000 (el descuento es solo de
  cuota social) · **2 meses de un menor = $6.000**, que es el caso exacto de la ficha, donde
  el bug daba $10.000.
- **Se agregó una regresión automática** en `scripts/qa_seguridad.py`: convierte al socio de
  prueba en menor, compara lo que muestra `/socio/cuotas/estado` contra lo que cobra
  `/socio/carrito/checkout` y verifica que sean iguales y menores al precio de lista. Esa
  suite necesita Docker/Postgres, así que **no la pude ejecutar acá**.
- ⚠️ **Órdenes ya creadas:** las que se generaron con el precio de adulto quedaron con ese
  monto congelado y el fix no las corrige. El pago #152 (DNI 77000002, 2 meses, $10.000) que
  usaste para reportar el bug es una de ellas: conviene rechazarla y volver a generarla, o
  ajustarla a mano.
**✅ Verificado en ejecución (2026-09-09, contra la branch de dev de Neon)**

Con un socio menor real de la base (id 17, nacido en 2010) y la cuota real de $5.000:

| Paso | Antes | Ahora |
|---|---|---|
| `/socio/cuotas/estado` | $3.000 | $3.000 |
| `POST /socio/carrito/checkout` (2 meses) | $10.000 | **$6.000** |
| `precio_unitario_historico` grabado en el DetalleOrden | $5.000 | **$3.000** |

El tercer punto es el que cierra el bug: ese valor congelado es el que después leen la vista
del admin, el mail al club y la notificación de aprobación. La orden de prueba se borró
(Pago, Orden, DetalleOrden, Notificación y AuditLog, por id exacto).

**Dimensión del problema:** en la base hay **150 socios y 38 son menores**. A cada uno se le
estaba cobrando $5.000 en vez de $3.000 por mes.

- **Igual conviene que lo mires vos** en el navegador, para confirmar los cinco puntos de la
  ficha tal como los ve una persona (carrito, pantalla de transferencia, mail al club, vista
  del admin, notificación de aprobación).

---

### BUG-13 — "Cancelar Trámite" usa confirmación nativa del navegador

- **Sección:** 3.3 / 3.9 (Cancelación de trámite por el socio)
- **Severidad:** Baja
- **Usuario / rol:** socio — DNI 77000001
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. Con una orden de cuota pendiente de verificación, ir a `/socio/cuotas`.
  2. Tocar "Cancelar Trámite".
- **Esperado:** Aparece un modal de confirmación in-app, consistente con el resto de las
  confirmaciones del sistema.
- **Obtenido:** Aparece el diálogo nativo del navegador (`window.confirm`), con la estética
  por defecto del sistema operativo/navegador, no la de la aplicación.
- **Evidencia:** Probado sobre la orden de 2 cuotas ($10.000) de DNI 77000001.
- **Notas:** Puramente cosmético/consistencia — la funcionalidad en sí funciona bien (la
  cancelación se registra correctamente en ambos lados, socio y admin). Reemplazar por el
  mismo componente de modal que se usa en el resto de las confirmaciones de la app.

**Corrección aplicada (ronda 3) — a re-testear**

- **Qué se hizo:** "Cancelar Trámite" en `/socio/cuotas` ahora abre un `<ConfirmDialog>`
  —el mismo componente que usa el resto de las acciones destructivas— en vez del
  `window.confirm()` del navegador. El texto además es sensible al método de pago: si el
  socio eligió efectivo le avisa que no cancele si ya pagó en el club, y si eligió
  transferencia le avisa que se anula también el comprobante subido.
- **De paso:** el error de esa acción se mostraba con `alert()` nativo; ahora se muestra
  in-app debajo del bloque.
- **Lo que NO toqué (quedan `window.confirm` en otras pantallas):** `AdminComercios` (2),
  `TecnicoPlanteles`, `SocioCarrito` ("¿Vaciar el carrito?"), `ReservaCalendar`,
  `AdminVerificaciones` (aprobar pago completo), y los bloques de FAQ y Sponsors del panel.
  No los cambié porque no estaban en el reporte y cada uno toca un flujo distinto que
  habría que re-testear. Si querés unificarlos todos, decime y los hago en una pasada.
- ⚠️ **Sin verificar por mí** (no tengo Node para levantar el frontend).
- **Al re-testear:** con una orden de cuota pendiente, tocar "Cancelar Trámite" y confirmar
  que aparece el modal de la app; probar "No, volver" y "Sí, cancelar".

**Ronda 4 (2026-09-11) — los `window.confirm` que quedaban**

`/socio/cuotas` ya usaba `<ConfirmDialog>` desde la ronda 3 (la corrida manual lo reportó
igual; puede haber sido contra el deploy anterior, o haber sido el diálogo de otra pantalla
del mismo flujo). Se reemplazaron los dos que quedaban en los caminos que toca el reporte:

- **"Vaciar carrito"** en `/carrito` — era el último popup del sistema operativo en el flujo
  de pago del socio.
- **"Aprobar Pago Completo"** en `/admin/verificaciones`, junto con el `window.alert()` del
  resumen de errores, que ahora es un aviso in-app con botón de cerrar.

Siguen en pie los de pantallas que no estaban en el reporte (FAQ, Sponsors, Comercios,
Planteles, ReservaCalendar) — listados en la ficha original.

---

### BUG-14 — [hallado en la ronda 3] 500 en recuperar contraseña cuando falla el envío del mail

- **Sección:** 2.4 (Recuperar password)
- **Severidad:** Media
- **Cómo apareció:** verificando D2 contra la base de dev. Un DNI **inexistente** devolvía
  el mensaje nuevo correctamente, pero un DNI que **sí existe** devolvía HTTP 500.
- **Causa raíz:** el `.env` de dev no tiene `RESEND_API_KEY`, así que httpx armaba el header
  `Authorization: Bearer ` (vacío) y lo rechazaba con
  `httpx.LocalProtocolError: Illegal header value b'Bearer '`. La excepción subía sin
  capturar hasta convertirse en un 500.
- **No lo introdujo D2.** El `await` del envío ya estaba dentro del mismo bloque antes del
  cambio, así que la excepción se propagaba igual. Lo que hizo D2 fue **exponerlo**: antes,
  como el mensaje era neutro, casi cualquier prueba se hacía con identificadores inventados
  que cortaban antes de llegar al envío.
- **Por qué importa en producción, no solo en dev:** si Resend se cae o la key se rota —y
  rotar todas las claves es uno de los pendientes explícitos del MVP— el socio recibe un 500
  en vez de algo que pueda entender. Peor: el token de recuperación **ya quedó generado y
  commiteado** en la base antes del intento de envío, así que el estado queda a medias.
- **Corregido en dos capas:**
  1. `mailer/services/email_service._enviar()` corta antes si no hay `RESEND_API_KEY`, con un
     mensaje que nombra la causa en vez del error críptico de httpx. Cubre a **todos** los
     mails del sistema, no solo a este.
  2. `routers/auth.solicitar_recuperacion` captura el fallo del envío, lo loguea con
     `logger.exception` y responde 200 con `estado: "error_envio"` y el texto *"Generamos el
     pedido, pero no pudimos enviarte el mail en este momento. Probá de nuevo en unos minutos
     o comunicate con el club."*
  3. El frontend pasó a mostrar el `mensaje` de **cualquier** respuesta con `ok:false`, en vez
     de enumerar los estados conocidos — así un estado nuevo del backend no lo agarra
     desprevenido mostrando un "listo, revisá tu mail" que sería mentira.
- **✅ Verificado:** los tres caminos devuelven 200 con el mensaje correcto, y el log del
  server ahora dice `RESEND_API_KEY no está configurada: no se puede enviar el mail
  'recuperar_password.html'...` en vez del error de httpx.
- **Al re-testear (en producción, con la key puesta):** pedir recupero con un DNI real y
  confirmar que el mail llega y que la respuesta es la de éxito.

---

### BUG-15 — [hallado en la ronda 3] El comprobante reemplazado no se ve hasta aprobar la orden

- **Sección:** 3.3 / 3.4 (comprobantes, admin)
- **Severidad:** Media
- **Usuario / rol:** admin — DNI 42542525
- **Dispositivo / navegador:** desktop
- **Pasos para reproducir:**
  1. En `/admin/verificaciones`, desplegar un Pago pendiente y reemplazar su comprobante.
  2. Abrir "Verificar" en una orden de ese mismo Pago.
- **Esperado:** Se ve el comprobante nuevo, el que se acaba de subir.
- **Obtenido:** Se sigue viendo el **anterior**. Recién después de aprobar la orden (que
  refresca la lista entera) aparece el nuevo.
- **Notas:** Es riesgoso más allá de lo cosmético: quien verifica puede aprobar mirando un
  comprobante que ya no es el que está guardado.

**Corregido en la ronda 4 (2026-09-11)**

No era caché del navegador ni del presigned URL (el key de S3 lleva un UUID, así que cada
reemplazo genera una URL distinta). Era estado duplicado en React: **la tarjeta del Pago y el
modal de verificación guardaban cada uno su propia copia** de la URL, inicializada al
montarse desde los datos con los que se cargó la bandeja. Reemplazar en uno no tenía forma de
enterar al otro, y el modal se monta de cero cada vez que se abre.

- **Corregido:** la URL vive en la pantalla, en un `Map` indexado por `id_pago` que comparten
  tarjeta y modal — el mismo patrón que `/admin/socios` ya usaba bien para su historial.
- **De paso (sugerencia §8.7 de la ronda 3):** en el historial de `/admin/socios`, que es
  donde se apilan varias fotos verticales una abajo de la otra, el comprobante ahora arranca
  **colapsado** con un ver/ocultar. En el modal de verificación sigue abierto, que es para lo
  que se entra ahí.

| Caso | Qué tiene que pasar |
|---|---|
| N1-A | Reemplazar desde la tarjeta → abrir "Verificar" muestra el **nuevo**, sin aprobar nada |
| N1-B | Reemplazar desde el modal → cerrar → la tarjeta muestra el nuevo |

---

### BUG-16 — [hallado en la ronda 5] El escáner no distingue "mes de ingreso" de "al día"

- **Sección:** 4.2 (Escáner en la puerta)
- **Severidad:** Baja
- **Usuario / rol:** socio DNI 77000006 (alta en el mes en curso) · operador: portero
- **Pasos para reproducir:**
  1. Escanear el QR (o tipear el DNI) de un socio dado de alta este mes, que todavía no pagó su primera cuota.
  2. Mirar la tarjeta de resultado.
- **Esperado:** el escáner lo deja entrar y además avisa que está en su mes de ingreso — verde, igual que "al día", pero diferenciado.
- **Obtenido:** dice simplemente "Al día", idéntico a un socio con la cuota efectivamente paga.
- **Notas:** no bloquea el acceso (entrar tiene que entrar), es información para el portero.

**Corrección (ronda 6) — a re-testear**

- **Causa:** el dato ya existía. `calcular_estado_financiero()` viene devolviendo
  `EstadoFinanciero.en_mes_ingreso` desde que se implementó D1, pero el wrapper del escáner
  —`_calcular_estado_financiero()` en `routers/qr_auth.py`— lo **descartaba**: devolvía una
  tupla `(moroso, cantidad_meses)` y el flag se perdía ahí mismo. O sea que la puerta era la
  única pantalla del sistema que no podía ver la diferencia, aunque el motor se la estuviera
  calculando.
- **Qué se hizo:** ese wrapper ahora devuelve el `EstadoFinanciero` entero. `en_mes_ingreso`
  viaja en `UsuarioQRValidacionResponse` y `AdminScanner.jsx` lo usa en dos lugares: el cartel
  grande dice **"SOCIO NUEVO ✓"** (mismo verde que "SOCIO HABILITADO ✓", porque entra igual) y
  la tarjeta suma el chip "Mes de ingreso — 1ª cuota pendiente". La línea de estado financiero
  pasa de "Al día ✓" a "Al día — mes de ingreso".
- **Privacidad:** `_minimizar_para_invitado()` lo borra, igual que los otros campos internos —
  un comercio adherido no tiene por qué saber si el socio recién se asoció.

| Caso | Qué tiene que pasar |
|---|---|
| 16-A | Escanear un socio en su mes de ingreso sin pagar → verde, "SOCIO NUEVO ✓" + chip "Mes de ingreso" |
| 16-B | El mismo socio después de pagar y aprobar la primera cuota → verde, "SOCIO HABILITADO ✓", **sin** chip |

---

### BUG-17 — [hallado en la ronda 5] El escáner no indica si el socio es menor de edad

- **Sección:** 4.2 (Escáner en la puerta)
- **Severidad:** Baja
- **Pasos para reproducir:** escanear el QR (o tipear el DNI) de un socio menor.
- **Esperado:** la tarjeta indica que es menor de edad.
- **Obtenido:** ninguna referencia a la edad.
- **Notas:** nice-to-have operativo, no bloqueante para el MVP.

**Corrección (ronda 6) — a re-testear**

- **Qué se hizo:** `es_menor` y `edad` se agregaron a `UsuarioQRValidacionResponse`, calculados
  con `utils.precios.es_menor()` / `calcular_edad()` — **la misma** función que decide el
  descuento de la cuota y el filtro "Menores" de `/admin/socios`, para que no puedan divergir
  (esa divergencia es justo lo que produjo BUG-10 y BUG-12). El chip "Menor de edad — N años"
  aparece en la tarjeta del escáner, junto al de mes de ingreso.
- **Sin fecha de nacimiento cargada** no se asume nada: `es_menor=false` y `edad=null`, que es
  la misma convención del motor de precios.
- **Privacidad:** también se recorta para el rol `invitado` (comercios adheridos).

| Caso | Qué tiene que pasar |
|---|---|
| 17-A | Escanear un socio menor (ej. DNI 77000002, nac. 2015) → chip "Menor de edad — N años". Un adulto, o un socio sin fecha de nacimiento → sin chip |

---

### BUG-18 — [hallado en la ronda 5] El socio no puede reemplazar el comprobante de una reserva desde `/mis-compras`

- **Sección:** 5 (Reservas de canchas / quincho)
- **Severidad:** Media
- **Usuario / rol:** socio DNI 77000006
- **Pasos para reproducir:**
  1. Reservar el quincho por transferencia y subir un comprobante.
  2. Ir a `/mis-compras` e intentar cambiarlo.
- **Esperado:** poder reemplazarlo mientras la orden siga pendiente, como ya se puede en `/socio/cuotas`.
- **Obtenido:** no hay ninguna opción — solo "Ver comprobante adjunto". El admin sí puede reemplazarlo desde `/admin/verificaciones`.

**Corrección (ronda 6) — a re-testear**

- **Causa:** era **solo frontend**. El endpoint `POST /socio/cuotas/pagos/{id_pago}/comprobante`
  nunca fue exclusivo de las cuotas: valida que el Pago sea del socio y esté en `pendiente`, y
  nada más — por diseño, porque el comprobante cuelga del **Pago**, no de la Orden (patrón
  Split-Order), así que un mismo archivo cubre cuota + alquiler + tienda de un mismo checkout.
  `/mis-compras` ya lo usaba para la primera subida; lo que faltaba era la puerta para volver a
  entrar: `puedeSubirComprobante` exigía `!comprobanteUrl`, o sea que una vez subido el archivo
  la tarjeta no ofrecía nada más.
- **Qué se hizo:** la tarjeta ahora muestra "Cambiar comprobante" junto a "Ver comprobante
  adjunto" mientras la orden esté en `pendiente_verificacion` y el método no sea efectivo ni
  Mercado Pago. Reusa el mismo `<UploadComprobante>`, en modo reemplazo. El backend, como ya
  hacía, borra el archivo viejo de S3 y **reinicia las 48 hs** de expiración de todas las
  órdenes hermanas del Pago.
- **Las órdenes de solo cuota no muestran el uploader acá** a propósito: ese trámite vive en
  `/socio/cuotas` y `/mis-compras` solo las resume (decisión D6).

| Caso | Qué tiene que pasar |
|---|---|
| 18-A | Reserva por transferencia con comprobante subido → en `/mis-compras` aparece "Cambiar comprobante"; reemplazarlo actualiza el link, y el admin ve el nuevo en `/admin/verificaciones` |
| 18-B | Una vez aprobada o rechazada la orden, el botón desaparece (y el backend rechaza la subida: el Pago ya no está en `pendiente`) |

---

### BUG-19 — [hallado en la ronda 7] Inhabilitar un turno desde `/admin/reservas` falla con "CORS / Failed to fetch"

- **Sección:** 5 (Reservas) / `/admin/reservas`
- **Severidad:** Alta
- **Usuario / rol:** admin general DNI 42542525
- **Pasos para reproducir:**
  1. Ir a `/admin/reservas`.
  2. Tocar un turno libre (ej. Quincho · 28/09 · Noche) y completar el motivo.
  3. Confirmar "Inhabilitar turno".
- **Esperado:** el turno queda bloqueado y desaparece de la vista del socio.
- **Obtenido:** `Failed to fetch` en el modal, y en consola
  `No 'Access-Control-Allow-Origin' header` sobre `/admin/reservas/bloqueo`.

**Corrección (ronda 8) — a re-testear**

- **Causa raíz: NO era CORS, ni el `include_router`, ni el decorator de auth.** Los tres se
  verificaron y estaban bien:
  - el router se incluye en `main.py` y la ruta existe — el `openapi()` de la app lista
    `POST /admin/reservas/bloqueo` y `DELETE /admin/reservas/bloqueo/{id_reserva}`;
  - las dos tienen `Depends(require_roles("admin_general", "personal_administrativo"))`;
  - `origins` en `main.py` ya incluye `https://clubatleticoroberts.com` **y**
    `https://www.clubatleticoroberts.com`.
- Lo que pasaba: **`reservas_instalaciones.id_producto` es `NOT NULL`** (migración 0001 y
  `models.ReservaInstalacion`), y `bloquear_turno()` construía la fila **sin ese campo**.
  Postgres rechazaba el INSERT con
  `null value in column "id_producto" ... violates not-null constraint`, la excepción subía
  sin manejar, y FastAPI devolvía un **500 generado por el `ServerErrorMiddleware`, que está
  POR FUERA del `CORSMiddleware`** — o sea, una respuesta sin el header
  `Access-Control-Allow-Origin`. El navegador no puede leer esa respuesta y reporta el error
  como si fuera de CORS. **El mensaje de CORS era el síntoma; el 500 era la causa.**
- El `401 — La contraseña es incorrecta` de `/auth/login` que aparecía en la misma consola es
  **una línea vieja, sin relación**: un intento de login previo. El request de bloqueo sí
  llevaba el `Authorization: Bearer`.
- **Verificado en ejecución** contra el schema real: insertar la fila sin `id_producto` da el
  `IntegrityError` de arriba, y con `id_producto` entra bien (prueba corrida dentro de una
  transacción con rollback, sin dejar datos).
- **El mismo bug estaba en `crear_reserva_manual()`** (el viejo botón "Nueva reserva"): también
  insertaba sin `id_producto` cuando no se registraba cobro. Se corrigió igual, aunque ese botón
  se eliminó por Mejora-02.
- **Qué se hizo:** módulo nuevo `backend/utils/reservas.py`, que resuelve qué
  `ProductoServicio` de alquiler corresponde a una celda de la grilla, con **la misma regla de
  nombres que ya usaba el socio** (`frontend/src/utils/reservas.js`:
  `Quincho — Turno Día` / `Quincho — Turno Noche` según la hora **del club**, `Cancha 1` /
  `Cancha 2`). En un bloqueo el producto es puro relleno de la FK —no hay plata de por medio y
  `_es_bloqueo_manual()` reconoce la franja por `id_usuario IS NULL AND id_orden IS NULL`—, así
  que si el producto propio del turno no está cargado se usa cualquier alquiler activo antes que
  negarle al admin poder cerrar la cancha. Si no hay **ninguno**, ahora devuelve un 422 que se
  lee, no un 500 disfrazado de CORS.

⚠️ **Dato de datos, no de código:** en la base hay productos de alquiler activos
`Quincho — Turno Día` (id 2), `Quincho — Turno Noche` (id 3) y `Cancha 1` (id 7). **No existe
`Cancha 2`.** Inhabilitar un turno de Cancha 2 funciona igual (cae en el reemplazo), pero
**asignarle un turno de Cancha 2 a un socio va a dar 422** pidiendo que se cargue el producto —
y por la misma razón el socio tampoco puede reservar Cancha 2 desde `/socio/cancha`. Hay que
darlo de alta en `/admin/productos` antes de probar §5 canchas.

| Caso | Qué tiene que pasar |
|---|---|
| 19-A | Quincho, turno libre → "Inhabilitar turno" con motivo → el turno queda rojo "Inhabilitado" en la agenda y **desaparece como disponible** en `/socio/reservas` |
| 19-B | El mismo turno, desde el detalle del bloqueo → "Quitar bloqueo y liberar el turno" → vuelve a estar libre para el socio |
| 19-C | Cancha 1, turno libre del día seleccionado → "Usar turno" → "Inhabilitar turno" → idem 19-A. Repetir en **Cancha 2** (funciona aunque falte el producto) |
| 19-D | Intentar inhabilitar un turno que ya está ocupado → 409 con el horario que choca, **no** un "Failed to fetch" |

---

### BUG-20 — [hallado en la ronda 7] El admin suspende una reserva pagada y el socio no se entera

- **Sección:** 5 (Reservas) / `/admin/reservas`
- **Severidad:** Alta
- **Usuario / rol:** admin DNI 42542525 sobre una reserva del socio DNI 77000001
- **Pasos para reproducir:**
  1. El socio tiene una reserva de quincho aprobada y pagada.
  2. El admin la abre en `/admin/reservas` y confirma "Suspender".
- **Esperado:** el socio se entera, la orden deja de decir "Aprobada" y hay una salida para la
  plata.
- **Obtenido:** el turno se liberaba en el calendario, pero el socio no recibía notificación
  in-app y `/mis-compras` seguía mostrando la orden como **Aprobada**.

**Corrección (ronda 8) — a re-testear**

- **Causa:** `suspender_reserva()` hacía **tres** de las cinco cosas que hacen falta: liberaba la
  franja, acreditaba el importe en `usuarios.saldo_a_favor` y mandaba el mail
  `reserva_suspendida`. Las dos que faltaban son justamente las que el socio mira primero: la
  **campana** y el **estado de la orden**. El mail existía pero depende de que el socio tenga
  email cargado y de que Resend entregue; la notificación in-app es lo único que siempre llega.
- **La devolución ya existía y es el saldo a favor** — es la decisión de diseño del proyecto
  ("los reintegros y las suspensiones acreditan saldo en `usuarios.saldo_a_favor`"). Lo que
  faltaba no era el mecanismo, sino **decírselo al socio y avisarle al admin** cuando ese saldo
  puede no alcanzar.
- **Qué se hizo:**
  1. **La orden pasa a `rechazada`** con un `motivo_rechazo` que arranca con
     `"Turno suspendido por el club: …"` y dice cuánto saldo se acreditó. No hay un estado
     `cancelada` en el sistema: el `CHECK` de `ordenes` solo admite
     `pendiente_verificacion | aprobada | rechazada | cancelada_socio | expirada`, y agregar uno
     nuevo obliga a migrar el constraint. **El `Pago` NO se toca**: la plata entró de verdad, el
     comprobante sigue siendo válido, y si el Pago agrupaba varias órdenes (split-order)
     rechazarlo tiraría abajo las hermanas que sí siguen en pie.
  2. **`/mis-compras` lo muestra distinto de un rechazo de pago:** cuando el motivo empieza con
     ese prefijo, el badge dice **"Cancelada por el club"** en ámbar, no "Rechazada" en rojo.
     Un rechazo de pago le dice al socio "revisá tu comprobante"; esto le dice "te cancelamos el
     turno y te devolvemos la plata", y pintarlos igual era lo que hacía parecer que el socio
     había hecho algo mal.
  3. **Notificación in-app** `reserva_cancelada` con el turno, el motivo, el monto acreditado y
     qué hacer si prefiere el dinero en vez del saldo.
  4. **El modal del admin avisa ANTES de confirmar:** si el turno se pagó por **transferencia**,
     cartel ámbar recordando que la devolución del dinero la gestiona el club a mano; si fue en
     **efectivo**, cartel azul indicando que se le entrega en el club. Para eso
     `ReservaAdminListResponse` ahora trae `metodo_pago`, y el detalle del turno lo muestra.
  5. **El mail `reserva_suspendida` suma el bloque de devolución** según el método, y el asunto
     ya no dice `quincho` sino `Quincho` (antes usaba la clave interna).
- **Punto 5 del pedido — reserva con pago pendiente:** verificado, **ya estaba cubierto y no se
  tocó**. Una reserva con la orden en `pendiente_verificacion` está en estado `bloqueada`, no
  `confirmada`, así que no ofrece "Suspender" sino **"Rechazar / Liberar turno"**, que pega a
  `POST /admin/ordenes/{id}/rechazar` — el camino que ya libera la franja, notifica y manda mail.
  El endpoint de suspensión, además, sigue devolviendo 422 sobre cualquier reserva que no esté
  `confirmada`.
- **De arrastre:** las reservas que el admin creaba con cobro **no se podían suspender**. El
  `DetalleOrden` nunca quedaba vinculado a la franja (`id_reserva` sin completar), y
  `suspender_reserva()` cortaba con "No se encontró el ítem de orden correspondiente a esta
  reserva". Ahora se vincula al crearla.

| Caso | Qué tiene que pasar |
|---|---|
| 20-A | Reserva **pagada por transferencia** → el detalle del turno muestra "Método: Transferencia"; al abrir "Suspender" aparece el cartel ámbar de devolución **antes** de confirmar |
| 20-B | Confirmar la suspensión → turno liberado, aviso verde arriba con el saldo nuevo + la advertencia de transferencia; en el socio: **campana con la notificación** y `/mis-compras` con el badge **"Cancelada por el club"** (ámbar) y el motivo debajo |
| 20-C | El socio revisa su saldo a favor: subió por el importe del turno, y lo puede aplicar en el checkout siguiente |
| 20-D | Reserva **con pago pendiente** (no aprobada) → el detalle ofrece "Rechazar / Liberar turno", **no** "Suspender"; rechazarla libera el turno y le llega el aviso de siempre |

---

### Mejora-01 — [ronda 7] El turno del carrito no se libera si el socio cierra sesión

- **Severidad:** Baja
- **Obtenido:** un turno en el carrito de un socio quedaba bloqueado para el resto hasta que
  alguien lo vaciara a mano.

**Corrección (ronda 8) — a re-testear**

- **Causa:** el TTL ya existía. `liberar_pre_reservas_expiradas()` libera toda franja
  `bloqueada` sin `id_orden` con más de **20 minutos** de antigüedad — pero solo se llamaba
  desde `expirar_ordenes_vencidas()`, que corre **cada 4 horas**. O sea que el mecanismo estaba,
  con una latencia inservible: el turno se liberaba cuando ya no le importaba a nadie.
- **Qué se hizo:** se enganchó la misma función también en `expirar_reservas_sin_pago()`, que
  corre **cada 15 minutos**, con su propio `try` para que un fallo ahí no impida el trabajo
  original del job. El turno vuelve a estar disponible a los ~20-35 minutos.
- **Por qué no se hizo el hook de logout:** cubriría solo el caso del socio que toca "Cerrar
  sesión". No cubre cerrar la pestaña, quedarse sin batería ni perder la conexión — que son la
  mayoría de los abandonos. El TTL los cubre todos y no agrega ningún endpoint.

⚠️ Sigue dependiendo de que **el proceso de Render esté despierto**: con el free tier dormido
los jobs no corren (condición conocida E2/E3). Es el mismo motivo por el que falta configurar
UptimeRobot contra `/health`.

| Caso | Qué tiene que pasar |
|---|---|
| M1-A | 77000001 agrega un turno al carrito y cierra sesión sin vaciarlo → a los ~35 min el turno vuelve a estar disponible para 77000002, sin que nadie toque nada |

---

### Mejora-02 — [ronda 7] "Nueva reserva" no correspondía al sistema de turnos fijos

- **Severidad:** Media

**Implementación (ronda 8) — a re-testear**

- **Se eliminó el botón "Nueva reserva manual"** del header de `/admin/reservas`. Pedía fecha y
  hora libres en un sistema donde los turnos son fijos: se podía cargar un alquiler de 10:20 a
  11:05 que no existe en ninguna grilla, y encima había que elegir el producto de una lista
  para que saliera el precio.
- **Un turno libre ahora abre una bifurcación:** "Asignar a un socio" / "Inhabilitar turno".
  En el quincho es el click sobre la celda; en canchas, el botón "Usar turno" de la fila del
  horario (antes decía "Inhabilitar" y hacía una sola cosa).
- **El modal de asignación** busca al socio por nombre o DNI, mantiene el botón
  "No es socio — usar la cuenta Invitado" para el alquiler de ventanilla sin socio, y pide
  **cómo pagó** (Efectivo / Transferencia, efectivo por default). El **importe sale del
  catálogo** según el turno, no de un selector: el frontend lo resuelve con
  `productoDeTurno()` y el backend repite la misma regla en `utils/reservas.py` por si el
  request no manda `id_producto`.
- **La reserva nace `confirmada` y pagada** (no pasa por verificación de comprobante, porque el
  admin la está cobrando en ventanilla) y **al socio le llega notificación in-app**
  `reserva_confirmada` — antes se enteraba recién al abrir `/socio/reservas`.
- `metodo_pago` ahora se guarda de verdad en el `Pago` (antes quedaba fijo en `"efectivo"`),
  que es lo que después alimenta el aviso de devolución de BUG-20.
- **Guarda de doble submit con `useRef`**, igual que el resto de los caminos de pago (BUG-03).

| Caso | Qué tiene que pasar |
|---|---|
| M2-A | El header de `/admin/reservas` ya **no** tiene el botón "Nueva reserva manual" |
| M2-B | Quincho, celda libre → "Asignar a un socio" → aparece el importe del turno ya resuelto; elegir socio por DNI, método Efectivo, confirmar → el turno queda azul "Pagada" con el nombre del socio, y el socio recibe la notificación |
| M2-C | El mismo flujo eligiendo **Transferencia** → el detalle del turno muestra "Método: Transferencia" (es lo que después dispara el aviso de BUG-20) |
| M2-D | Usar "No es socio — usar la cuenta Invitado" → el turno queda a nombre de Invitado y el ingreso aparece en Estadísticas |

⚠️ Para probar canchas hace falta dar de alta el producto **`Cancha 2`** en `/admin/productos`
(ver el aviso de BUG-19): hoy no existe, y sin él el modal muestra "Falta «Cancha 2» en el
catálogo" y no deja confirmar.

---

### D6 — Cuotas en `/mis-compras` y links de las notificaciones in-app

- **Sección:** 3.2 / 5.3 (transversal)
- **Origen:** se reportó como bug en tres rondas (BUG-06 en la ronda 1, y otra vez en las
  rondas 3 y 5), hasta que en la ronda 5 se cerró como decisión de producto.
- **Decisión (ronda 5 manual):** las compras de cuota **sí** aparecen en `/mis-compras`, pero
  con poco detalle; el desglose mes por mes sigue siendo de `/socio/cuotas`. Y las
  notificaciones de pago de cuota tienen que linkear a `/socio/cuotas`, no a `/mis-compras`.

**Implementación (ronda 6) — a re-testear**

- **El endpoint:** `listar_mis_compras()` excluía con un `NOT EXISTS` toda orden que tuviera un
  ítem de categoría `cuota_social`. Se sacó. Ahora devuelve todas las órdenes del socio; cuánto
  detalle mostrar quedó del lado de la pantalla, que es donde corresponde decidirlo.
- **La pantalla** (`SocioCompras.jsx`) separa los ítems por `producto.categoria`, que ya venía
  en la respuesta:
  - **Orden de solo cuota** → una línea, "Cuota social — N mes(es) — $X", y el link "Ver
    detalle en Gestión de Cuotas". Sin desglose de meses y sin uploader de comprobante.
  - **Orden mixta** → el listado normal de ítems y, al final, la línea "$X en cuotas (N meses)
    — Ver detalle →". Redundante a propósito, como se pidió.
- **Los links de las notificaciones:** el frontend no podía decidirlo solo, porque depende de
  **qué** se compró y la notificación solo trae `referencia_tabla` + `referencia_id`. Mandaba a
  `/mis-compras` cualquier notificación con `referencia_tabla='ordenes'` — incluido el
  "¡Pago verificado!" de una cuota, que caía en una pantalla donde esa compra ni figuraba.
  Ahora el backend calcula y manda `ruta_destino` en `NotificacionResponse`: **solo cuota** →
  `/socio/cuotas`; **mixta o tienda/alquiler** → `/mis-compras`; referencia que no existe o no
  es del socio → sin botón. Se resuelve en una sola query para toda la lista, y cubre también
  las notificaciones que apuntan a un Pago (`referencia_tabla='pagos'`, el aviso "Comprobante
  #N generado"), que antes no mostraban ningún botón de acción.
- **Sin migración de Alembic:** `ruta_destino` es un campo calculado del schema, no una columna.

| Caso | Qué tiene que pasar |
|---|---|
| D6-A | Pagar cuotas → la orden aparece en `/mis-compras` como "Cuota social — N mes(es) — $X" + link a Gestión de Cuotas |
| D6-B | Tocar ese link → cae en `/socio/cuotas` con el desglose completo |
| D6-C | Notificación "¡Pago verificado!" de una cuota → el botón dice "Ver en Gestión de Cuotas" y va a `/socio/cuotas` |
| D6-D | Notificación de una compra de tienda/alquiler → sigue diciendo "Ver detalle de compra" y va a `/mis-compras` |
| D6-E | *(cuando haya compras mixtas)* orden mixta → ítems normales + línea "$X en cuotas — Ver detalle" al final, y su notificación va a `/mis-compras` |

---

## Veredicto

| | |
|---|---|
| **Secciones completadas** | §1 y §2 completas. §3 completa hasta 3.9 (incluye 3.5 y 3.7, que quedaron pendientes en la ronda 1). Queda 3.8 (precio indexado). §4 en progreso — 4.1 completa, 4.2 a mitad de camino sin resultados capturados. §5 a §13 sin empezar. |
| **Hallazgos** | Bloqueantes: 1 (**BUG-12**, nuevo — reemplaza a BUG-08 que ya se cerró) · Altas: 2 (BUG-01, BUG-02, ambos reabiertos) · Medias: 5 (BUG-04 parcial, BUG-05 parcial, BUG-09, BUG-10, BUG-11) · Bajas: 1 (BUG-13) · Verificados y cerrados: 3 (BUG-03, BUG-06, BUG-08) · Reencuadrados como decisión: 1 (BUG-07 → D5) · Preguntas de negocio: D1, D2, D3 decididas (pendientes de implementar), D4 resuelta y verificada, D5 nueva. |
| **Regresiones reaparecidas** | #4 confirmada reabierta (BUG-01, con causa raíz ya identificada) y #5 confirmada reabierta (BUG-02, el fix no tuvo efecto) |
| **¿Listo para etapa 3 (limpieza de base + MVP)?** | **No todavía.** Hay un hallazgo bloqueante nuevo (**BUG-12**, cobro incorrecto a menores) que afecta directamente los ingresos del club, y dos bugs de la ronda 1 que reaparecieron sin arreglarse (BUG-01, BUG-02). Priorizar estos tres antes de seguir. |

**Notas de cierre:** Buen progreso en esta ronda — 3 de 8 hallazgos originales quedaron verdaderamente cerrados, y las decisiones de negocio que estaban trabando el avance (D1–D4) ya se tomaron. Pero aparecieron dos problemas de bloqueo tan o más serios que el original: BUG-02 sigue exactamente igual pese al fix, y BUG-12 es un problema de cobro real a menores que no estaba en el radar de la ronda 1. Recomendación: resolver BUG-12 primero (afecta plata real), después BUG-01 (ya tiene causa raíz clara, fix debería ser rápido) y BUG-02 (necesita diagnóstico de por qué el fix no prendió), y recién ahí retomar el checklist en 3.8 → §4 completo.

---

## Cómo retomar la corrida (ronda 1 → ronda 2, histórico)

_(Sección de la ronda anterior, se deja como registro histórico — para la guía vigente ver "Cómo retomar la corrida (ronda 3)" más abajo.)_

**Estado:** los 8 hallazgos están atendidos en código. **Nada está verificado** — no se pudo
ejecutar ni el frontend ni el backend acá (no hay Node ni las dependencias de Python
instaladas, ni Docker para levantar la base local), así que los arreglos son revisión de
código, no ejecución. El primer paso es deployar.

### Paso 0 — Deployar

El frontend y el backend cambiaron los dos, y varios arreglos son de **contrato**
(`es_activo` en los roles, `estado`/`motivo_rechazo` en el historial de cuotas,
`comprobante_url` firmada). **Deployar los dos, backend primero.**

Si solo se deploya el frontend, `rolesDeUsuario()` no va a recibir `es_activo` — no rompe
(el filtro lo trata como activo), pero BUG-01 no queda arreglado.

### Paso 1 — Re-testear los 8 hallazgos, en este orden

Es el orden más barato: cada uno desbloquea al siguiente.

| Orden | Qué probar | Cómo saber que está bien |
|---|---|---|
| 1 | **BUG-08** — socio paga cuota en **efectivo** → admin aprueba | El admin puede aprobar sin comprobante; el socio no ve "Subir Comprobante" ni "a transferir"; se acreditan los meses |
| 2 | **BUG-02** — alta manual → primer login → cambiar contraseña | Después de guardar, entra solo a `/socio` sin volver a loguearse |
| 3 | **BUG-01** — login con los 3 morosos (42542572, 37889456, 36778899) | Los 3 caen en `/socio`. Si alguno cae en `/admin`, chequear en `/admin/socios` qué roles tiene de verdad |
| 4 | **BUG-05** — subir comprobante **JPG y PDF**, mirarlos como socio y como admin | Los 4 casos abren la imagen/PDF. Ojo con el PDF, que antes se rompía distinto |
| 5 | **BUG-06** — rechazar una orden de cuota; abrir el link del mail `orden_generada` de una cuota pura | El rechazo queda visible en el historial de `/socio/cuotas` con el motivo; el link del mail abre `/socio/cuotas` |
| 6 | **BUG-03** — pedir recupero, usar el link, **volver a abrirlo** | La segunda vez muestra "Este link ya no sirve" con botón para pedir otro |
| 7 | **BUG-04** — `/socio/cuotas` con un moroso | El mes en curso dice **"Vence el 10"** en ámbar, no "Futuro". La deuda en pesos **no cambia** |
| 8 | **BUG-07** — aprobar una cuota y buscar el mail | Asunto **"✅ Compra confirmada"** (no "orden aprobada"). Si no llega, mirar logs de Render buscando `compra_confirmada` |

### Paso 2 — Retomar el checklist donde quedó

1. **§3 completo desde 3.2**, ahora sí con las 3 cuentas moroso (quedó sin probar el pago
   con esas cuentas).
2. **§3.5** (cobro manual desde admin) — no se probó nada.
3. **§3.7 y §3.8** (descuento a menores, precio indexado) — hace falta crear el
   `socio-menor` que quedó pendiente.
4. Recién ahí, **§4 en adelante**.

Antes de §4 conviene crear los usuarios de prueba que quedaron pendientes:
`socio-menor`, `socio-baja`, `staff` (personal_administrativo) y `portero` (admin_temporal).

### Paso 3 — Ítems que quedaron marcados "—" y conviene no perder

Están sueltos en las tablas de arriba, los junto acá:

- §2.2: DNI inexistente → mismo mensaje genérico; reintento después del rate limit
- §2.3: no se puede navegar a otra ruta antes de cambiar la contraseña
- §2.4: mail inexistente → mensaje neutro y NO llega mail; 6º pedido → rate limit
- §3.3: regresión #2 ("Pago en verificación" en vez de "MOROSO") con un socio **efectivamente
  moroso** — se probó con uno al día, así que la regresión sigue sin confirmar
- Regresión #3: forzar un **doble clic real** en confirmar
- Regresión #6 (badge de notificaciones) y #9 (foto de perfil): sin probar

### Preguntas de negocio: en qué quedaron

- **D4 (efectivo)** — **resuelta** con el arreglo de BUG-08: el efectivo no pide comprobante
  nunca, y lo aprueba el admin al cobrar. Falta solo decidir si el club quiere además poder
  adjuntar una foto como registro interno (hoy la trazabilidad queda en `audit_log`).
- **D1, D2, D3** — siguen abiertas, son decisiones del club, no código.

---

## Cómo retomar la corrida (ronda 3)

> ✅ **El "Paso 1 — Prioridad de arreglos" de abajo ya está hecho** (Claude Code,
> 2026-09-09). Los 10 puntos —BUG-01, BUG-02, BUG-04, BUG-05 (admin), BUG-09, BUG-10,
> BUG-11, BUG-12, BUG-13 y D1/D2/D3/D5— están corregidos en el working tree. El detalle
> de cada uno está en su ficha, bajo "Corrección aplicada (ronda 3)".
>
> **Lo que falta antes de re-testear: deployar** (ver "Estado de la ronda 3" al final de
> esta sección, con la lista de lo que NO pude verificar y por qué).

**Estado al cierre de la ronda 2:** 3 hallazgos cerrados y verificados (BUG-03, BUG-06,
BUG-08), 2 reabiertos (BUG-01, BUG-02), 2 parciales (BUG-04, BUG-05), 5 nuevos (BUG-09 a
BUG-13, con **BUG-12 bloqueante**), y 5 decisiones de negocio ya tomadas (D1–D4 resueltas o
pendientes de implementar, D5 nueva). §4.2 quedó a mitad de camino sin resultados
capturados.

### Paso 0 — Deployar de nuevo

Todo lo de esta ronda son cambios de código nuevos (no solo revisión) — deployar backend y
frontend antes de re-testear cualquier cosa.

### Paso 1 — Prioridad de arreglos, en este orden

| Orden | Qué | Por qué primero |
|---|---|---|
| 1 | **BUG-12** [bloqueante] — descuento de menores se pierde en el checkout real | Afecta plata real cobrada de más a un menor. Máxima prioridad. |
| 2 | **BUG-01** — routing post-login | Causa raíz ya identificada por la propia tester (cualquier rol además de `socio` manda a `/admin`). Fix acotado y claro. |
| 3 | **BUG-02** — cambio de contraseña obligatorio sigue sin redirigir | El fix anterior no tuvo efecto — diagnosticar por qué antes de re-intentar (ver la ficha: revisar si el deploy realmente se aplicó). |
| 4 | **BUG-05 (admin)** — agregar la vista de comprobante que falta en `/admin/verificaciones` y `/admin/socios` | Bloquea que el admin pueda auditar/verificar pagos con criterio. |
| 5 | **BUG-04** — unificar el contador de "meses adeudados" con el mismo corte de fecha que ya usa el calendario | Inconsistencia de datos visible, aunque no bloquea. |
| 6 | **D1, D2, D3** — implementar las 3 decisiones de negocio ya tomadas | Desbloquea, de paso, poder probar limpio la Regresión #2 (D1). |
| 7 | **BUG-09, BUG-10, BUG-11, BUG-13** | Menor urgencia, no bloquean flujos. |
| 8 | **D5** — rediseñar cuándo sale mail vs. in-app | Antes de esto no tiene sentido seguir persiguiendo el mail de aprobación de cuota. |

### Paso 2 — Re-testear en este orden (cada uno más barato si el anterior ya está bien)

1. BUG-12 con el mismo socio menor (DNI 77000002) — pagar 2 meses y confirmar $6.000 de punta
   a punta (carrito, transferencia, mail al club, admin, notificación de aprobación).
2. BUG-01 con DNI 42542572 y 37889456 (multi-rol) y con un `admin_temporal` puro nuevo.
3. BUG-02 con un alta manual nueva.
4. BUG-05 desde `/admin/verificaciones` y `/admin/socios`, con un JPG y un PDF.
5. BUG-04 el día antes y el día después del vencimiento de un moroso, mirando que el header y
   el calendario cambien juntos.
6. D1/D2/D3 una vez implementadas.
7. BUG-09, BUG-10, BUG-11, BUG-13.

### Paso 3 — Retomar el checklist donde quedó

1. Terminar **§3.8** (precio de cuota indexado) — no se tocó todavía.
2. Terminar **§4.2** (escáner) — repetir la prueba con DNI 77000001/77000002/77000003 y esta
   vez documentar el resultado (estado en `/socio` vs. resultado del escaneo del admin), y
   completar 4.3.
3. Crear los usuarios de prueba que siguen pendientes: `socio-baja`, `staff`
   (personal_administrativo), `portero` (admin_temporal) — recién tiene sentido crearlos una
   vez resuelto BUG-01, para no confundir un bug de routing general con un problema puntual
   de esos roles.
4. **§5 en adelante**, de punta a punta.

### Ítems que siguen sueltos, para no perder

- §2.2: DNI inexistente → mismo mensaje genérico; reintento después del rate limit.
- §2.3: no se puede navegar a otra ruta antes de cambiar la contraseña.
- Regresión #3: forzar un doble clic real en confirmar.
- Regresión #6 (badge de notificaciones) y #9 (foto de perfil): sin probar.
- §3.4: el caso clave de la transición moroso → al día en el instante de aprobación (bloqueado
  por D1).

---

## Estado de la ronda 3 (correcciones aplicadas por Claude Code, 2026-09-09)

### Deploy — hacer esto primero

Cambiaron **backend y frontend**, y hay cambios de **contrato** entre los dos, así que hay
que deployar los dos y **el backend primero**:

| Cambio de contrato | Si falta el backend |
|---|---|
| `fecha_nacimiento` / `fecha_ingreso` en `UsuarioListResponse` | El filtro "Menores" sigue vacío (BUG-10) |
| `en_mes_ingreso` en `/socio/cuotas/estado` | El socio nuevo no ve "Mes de ingreso" (D1) |
| `estado` / `mensaje` en `/auth/recuperar-password` | El mensaje sigue siendo neutro (D2) |
| `POST /admin/pagos/{id_pago}/comprobante` | El botón "Reemplazar" da 404 (BUG-05) |
| `metodo_pago` en el pago manual | El selector no se guarda (BUG-09) |

El backend se deploya en Render; el frontend sale solo por GitHub Actions al pushear
`frontend/**` a `main` (el workflow hace build → `s3 sync` → invalidación de CloudFront).

**No hay migraciones de Alembic** en esta ronda: no se agregó ni cambió ninguna columna.
La notificación in-app nueva de D5 reusa el tipo `sistema`, que ya está permitido por el
CHECK `chk_notificacion_tipo`, justamente para no necesitar una.

### Qué se implementó de D1/D2/D3/D5

**D1 — estado "mes de ingreso".** Se implementó tal como se pidió: el socio se ve al día
durante su mes de alta, **pero ese mes se sigue debiendo y se cobra de verdad**.

- Un socio nuevo ahora nace con la cobertura hasta el **vencimiento del mes anterior** al de
  ingreso (`cobertura_inicial_para_ingreso()`), no hasta el último día del mes de ingreso.
  Ese era el origen del problema: el mes de alta quedaba marcado como cubierto sin pago, y
  como el motor de pagos extiende la cobertura *desde* ahí, la primera cuota de cada socio
  nuevo **no se cobraba nunca**. Aplicado en los dos caminos de alta: aprobar una solicitud
  y el alta manual del admin.
- Para que igual no aparezca como moroso el día que se asocia, hay una ventana de gracia
  explícita: mientras `hoy` cae en el mismo mes calendario que `fecha_ingreso`, el motor
  devuelve 0 meses adeudados y marca `en_mes_ingreso = true`. **No es una condonación**: la
  deuda vive en la fecha de cobertura y aparece sola cuando el mes termina.
- En pantalla: estado propio **"Mes de ingreso"** (índigo, ícono aparte) en el calendario,
  con su chip en el resumen y su entrada en la leyenda — deliberadamente distinto de
  "Pagado". El header dice "Este es tu mes de ingreso... La cuota de este mes queda
  pendiente de pago", y el botón ofrece **"Pagar mi primera cuota"**.
- Verificado por mí (cálculo): ingreso 2026-09-05 con vencimiento 10 → cobertura inicial
  2026-08-10; al día durante todo septiembre; el 1 de octubre pasa a deber 1 mes (el de
  ingreso); y pagando 1 mes la cobertura avanza a 2026-09-10, o sea que **septiembre se
  cobró**.
- ⚠️ **Efecto sobre la suite de QA:** los usuarios de prueba de `scripts/qa_seguridad.py` se
  creaban sin `fecha_ingreso` explícita, así que tomaban la de hoy → caían todos en su mes
  de ingreso → los escenarios de deuda medían 0 y el test de BUG#1 habría fallado. `mkuser`
  ahora setea la fecha de ingreso dos años atrás (un socio con meses adeudados es, por
  definición, alguien que no se asoció este mes).
- ⚠️ **Solo afecta a socios nuevos.** Los que ya están en la base conservan su
  `mes_cubierto_hasta` actual, con el mes de alta dado por cubierto. Si el club quiere
  recuperar esa primera cuota de los socios cargados hasta hoy, es una corrección de datos
  aparte — decime y preparo el `UPDATE`, pero es plata que habría que reclamarle a socios
  existentes, así que es tu decisión, no un detalle técnico.

**D2 — mensaje de recuperar contraseña.** `/auth/recuperar-password` ya no responde neutro:
distingue `no_registrado` ("No encontramos ninguna cuenta con ese DNI o email...") y
`sin_email` ("Tu cuenta no tiene un email cargado... comunicate con el club"), y la pantalla
los muestra como error en vez de mandar a revisar una casilla que nunca va a recibir nada.

- ⚠️ **Es un cambio de postura de seguridad, hecho a pedido y con los ojos abiertos:** ahora
  cualquiera puede averiguar si un DNI está registrado en el club probándolo en el
  formulario (*enumeración de usuarios*). Se decidió priorizar que el socio entienda qué
  pasó. La mitigación que queda en pie es el **rate limit de 5 pedidos por hora por IP**,
  que es lo que hace impracticable barrer el padrón a fuerza bruta. Lo dejo anotado acá para
  que quede claro que fue una decisión y no un descuido.
- **No toqué `/registro`.** La ficha de D2 lo mencionaba como "probablemente"; ese endpoint
  ya responde explícitamente "El DNI ya está registrado" / "El email ya está registrado", así
  que no había nada vago que cambiar.

**D3 — datos de acceso en el mail de alta manual.** El mail de bienvenida ahora incluye el
**DNI** y la **contraseña temporal** que cargó el admin, en un bloque destacado. Antes decía
"usá el DNI y la contraseña temporal que te compartieron" sin que esa contraseña viajara por
ningún canal, así que el socio no tenía con qué entrar.

- Es aceptable mandar la clave en texto plano porque la cuenta nace con
  `requiere_cambio_password = true`: sirve para un único ingreso y el sistema fuerza a
  cambiarla antes de dejar navegar. El mail lo dice explícitamente ("hasta que la cambies,
  cualquiera que vea este mail puede entrar a tu cuenta").
- La contraseña **no** se loguea en ningún momento (los logs de Render los ve cualquiera con
  acceso al dashboard).
- Verificado por mí: el template renderiza el DNI y la clave, y si el llamador no pasa
  contraseña cae al texto genérico anterior sin dejar una línea vacía.

**D5 — mail solo en la aprobación, in-app para el resto.** Ver la ficha de BUG-07: se
eliminó el mail de "orden generada" y se reemplazó por una notificación in-app con texto
según el método de pago. El mail queda reservado para la aprobación.

- **Y apareció por qué los logs estaban vacíos:** el proyecto **no tenía ninguna
  configuración de logging**, así que todo `logger.info(...)` se descartaba en silencio —
  incluidos los diagnósticos que se habían agregado en la ronda 2 exactamente para
  diagnosticar este mail. Ya está configurado (`logging.basicConfig` en `main.py`,
  `LOG_LEVEL` por env var, default INFO). **Esto es lo que hace que el mail de aprobación
  sea diagnosticable en la ronda 3**, llegue o no.

### Verificación en ejecución (2026-09-09, después de configurar el entorno)

Con Node, el backend levantado y la branch de dev de Neon, pude ejecutar de verdad casi
todo lo que antes había quedado solo revisado. Resultados:

**Frontend — el build pasa.** `npm run build`: 1888 módulos transformados, **0 errores**.
Era el chequeo que faltaba: valida imports, JSX y nombres en los 10 archivos tocados.
(El warning de "chunks > 500 kB" es preexistente y no tiene que ver con estos cambios.)

**ESLint — no agregué ni un error.** El proyecto tiene 821 hallazgos, casi todos
`react/prop-types` preexistentes en todo el código. Filtrando lo que importa:
**cero `no-undef` en mis archivos**. Los 3 `no-unused-vars` que aparecen en
`AdminVerificaciones.jsx` (`Receipt`, `User`, `hayPendientes`) los verifiqué contra el
commit anterior: **ya estaban sin usar antes de mis cambios**. Lo único que sumé son 2
warnings de `react-refresh/only-export-components` en `RequireRole.jsx`, por exportar las
dos constantes de roles nuevas; afecta solo al fast-refresh del dev server, y el archivo ya
tenía ese patrón (igual que `CartContext` y `CalendarioMensual`).

**BUG-12 — verificado de punta a punta contra datos reales.** Con un socio menor de la base
(id 17, nacido en 2010) y la cuota real de $5.000:

| Paso | Resultado |
|---|---|
| `/socio/cuotas/estado` (lo que ve la pantalla) | $3.000 |
| `POST /socio/carrito/checkout` por 2 meses | **$6.000** (antes: $10.000) |
| `precio_unitario_historico` grabado en el DetalleOrden | **$3.000** |
| `orden.monto_total` | $6.000 |

El tercer punto es el que cierra el bug: ese es el valor congelado que después leen la
vista del admin, el mail al club y la notificación de aprobación. La orden de prueba se
borró después (Pago, Orden, DetalleOrden, Notificación y AuditLog, por id exacto).

**Cuánta plata era.** En la base de dev hay **150 socios, de los cuales 38 son menores**
según la misma regla que aplica el descuento. A cada uno se le estaba cobrando $5.000 en
vez de $3.000 por mes.

**BUG-10 — verificado contra el listado real.** `GET /admin/usuarios/` devuelve las 150
filas y ahora **las 150 traen `fecha_nacimiento` y `fecha_ingreso`**. Aplicando el mismo
filtro que usa el frontend, "Menores" devuelve **38 socios** — antes devolvía 0.

**D1 — verificado de punta a punta.** Alta manual real de un socio de prueba:

- `fecha_ingreso` 2026-09-10 → `mes_cubierto_hasta` quedó en **2026-08-10**, o sea el
  vencimiento del mes anterior. Antes quedaba 2026-09-30 y ese mes se daba por pagado.
- `/socio/cuotas/estado` devuelve `meses_adeudados: []`, `en_mes_ingreso: true`,
  `deuda_total_pesos: 0` → **el socio nuevo se ve al día**, como se pidió.
- Proyectado al 2026-10-10 (ya fuera del mes de ingreso): **1 mes adeudado, y es
  justamente 2026-09-10** — el mes de ingreso, que ahora sí se cobra.

El usuario de prueba se borró después.

**D2 — verificado, y apareció un 500 que no era mío.** Ver más abajo.

**D3 — el template renderiza** con DNI y contraseña, y sin contraseña cae al texto
genérico. El envío real no se puede probar acá porque el `.env` de dev no tiene
`RESEND_API_KEY`.

**El gate del primer ingreso es del backend, no solo del frontend.** Probando D1 me topé
con un 403 `requiere_cambio_password` al leer `/socio/cuotas/estado` con una cuenta que
todavía no cambió la clave. Es correcto y vale anotarlo: significa que el desbloqueo
optimista que hace ahora el frontend para BUG-02 **no puede saltearse nada** — si la
contraseña no se cambió de verdad, el backend sigue rechazando igual.

### Hallazgo nuevo de esta verificación — 500 en recuperar contraseña

Probando D2 con un DNI que **sí** existe, `/auth/recuperar-password` devolvió **HTTP 500**
con `httpx.LocalProtocolError: Illegal header value b'Bearer '`.

- **Causa:** con `RESEND_API_KEY` vacía, el header queda en `Bearer ` y httpx lo rechaza.
- **Es preexistente, no lo introdujo D2:** el `await` del envío ya estaba dentro del mismo
  bloque y la excepción se propagaba igual. Lo que hizo D2 fue exponerlo, porque antes
  cualquier identificador inexistente cortaba antes de llegar al envío.
- **Por qué importa igual en producción:** si Resend se cae o la key se rota (y rotar las
  claves es uno de los pendientes del MVP), el socio recibe un 500 en vez de algo accionable
  — y el token de recuperación ya quedó generado en la base.
- **Corregido, dos capas:**
  1. `_enviar()` corta antes con un mensaje que nombra la causa
     ("RESEND_API_KEY no está configurada..."), en vez del error críptico de httpx.
  2. `solicitar_recuperacion` captura el fallo del envío y responde 200 con
     `estado: "error_envio"` y un texto útil: *"Generamos el pedido, pero no pudimos
     enviarte el mail en este momento. Probá de nuevo en unos minutos o comunicate con el
     club."* El frontend ahora muestra el `mensaje` de cualquier respuesta con `ok:false`,
     así que un estado nuevo del backend no lo agarra desprevenido.
- **Verificado después del fix:** los tres caminos dan 200 con el mensaje correcto
  (`no_registrado`, `error_envio`, y `enviado` cuando haya key), y el log del server ahora
  dice la causa real.

### Arranque en Windows sin `PYTHONUTF8` — corregido

Aprovechando que lo mencionaste: `main.py` ahora fuerza UTF-8 en stdout/stderr al arrancar
(`sys.stdout.reconfigure`), envuelto en `try/except` por si la salida está redirigida.
Verificado arrancando el backend en PowerShell **sin** `PYTHONUTF8` ni `PYTHONIOENCODING`:
imprime el `✓` y los mensajes con acentos sin `UnicodeEncodeError`. En Linux (Render) es un
no-op. Ya no hace falta acordarse del env var en cada terminal nueva.

### Lo que sigue sin verificar

| Sin verificar | Por qué |
|---|---|
| **BUG-02, BUG-05 y BUG-13 en el navegador** | No tengo forma de manejar un browser desde acá. El build y ESLint descartan la clase de error "identificador roto / import mal escrito", pero el comportamiento en pantalla —que el redirect ocurra, que el comprobante se vea y se reemplace, que el modal aparezca— necesita tu pasada manual. Son los 3 primeros que probaría. |
| **`scripts/qa_seguridad.py`** | Necesita Docker + una base `car_test` aparte, y no hay Docker acá. Le agregué 3 escenarios (BUG-04, D1, BUG-12) y ajusté `mkuser`, pero no la corrí. |
| **Mails realmente entregados** | El `.env` de dev no tiene `RESEND_API_KEY`. Solo verifiqué el render del template y el manejo del fallo de envío. |

### Datos viejos que el código no arregla solo

Tres cosas que quedaron en la base de la ronda 2 y que ningún fix corrige hacia atrás:

1. **Pago #152** (DNI 77000002, 2 meses, $10.000) — se creó con precio de adulto por BUG-12.
   El monto quedó congelado en la orden. Conviene rechazarla y regenerarla.
2. **Órdenes de cobro manual anteriores** — guardadas con `metodo_pago = 'transferencia'`
   por BUG-09; van a seguir mostrándose así.
3. **Socios ya existentes y el mes de ingreso** — ver el ⚠️ de D1: conservan el mes de alta
   dado por cubierto.

### Riesgo anotado, no corregido

El frontend tiene el **40% de descuento hardcodeado** en `DESCUENTO_MENOR_PORCENTAJE`
(`AdminSocios.jsx`), mientras el backend lo lee de `ConfiguracionGlobal.descuento_menor_pct`,
que el Admin General puede editar desde el Catálogo de Productos. Hoy los dos valen 40, así
que no se nota. Si alguien cambia ese porcentaje, `/admin/socios` va a mostrar un precio
distinto del que el sistema cobra — la misma clase de divergencia que causó BUG-12, pero al
revés (backend bien, pantalla mal). No lo toqué porque no estaba en el reporte; conviene
resolverlo antes del MVP.

---

## Estado de la ronda 4 (correcciones aplicadas por Claude Code, 2026-09-11)

Responde a lo que encontró la corrida manual de la ronda 3 (§7 mes de ingreso, §8
comprobantes, §9 QR parcial). Todo está en el commit `914cc3b`, **pendiente de push**.

### Deploy

Push a `main` y salen los dos solos: el frontend por GitHub Actions (build → `s3 sync` →
invalidación de CloudFront) y el backend por **Render con Auto-Deploy** (la conexión es
Render↔GitHub, por eso no hay workflow de backend en el repo).

**No son atómicos**: tardan distinto. Y los fixes pesados de esta ronda son de **backend**
(BUG-07 y la gracia de mes de ingreso), así que si se empieza a testear apenas termina el
workflow del frontend, 7-x y D1-C pueden seguir fallando unos minutos contra el backend
viejo. Esperar a ver el deploy terminado en el dashboard de Render; con el free tier, además,
el primer request le pega a un proceso frío (40-60 s).

**No hay migraciones de Alembic**: no se tocó `models.py`.

### Qué se corrigió

| # | Qué era | Casos a retestear |
|---|---|---|
| BUG-07 | `autoflush=False` + decisión por query → el mail de aprobación no salía **nunca**, por ningún camino | 7-A…E |
| D1 / §7 | La gracia de mes de ingreso no se apagaba al pagar, y el inicio tenía una copia vieja del cálculo | D1-A…E |
| BUG-04 | El inicio del socio contaba un mes de más (misma copia vieja) | 04-A…C |
| BUG-01 | `es_activo` no viajaba en el schema, así que el filtro de roles del frontend no filtraba nada | 01-A…E |
| BUG-15 | Copia duplicada de la URL del comprobante en tarjeta y modal | N1-A/B |
| BUG-11 | El frontend mandaba el PUT de roles en toda edición | N2-A/B |
| BUG-13 | `confirm()` / `alert()` nativos que quedaban | — |

El detalle y la causa raíz de cada uno están en su ficha, en "Detalle de hallazgos".

### D1 — qué faltaba de la implementación de la ronda 3

La corrida manual dio en la tecla: la lógica estaba, pero mal terminada en tres puntos.

1. **El inicio mostraba MOROSO.** `SocioInicio.jsx` tenía su propia copia de
   `calcularEstadoFinanciero()`, que no conocía ni la gracia ni el corte por vencimiento. Las
   tres copias del frontend se unificaron en `frontend/src/utils/cuotas.js`.
2. **El calendario decía "Inactivo" en el mes de alta.** La regla comparaba el día de
   vencimiento contra la fecha de alta: un socio que se asocia el 11, con vencimiento el 10,
   tenía su propio mes marcado como "no era socio". Ahora la comparación es **por mes**.
3. **Pagar la primera cuota no actualizaba nada.** Dos cosas: la regla "pagado" del calendario
   usaba `<` contra `mes_cubierto_hasta`, que **es** el vencimiento del último mes cubierto
   (así que el mes recién pagado nunca entraba), y en el backend la gracia dependía solo del
   mes calendario, sin mirar si esa cuota ya estaba paga. Ahora `<=` y
   `mes_de_ingreso_pendiente()` respectivamente. **Ya no hace falta un segundo pago.**

También: si el socio en su mes de ingreso ya mandó la primera cuota y está esperando
verificación, las dos pantallas muestran "Pago en verificación" en vez de seguir
invitándolo a pagar. Y `/admin/socios` muestra "Mes de ingreso — 1ª cuota pendiente" en vez
de un "Al día" que parecía decir que el socio nuevo ya pagó.

| Caso | Qué tiene que pasar |
|---|---|
| D1-A | Alta de un socio nuevo → inicio muestra AL DÍA + "Mes de ingreso", nunca MOROSO |
| D1-B | Calendario: el mes de alta figura "Mes de ingreso" (índigo), no "Inactivo" |
| D1-C | Pagar la primera cuota y aprobarla → inicio "Al día" sin cartel de ingreso, calendario en verde, sin botón "Pagar mi primera cuota". **Sin un segundo pago** |
| D1-D | Alta con fecha posterior al día de vencimiento (ej. alta el 20, vence el 10) → igual figura "Mes de ingreso" |
| D1-E | Pasado el mes de ingreso sin pagar → ese mes pasa a adeudado (la deuda no se condona) |

⚠️ Para probar D1 hace falta **un socio dado de alta en el mes en curso**. El 77000004 ya
tiene pagos encima de las rondas anteriores, así que no sirve para ver el arranque limpio.

### Verificado de la ronda 3 que NO era bug

| Ítem de la ronda 3 | Conclusión |
|---|---|
| §2.6 — link de recupero vencido (>60 min) | Sin bug. El token expira a la hora, `resetear_password` lo valida, y la pantalla consulta `GET /auth/reset-password/estado` antes de mostrar el formulario. Lo que se ve "accesible" es la página, no el cambio |
| §3.12 — comprobante opcional en pagos en efectivo | Ya es posible: el botón "Adjuntar" del historial de `/admin/socios` sube un comprobante a un pago en cualquier estado, incluido efectivo |
| §3.2 / §5.3 — `/mis-compras` sin cuotas | No es bug, es una decisión sin cerrar → ahora está como **D6** en "Preguntas de negocio abiertas" |

### Regresión automatizada

Dos asserts nuevos en `backend/scripts/qa_seguridad.py` (sección "ronda 3 · D1"): que la
gracia de mes de ingreso se apague al pagar la primera cuota, y que el mes siguiente se siga
cobrando normal. Sigue sin correrse acá (necesita Docker + base `car_test`).

### Lo que sigue sin verificar

| Sin verificar | Por qué |
|---|---|
| Todo lo de esta ronda en el navegador | Mismo motivo de siempre: no manejo un browser desde acá. El build del frontend y ESLint pasan, y la lógica de cuotas está testeada por separado en backend y frontend con los mismos casos, pero el comportamiento en pantalla necesita tu pasada |
| Los mails realmente entregados | El `.env` de dev no tiene `RESEND_API_KEY`. La cadena de código quedó verificada por lectura; que Resend entregue, no |
| 7-D (expirada → reabierta → aprobada) | Necesita que el scheduler esté vivo para que algo expire. Con Render dormido no expira nada — sigue pendiente lo de UptimeRobot sobre `/health` |

### Riesgo anotado, no corregido

`backend/.env` (el de desarrollo local) tiene `DATABASE_URL` apuntando a la **Neon de
producción**. Importar `main.py` en local arranca el APScheduler contra la base real — me
pasó al validar los imports de esta ronda. Conviene apuntarlo a una base local (el
`docker-compose.yml` de la raíz levanta una) antes de seguir tocando backend. Sigue en pie,
además, lo del 40% hardcodeado en `AdminSocios.jsx` anotado en la ronda 3.

---

## Estado de la ronda 5 manual (Merlina, 2026-09-11)

Corrida sobre producción con el deploy de la ronda 4 (commit `914cc3b`) ya publicado.

### Resumen

| Bug / decisión | Veredicto |
|---|---|
| BUG-02 | ✅ **Verificado arreglado** — redirige a `/socio` tras cambiar la contraseña obligatoria |
| D1 (mes de ingreso) | ✅ **Verificado arreglado** — flujo completo OK con DNI 77000005 y 77000006 |
| D6 (cuotas en `/mis-compras`) | 🔧 **Decisión tomada** — ver abajo |
| §4 QR / escáner | ✅ **Verificado** — coherente en todos los casos probados |
| §5 Reservas / quincho | ✅ **Flujo completo verificado** — primer recorrido de punta a punta |
| Admin reservas | ⚠️ **Mejora propuesta** — rediseño del panel de admin |
| BUG-16 🆕 | ❌ Escáner no distingue "mes de ingreso" de "al día" |
| BUG-17 🆕 | ❌ Escáner no informa si el socio es menor de edad |
| BUG-18 🆕 | ❌ Socio no puede cambiar su comprobante en reservas/alquileres desde `/mis-compras` |

---

### BUG-02 — verificado arreglado

- **Usuario:** DNI 77000005 (alta manual, primer login)
- **Resultado:** al cambiar la contraseña en el primer ingreso, redirige automáticamente. ✅
- **Credenciales post-test:** DNI 77000005 · pass `wewewewe1` · mail `random5@gmail.com`

---

### D1 — mes de ingreso — verificado arreglado

Testeado con dos socios nuevos: **77000005** (alta manual) y **77000006** (alta manual).

**77000005:**

- Primer ingreso: inicio muestra **al día** + "mes de ingreso — tu primera cuota queda pendiente". ✅
- Texto en inicio: *"¡Bienvenido al club! Tu acceso ya está habilitado. La cuota de este mes, tu primera, queda pendiente de pago y podés abonarla cuando quieras."* ✅
- Septiembre figura **"Mes de ingreso"** en el calendario (no "Inactivo"). ✅
- Pago de primera cuota + aprobación admin → septiembre pasa a **"Pagado"**, sin botón "Pagar mi primera cuota", sin cartel de ingreso. ✅ **Sin necesitar un segundo pago.**

**Observación sobre el mes de ingreso (no es bug):**
Si el socio se da de alta entre el 1 y el 10 del mes (ej. 9 de septiembre, con vencimiento el 10), el mes de ingreso sigue siendo septiembre y la primera cuota vence el 10 de octubre. La situación desventajosa de darse de alta el 29 del mes (pagar casi un mes completo sin haber sido socio) es una decisión general de abonos mensuales, no un bug del sistema.

**D1-E (mes de ingreso vencido sin pagar):** pendiente de verificar de forma natural — requiere esperar al mes siguiente. La lógica de backend fue verificada por Claude Code en la ronda 4.

---

### D6 — cuotas en `/mis-compras` — decisión tomada

**Decisión:**
- Las notificaciones in-app de pagos de cuotas deben linkear a `/socio/cuotas` (no a `/mis-compras`). ← **A corregir.**
- Las compras de cuotas **también deben aparecer en `/mis-compras`** — pero con menos detalle que el historial de `/socio/cuotas`. Solo: "compraste $X en cuotas" con un link a `/socio/cuotas` para ver el desglose.
- **Compras mixtas** (cuota + alquiler + tienda): al final del listado de ítems debe aparecer "$ X en cuotas — Ver detalle en Gestión de Cuotas". Redundante pero claro.
- Este flujo se pulirá mejor cuando se prueben alquileres, tienda y compras mixtas de punta a punta.

**BUG a registrar:** los links de notificaciones in-app de cuotas apuntan a `/mis-compras` siendo que el detalle vive en `/socio/cuotas`. Corregir los links antes de las compras mixtas.

---

### §4 — QR y escáner

**QR en inicio:** coherente con el estado del socio en todos los casos. ✅

**Escáner general — casos verificados:**

| DNI | Estado en inicio | Resultado escáner |
|-----|-----------------|-------------------|
| 77000001 | Al día | ✅ Al día |
| 77000002 | Al día | ✅ Al día |
| 77000003 | Al día | ✅ Al día |
| 77000005 | Al día | ✅ Al día |
| 77000006 | Al día (mes de ingreso) | ⚠️ "Al día" — no distingue estado "mes de ingreso" |
| 77000006 (DNI directo, no registrado antes de crear) | — | ✅ "DNI NO ENCONTRADO" |
| QR de Google (random) | — | ✅ "Formato inválido — se esperaba UUID" |
| 55000033 (inactivo) | — | ✅ "DNI NO ENCONTRADO" (podría decir "DADO DE BAJA") |
| 14000111 (moroso, 1 mes) | — | ✅ "SOCIO NO HABILITADO" |
| 37889456 (moroso, 5 meses) | — | ✅ "SOCIO NO HABILITADO" |

**BUG-16 🆕 — Escáner no distingue "mes de ingreso" de "al día"**

- **Sección:** §4.2
- **Severidad:** Baja
- **Pasos:** Escanear QR o ingresar DNI de un socio en su mes de ingreso (ej. 77000006).
- **Esperado:** escáner muestra "Mes de ingreso" (en verde, igual que al día, pero diferenciado).
- **Obtenido:** muestra simplemente "Al día", sin distinción.
- **Notas:** no bloquea el acceso, es cosmético pero útil para el portero.

**BUG-17 🆕 — Escáner no informa si el socio es menor de edad**

- **Sección:** §4.2
- **Severidad:** Baja
- **Pasos:** Escanear QR o ingresar DNI de un socio menor.
- **Esperado:** escáner indica si el socio es menor de edad (podría ser útil operativamente).
- **Obtenido:** no aparece ninguna indicación de edad.
- **Notas:** nice-to-have, no es bloqueante para el MVP.

**Dato interesante:** el escáner sí muestra los roles del usuario (socio, jugador, etc.) correctamente.

---

### §5 — Reservas / quincho — primer recorrido completo

**Usuario:** DNI 77000006 · pass `wewewewe1` · mail `random6@gmail.com`

| Ítem | Resultado |
|------|-----------|
| Calendario de reservas: coherente, clara la selección | ✅ OK |
| Solo se puede seleccionar 1 turno a la vez (agregar al carrito y repetir) | ✅ OK — comportamiento esperado |
| Ítem en carrito: `Quincho — Noche — 2026-09-23` con valor | ✅ OK |
| Finalizar compra por transferencia + comprobante | ✅ OK |
| Notificación in-app al generar comprobante | ✅ OK |
| Mail al club con aviso de nueva compra | ✅ OK (mail real del club) |
| Orden aparece en `/mis-compras` del socio | ✅ OK — aparece con estado, detalle y "Ver comprobante" |
| Ver comprobante desde `/mis-compras` | ✅ OK |
| Reserva bloqueada en el calendario tras confirmar | ✅ OK |
| Admin ve la reserva en `/admin/verificaciones` + comprobante + opción de reemplazar | ✅ OK |
| Admin rechaza reserva → notificación in-app + `/mis-compras` actualizado | ✅ OK |
| Fecha se libera en el calendario tras rechazo | ✅ OK |
| Re-reservar la misma fecha en efectivo tras rechazo | ✅ OK |
| Admin aprueba pago en efectivo | ✅ OK |
| Notificaciones in-app completas y coherentes (generación + aprobación) | ✅ OK |
| Fecha queda bloqueada definitivamente tras aprobación | ✅ OK |

**BUG-18 🆕 — Socio no puede reemplazar comprobante de reserva desde `/mis-compras`**

- **Sección:** §5
- **Severidad:** Media
- **Pasos:** Hacer una reserva por transferencia desde el socio → ir a `/mis-compras` → intentar cambiar el comprobante.
- **Esperado:** el socio puede reemplazar el comprobante si lo subió mal (como sí puede en `/socio/cuotas`).
- **Obtenido:** no hay opción de reemplazar comprobante desde `/mis-compras`. El admin sí puede hacerlo desde `/verificaciones`.
- **Notas:** en cuotas sí existe esta funcionalidad. Sería consistente tenerla también en reservas/alquileres.

---

### Mejora propuesta — panel `/admin/reservas`

El panel actual tiene mucho ruido visual mezclando quincho y canchas. Propuesta:

**Quincho:**
- Mismo calendario que el socio, pero el admin ve quién tiene cada turno bloqueado (nombre del socio o motivo del bloqueo: "reunión de comisión", etc.).
- Vista más limpia que la lista actual.

**Canchas:**
- Mismo calendario mensual que el socio (el admin debe poder ver todos los meses, pasados y futuros, sin la restricción de 2 semanas del socio).
- Al seleccionar un día, se despliegan todos los turnos del día con su estado: libre / reservado por [socio] / inhabilitado por [motivo].
- Esto es más práctico que las listas actuales, especialmente con 6+ turnos por día.

**Estado:** 🔧 **Implementada en la ronda 6** — ver "Rediseño de `/admin/reservas`" al pie.
Se hicieron las dos vistas (quincho y canchas) y también la escritura de bloqueos manuales,
que era lo que podía quedar para una segunda pasada. **Retestear con los casos R-A…F.**

---

### Credenciales actualizadas post-ronda 5

| DNI | Contraseña | Mail | Estado |
|-----|-----------|------|--------|
| 42542525 | wewewewe | — | Admin general |
| 77000001 | wewewewe3 | tmtwifi0@gmail.com | Al día — pagado hasta abr 2027 |
| 77000002 | wewewewe1 | merlina.cabrera@alu.ing.unlp.edu.ar | Menor (nac. 2015) |
| 77000003 | wewewewe1 | — (random) | Alta manual |
| 77000004 | wewewewe1 | random4@gmail.com | Menor; tiene pagos de rondas anteriores |
| 77000005 | wewewewe1 | random5@gmail.com | Al día — primera cuota pagada (sept) |
| 77000006 | wewewewe1 | random6@gmail.com | Al día — reserva de quincho aprobada |
| 42542572 | wewewewe | cmerlina402@gmail.com | Moroso — 1 mes |
| 37889456 | wewewewe | — (random) | Moroso — varios meses |
| 36778899 | wewewewe | — (random) | Moroso — 15 meses |

---

### Índice actualizado de hallazgos nuevos de esta ronda

| # | Sección | Severidad | Resumen | Estado |
|---|---------|-----------|---------|--------|
| BUG-16 | 4.2 | Baja | Escáner no distingue "mes de ingreso" de "al día" | 🔧 Corregido en la ronda 6 — casos 16-A/B |
| BUG-17 | 4.2 | Baja | Escáner no informa si el socio es menor de edad | 🔧 Corregido en la ronda 6 — caso 17-A |
| BUG-18 | 5 | Media | Socio no puede reemplazar comprobante de reserva en `/mis-compras` | 🔧 Corregido en la ronda 6 — casos 18-A/B |

### Qué sigue sin verificar (para ronda 6)

> Actualizado tras la ronda 6 (correcciones de Claude Code). Lo tachado se corrigió o se
> verificó en código; sigue faltando la pasada en el navegador de todo lo de esa ronda.

- [x] ~~BUG-01, BUG-04, BUG-07, BUG-11, BUG-13, BUG-15 — retestear los casos A…E de la ronda 4~~ → **verificados en código** en la ronda 6 (incluido un diferencial de 484 casos entre el motor de cuotas del frontend y el del backend, 0 divergencias). **Falta la pasada en el navegador.**
- [ ] D1-E — mes de ingreso vencido sin pagar pasa a adeudado (requiere esperar al mes siguiente o simular)
- [x] ~~D6 — corrección de links de notificaciones in-app de cuotas~~ → corregido (casos D6-C/D)
- [x] ~~D6 — integrar cuotas en `/mis-compras`~~ → corregido (casos D6-A/B)
- [x] ~~BUG-18 — reemplazo de comprobante por socio en `/mis-compras`~~ → corregido (casos 18-A/B)
- [x] ~~BUG-16 / BUG-17 — escáner: mes de ingreso y menor de edad~~ → corregidos (casos 16-A/B, 17-A)
- [ ] §3 completo con socios morosos: flujo pago → aprobación → transición a "al día" + QR
- [ ] §5 — reservas de canchas (no probado todavía)
- [ ] §5 — compras mixtas (cuota + alquiler + tienda) — incluye el caso D6-E
- [x] ~~Panel `/admin/reservas` — mejora propuesta~~ → implementado en la ronda 6, **a retestear**:
  - [ ] R-A — Quincho: el mes se ve como la grilla del socio, y cada turno ocupado dice el nombre + DNI de quien lo tiene
  - [ ] R-B — Quincho: tocar un turno libre lo inhabilita con motivo; el socio lo ve ocupado en `/socio/reservas`
  - [ ] R-C — Canchas: elegir un día despliega abajo los 9 turnos con su estado; «Inhabilitar» funciona
  - [ ] R-D — Quitar un bloqueo devuelve el turno a la agenda, del lado del admin y del socio
  - [ ] R-E — Navegar a un mes pasado y a uno futuro; ver la reserva de quincho del 23/09 con el nombre del socio
  - [ ] R-F — Sobre la reserva de un socio siguen apareciendo «Rechazar / Liberar turno» y «Suspender por lluvia» (no «Quitar bloqueo»)
  - [ ] R-G — **Regresión de la extracción de turnos**: `/socio/reservas` y `/socio/cancha` siguen mostrando los mismos turnos y dejan reservar igual
- [ ] Correr `scripts/qa_seguridad.py` completo (necesita Docker + base `car_test`) — está sin ejecutar desde la ronda 3, y eso dejó pasar un bloque de regresión roto (ver ronda 6)
- [ ] `models.TIPOS_NOTIFICACION` desactualizado respecto del CHECK de la base — riesgo para el próximo autogenerate (ver ronda 6)
- [ ] Decidir si el cobro en ventanilla (`admin_pagos.py`) tiene que mandar mail — hoy no manda ninguno (ver ronda 6)
- [ ] `ReservaCalendar` sigue usando `window.confirm`/`alert` — último del camino del socio (BUG-13)
- [ ] 40% de descuento hardcodeado en `AdminSocios.jsx` — riesgo anotado, sin corregir
- [ ] Socios con roles múltiples (staff, portero) — pendiente desde ronda 2 (bloqueado por BUG-01; verificar si ahora está desbloqueado)

---

## Estado de la ronda 6 (correcciones aplicadas por Claude Code, 2026-09-11)

Responde a los tres hallazgos de la ronda 5 manual (BUG-16, BUG-17, BUG-18) y a la decisión
D6. Además, re-verificación **en código** de los casos que la ronda 5 dejó pendientes.
Pendiente de push/deploy.

### Deploy

Push a `main` y salen los dos solos: el frontend por GitHub Actions y el backend por Render
con Auto-Deploy. **No son atómicos.** Esta ronda tiene cambios de los dos lados y el frontend
depende del backend en tres de los cuatro fixes:

| Fix | Necesita backend nuevo | Síntoma si testeás con el backend viejo |
|---|---|---|
| BUG-16 | **Sí** (`en_mes_ingreso` en la respuesta del escáner) | El chip no aparece nunca (el campo llega `undefined`) |
| BUG-17 | **Sí** (`es_menor` / `edad`) | Ídem |
| D6 — cuotas en `/mis-compras` | **Sí** (el endpoint las excluía) | La pantalla sigue sin mostrar cuotas |
| D6 — links de notificaciones | **Sí** (`ruta_destino`) | **Desaparece el botón "Ver detalle"**: el frontend ahora lo condiciona a ese campo |
| BUG-18 | No (solo frontend) | — |

Esperar el deploy terminado en el dashboard de Render antes de testear; con el free tier, el
primer request le pega a un proceso frío (40–60 s).

**No hay migraciones de Alembic**: no se tocó `models.py`. `ruta_destino` es un campo
calculado del schema, no una columna.

### Qué se corrigió

| # | Qué era | Casos a retestear |
|---|---|---|
| BUG-16 | El escáner tiraba el flag `en_mes_ingreso` que el motor de cuotas ya calculaba | 16-A/B |
| BUG-17 | La edad no viajaba a la puerta | 17-A |
| BUG-18 | El uploader de `/mis-compras` solo aparecía si NO había comprobante | 18-A/B |
| D6 | Cuotas excluidas de `/mis-compras` + links de notificaciones al lugar equivocado | D6-A…D |
| `/admin/reservas` | Listas de filas que mezclaban quincho y canchas; no se podía ver de quién era cada turno ni bloquear uno a mano | R-A…G |

La causa raíz de cada uno está en su ficha, en "Detalle de hallazgos".

### Rediseño de `/admin/reservas` — implementado

La agenda era una lista de filas que mezclaba quincho y canchas: para saber si un turno
estaba libre había que leer la tabla entera y hacer la cuenta a mano. Se reemplazó por la
**misma grilla que ve el socio**, con las dos diferencias que necesita el club: cada turno
ocupado dice **de quién es**, y **no hay tope de fechas** (el admin navega a cualquier mes,
pasado o futuro). Las listas viejas se eliminaron por completo, junto con el toggle
Lista/Calendario y los filtros que existían solo para hacerlas navegables.

**Quincho** — grilla mensual con las dos franjas fijas (Día 9–19 / Noche 19–24), igual que
`/socio/reservas`. Cada franja muestra en la celda el nombre del socio y su DNI, o el motivo
si es un bloqueo del club. Tocar una franja libre la inhabilita; tocar una ocupada abre el
detalle.

**Canchas** — calendario mensual para elegir día (con el conteo de turnos ocupados e
inhabilitados por día) y, **debajo**, la lista de los 9 turnos de ese día uno por uno:

| Turno | Estado |
|---|---|
| 09:00–10:30 | 🟢 Libre · botón «Inhabilitar» |
| 10:30–12:00 | 👤 Mauro Cabrera · DNI 77000001 · badge del estado del pago |
| 12:00–13:30 | 🚫 Mantenimiento de la cancha · «Quitar bloqueo» desde el detalle |

**Qué se agregó en el backend:**

- `POST /admin/reservas/bloqueo` — inhabilita un turno. Es una franja `confirmada` **sin
  socio y sin orden**, con el motivo obligatorio (mínimo 3 caracteres). Chequea colisión →
  409 si el turno ya está tomado.
- `DELETE /admin/reservas/bloqueo/{id_reserva}` — lo quita y devuelve el turno a la agenda.
- `ReservaAdminListResponse.es_bloqueo_manual` — lo calcula el backend
  (`_es_bloqueo_manual()`), así el frontend no tiene que deducir la regla a partir de campos
  nulos. La regla es "sin socio, sin orden, y `confirmada`"; el `confirmada` es lo que deja
  afuera las **pre-reservas huérfanas de D2** (`bloqueada` con `id_orden` NULL), que ocupan
  la agenda pero no son bloqueos del club.
- **Sin migración**: no se tocó `models.py`. El bloqueo usa las columnas que ya existían.

**Por qué un endpoint nuevo y no `POST /admin/reservas` con el cobro vacío** (que es lo que
hacía la agenda vieja): ese endpoint exige un `nombre_responsable`, así que el motivo
terminaba guardado mezclado con un nombre de persona en el mismo campo de notas, y no había
forma de distinguir después un bloqueo de mantenimiento de la reserva de un socio. El modal
«Nueva reserva manual» **se mantiene** para el caso que sí es suyo: un alquiler cobrado en
ventanilla, con socio o con la cuenta Invitado.

**Lo que el endpoint de bloqueo NO hace, a propósito:** no sirve para cancelar la reserva de
un socio. Si se le pasa una, responde 422 y dice que use rechazar la orden o suspender — que
son los caminos que le avisan al socio y le devuelven la plata. Hay un assert de regresión
específico para esto, porque es la forma en que un endpoint así se convierte en un agujero.

**De yapa:** el DELETE también acepta una pre-reserva huérfana (`bloqueada` sin orden ni
socio). Son las 11 filas de **D2** que quedan cuando alguien abandona el carrito y el
scheduler está dormido: hasta ahora no había ninguna forma de sacarlas de la agenda a mano.

### Turnos: una sola definición para socio y admin

Los turnos estaban definidos dos veces —las franjas del quincho en `Reservas.jsx`, los
bloques horarios de cancha en `SocioCancha.jsx`—, cada una con su copia de `rangoTurno()` y
`turnoOcupado()`. Mientras el admin miraba una lista de filas daba igual. Con el admin
mostrando la misma grilla, no: dos definiciones distintas de "qué turnos hay" son dos
pantallas contradiciéndose sobre el mismo turno, y el síntoma sería el peor de todos —el
panel mostrando libre un turno que el socio no puede pedir, o dos personas con el mismo
quincho.

Se extrajeron a **`frontend/src/utils/reservas.js`**, que ahora importan las tres pantallas.
Es el mismo criterio que `utils/cuotas.js`, por la misma razón y después del mismo tipo de
bug (BUG-04, BUG-12).

⚠️ **Esto tocó `Reservas.jsx` y `SocioCancha.jsx`, que vos acabás de validar en la §5.** Es
una extracción mecánica —las funciones se movieron tal cual—, y hay un chequeo automático que
compara los valores del módulo nuevo contra los que tenían las pantallas antes (horarios,
nombres de producto, la grilla de 9 turnos de cancha, el rango del turno Noche que cruza la
medianoche): **17 asserts, todos en verde**. Aun así, conviene una pasada rápida por
`/socio/reservas` antes de dar por buena la §5.

### Bug encontrado al rediseñar — el último turno del mes no cargaba

El fetch de la agenda pedía `hasta = <último día del mes>`. El backend filtra
`fecha_inicio <= hasta` contra un `date`, que Postgres castea a **medianoche**: una reserva
del 30 de septiembre a las 19:00 quedaba afuera del rango. O sea que **el turno Noche del
último día de cada mes no aparecía nunca en la agenda del admin**. Venía de la versión
anterior de la pantalla; se veía poco porque la vista Lista no usaba ese filtro. Corregido:
ahora `hasta` va al día siguiente.

### Regresión automatizada de la agenda

Bloque nuevo en `scripts/qa_seguridad.py` (`ronda 6 · agenda del admin · bloqueos manuales`),
8 asserts: bloquear → 201 con `es_bloqueo_manual`; motivo vacío → 422; turno ya ocupado →
409; un socio intentando bloquear → 403; el turno bloqueado le figura ocupado al socio en
`/socio/reservas`; quitarlo → 200 y vuelve a estar libre; y el 422 de arriba, verificando
además que sea *por ser de un socio* y no por el estado. Necesita el servidor levantado:
**sin correr**, como el resto de la suite.

Del lado del frontend, el módulo `utils/reservas.js` se verificó con un script aparte
(17 asserts, 0 fallas) que además chequea que `esBloqueoManual()` dé exactamente lo mismo
que `_es_bloqueo_manual()` del backend en los 6 casos que importan.

### Hallazgo suelto — `ReservaCalendar.jsx` es código muerto

Buscando qué componentes de calendario reusar apareció que
`frontend/src/components/ReservaCalendar.jsx` **no lo importa nadie**. Y tiene tres cosas
feas adentro: un `window.confirm` + `alert()` (es el que figuraba en la ficha de **BUG-13**
como "el último del camino del socio" — no lo es, porque no se renderiza nunca), un producto
**hardcodeado** con `id_producto: 11` y precio `15000` fijo, y un typo en el JSX
(`defaultView="week"s`, que React interpreta como un atributo `s`). **No lo borré** —
borrar archivos no estaba en el pedido. Si querés, lo elimino en un commit aparte: hoy solo
suma ruido y una trampa para el que lo lea creyendo que está vivo.

Con eso, **BUG-13 queda sin nada pendiente en el camino del socio**: los `window.confirm`
que quedan vivos están todos en pantallas de admin/técnico (FAQ, Sponsors, Comercios,
Planteles).

### Lo que NO se hizo del rediseño

| Pendiente | Por qué |
|---|---|
| Editar el motivo de un bloqueo ya creado | Hoy se quita y se vuelve a crear. Si lo usan seguido, agrego un PATCH |
| Bloquear un rango de días de una (ej. toda una semana de mantenimiento) | Se bloquea turno por turno. Vale la pena recién si aparece el caso real |
| Turnos configurables desde el admin | Siguen fijos en `utils/reservas.js` (`TODO` que ya venía de antes, ahora en un solo lugar en vez de dos) |
| Los turnos pasados libres | Se muestran apagados y no se pueden inhabilitar: hacia atrás no cambia nada |

### Re-verificación en código de lo que quedó pendiente de la ronda 5

No manejo un navegador desde acá, así que esto **no reemplaza tu pasada en pantalla**. Lo que
sí se pudo hacer es verificar la lógica y el cableado, y eso descartó que alguno de estos
fixes estuviera a medio camino como pasó con D1 en la ronda 3.

| Caso | Qué se verificó | Resultado |
|---|---|---|
| BUG-01 / 01-A…E | `homePorRol()` es el único mapa de destino, y `RequireRole` usa el mismo (no puede haber doble redirect ni loop). `RolResponseSimple` manda `es_activo`, así que `rolesDeUsuario()` filtra los roles desactivados igual que `require_roles()` del backend | ✅ **Cableado correcto.** Solo `admin_general` devuelve `/admin`; el resto cae en `/socio` salvo portero/invitado puros, que van a `/admin/escaner` |
| BUG-04 / 04-A…C | No queda ninguna copia de `calcularEstadoFinanciero`: hay **una** definición en `utils/cuotas.js` y la importan las tres pantallas. Además se corrió un **diferencial de 484 casos** (24 fechas de cobertura × 4 de ingreso, más los casos sin cobertura) comparando `utils/cuotas.js` contra `utils/cuotas_periodos.py` | ✅ **0 divergencias** entre frontend y backend en moroso, cantidad de meses y `enMesIngreso` |
| BUG-07 / 7-A…E | El `db.flush()` está antes de la query que decide el mail; la bandera `mail_confirmacion_enviado` se levanta recién al encolar; el lock de la fila del Pago se toma después del flush. Los **cuatro** caminos que aprueban llaman a `finalizar_pago_si_corresponde()` | ✅ **Cadena completa.** El caso 7-D (expirar → reabrir → aprobar) sigue sin poder probarse acá: necesita el scheduler vivo |
| BUG-11 / N2-A/B | `rolesCambiaron` compara `selectedRoles` contra la foto `rolesIniciales` y el PUT de roles solo se manda si difieren — cubre también el caso de que el fetch de roles falle | ✅ **Correcto** |
| BUG-13 | `<ConfirmDialog>` en `/socio/cuotas`, `/carrito` ("Vaciar carrito") y `/admin/verificaciones` ("Aprobar Pago Completo"). Sigue habiendo `window.confirm`/`alert` en FAQ, Sponsors, Comercios, Planteles y **`ReservaCalendar`** | ⚠️ **Correcto en lo reportado.** `ReservaCalendar` es el único del camino del socio que queda, y lo tocaste en la §5 de la ronda 5 — no lo cambié para no alterar un flujo que acabás de validar. Decime y lo unifico |
| BUG-15 / N1-A/B | La URL del comprobante vive en la pantalla (`comprobantesReemplazados`, un `Map` indexado por `id_pago`) y se pasa por props a la tarjeta **y** al modal | ✅ **Una sola fuente** |

### Defecto encontrado en la suite de regresión (y corregido)

El bloque de regresión de **BUG-12** que se agregó en la ronda 4 a `scripts/qa_seguridad.py`
**no podía correr**: quedó con las llaves duplicadas de una plantilla (`f"{{BASE}}/..."`,
`json={{...}}`). Con esas llaves, la URL es el texto literal `{BASE}/socio/carrito/checkout` y
el `json=` es un `set` que contiene un `dict` → `TypeError` al evaluarse. Nunca se notó porque
el script necesita Docker + la base `car_test` y no se corrió desde entonces. Corregido.

Vale como recordatorio de que **los asserts de esa suite están sin ejecutar desde la ronda 3**:
los de D1 (ronda 4) y los nuevos de esta ronda tampoco corrieron contra la base de test.

### Regresión automatizada nueva

Dos bloques en `scripts/qa_seguridad.py`:

- **`ronda 5 · BUG-16/17`** — que el escáner marque al socio en su mes de ingreso, que deje de
  marcarlo apenas paga, que el menor viaje con su edad y que sin fecha de nacimiento no se
  asuma nada. Es lógica pura (no pega contra el servidor), así que **sí se pudo correr acá**:
  **4 PASS / 0 FAIL**.
- **`ronda 5 · D6`** — que la orden de cuota que crea el bloque de BUG-12 aparezca en
  `/socio/carrito/mis-compras`, y que cada detalle viaje con `producto.categoria` (si el schema
  dejara de mandarla, la pantalla mostraría "Producto x2" en vez de la línea de cuota). Este
  necesita el servidor levantado: **sin correr**, como el resto de la suite.

### Lo que sigue sin verificar

| Sin verificar | Por qué |
|---|---|
| Todo lo de esta ronda en el navegador | No manejo un browser desde acá. El build del frontend pasa y la lógica está verificada por separado, pero lo que se ve en pantalla necesita tu pasada |
| La suite `qa_seguridad.py` completa | Necesita Docker + base `car_test`; Docker Desktop no está corriendo en esta máquina |
| Los mails realmente entregados | El `.env` de dev no tiene `RESEND_API_KEY` |
| 7-D (expirada → reabierta → aprobada) | Necesita el scheduler vivo — sigue pendiente UptimeRobot sobre `/health` |
| D1-E (mes de ingreso vencido sin pagar) | Requiere esperar al mes siguiente. La lógica está cubierta por el assert "pasado su mes de ingreso, ese mes se debe" |

### Observación nueva, sin corregir — el cobro en ventanilla no manda mail

Verificando la cadena de BUG-07 apareció que `routers/admin_pagos.py` —el cobro manual desde
`/admin/socios`— **no manda ningún mail**: no importa `email_tasks` ni llama a
`finalizar_pago_si_corresponde()`. Crea la orden ya aprobada y deja solo la notificación
in-app. No lo toqué porque **no está claro que sea un bug**: el socio está parado en la
ventanilla cuando se registra el pago, así que el mail puede ser redundante. Pero **D5 dijo
"mail cuando el pago se aprueba"**, y este es un camino de aprobación que no lo manda. Queda
como decisión a tomar, no como corrección pendiente.

### Riesgos anotados, no corregidos

- `models.TIPOS_NOTIFICACION` está **desactualizado**: no incluye `beca_actualizada` ni
  `pago_verificado`, que la migración `a7b8c9d0e1f2` sí agregó al CHECK de la base. Como
  `models.py` es la fuente de verdad para autogenerar migraciones, el próximo
  `alembic revision --autogenerate` que toque esa tabla puede proponer **revertir** el CHECK a
  la lista vieja y romper la asignación de becas. No lo cambié porque toca `models.py` y esta
  ronda se cerró sin migraciones; conviene arreglarlo en la próxima que sí tenga una.
- Sigue en pie que `backend/.env` apunta a la **Neon de producción** (importar `main.py` en
  local arranca el APScheduler contra la base real), y el 40% hardcodeado en `AdminSocios.jsx`.
- `tzdata` sigue sin estar en `requirements.txt`. En Render (Linux) da igual, pero en Windows
  cualquier import de `utils/fechas.py` o `routers/qr_auth.py` revienta con
  `ZoneInfoNotFoundError` si el venv no lo tiene. El venv de `backend/` ya lo tiene instalado,
  por eso las verificaciones de esta ronda pudieron correr.

---

## Estado de la ronda 7 manual (Merlina, 2026-09-11)

Corrida sobre producción con el deploy de la ronda 6 (correcciones Claude Code, `/admin/reservas` rediseñado) ya publicado.

### Resumen ronda 7

| Área | Veredicto |
|------|-----------|
| §5 Reservas quincho — selección múltiple de turnos | ✅ OK |
| §5 Reservas quincho — pago efectivo + aprobación admin | ✅ OK |
| `/mis-compras` actualizado con cuotas y reservas | ✅ **D6 verificado en navegador** |
| `/admin/reservas` — vista de reservas por socio | ✅ OK |
| `/admin/reservas` — suspensión de reserva pagada | ✅ OK |
| `/admin/reservas` — inhabilitar turno (bloqueo manual) | ❌ **BUG-19** — `failed to fetch` / CORS 401 |
| Cancelación admin sin aviso al socio ni comprobante de devolución | ❌ **BUG-20** |
| Carrito: turno en carrito de otro socio inhabilita el turno para el resto | ✅ OK — comportamiento esperado |
| Carrito: vaciar carrito libera el turno | ✅ OK |
| Carrito: turno no se libera si se cierra sesión sin vaciar | ⚠️ **Mejora-01** — pre-reserva huérfana si el socio cierra sesión |
| `/admin/reservas` — "Nueva reserva" (botón) | ⚠️ **Mejora-02** — propuesta de rediseño del flujo |

---

### D6 — verificado en navegador ✅

`/mis-compras` del DNI 77000001 muestra historial completo con cuotas resumidas y reservas:

```
Mis Compras
Historial y estado de tus pedidos: tienda, alquileres y cuota social.

11/09 · Orden #169 · Aprobada
Quincho — Turno Día x1 · Total $15.000,00

09/09 · Orden #162 · Cancelada
Cuota social — 2 meses — $10.000,00
Ver detalle en Gestión de Cuotas · Total $10.000,00

09/09 · Orden #160 · Aprobada
Cuota social — 2 meses — $10.000,00
Ver detalle en Gestión de Cuotas · Total $10.000,00
[...]
```

Las cuotas aparecen con la línea resumida y el link a `/socio/cuotas`. ✅
Las reservas de alquiler aparecen con detalle del ítem. ✅

---

### §5 — Reservas quincho — verificado

**Usuario:** DNI 77000001 · pass `wewewewe2`

| Ítem | Resultado |
|------|-----------|
| Seleccionar turno Día y Noche de la misma fecha | ✅ Ambos se marcan reservados en tiempo real |
| Anular desde el carrito → el turno se libera instantáneamente | ✅ OK |
| Vaciar carrito → los turnos se liberan | ✅ OK |
| Pago en efectivo (turno seleccionado) | ✅ OK |
| La reserva queda bloqueada en `/socio/reservas` tras confirmar | ✅ OK |
| Notificación in-app al aprobar (desde admin) | ✅ OK |
| Fecha queda definitivamente bloqueada tras aprobación | ✅ OK |

---

### `/admin/reservas` — verificado parcialmente

| Ítem | Resultado |
|------|-----------|
| Ver reservas en calendario con nombre del socio | ✅ OK |
| Click en reserva pagada → opción "Suspender" + aviso de que está pagada | ✅ OK |
| Confirmar suspensión → turno liberado instantáneamente en el calendario | ✅ OK |
| Socio (77000001) no recibe notificación in-app tras la suspensión | ❌ **BUG-20** |
| Socio: `/mis-compras` sigue diciendo "Aprobada" tras la suspensión | ❌ **BUG-20** |
| Socio: turno liberado visible en `/socio/reservas` | ✅ OK (el turno se libera) |
| Inhabilitar turno manual (bloqueo de club) | ❌ **BUG-19** — falla con CORS/401 |

---

### BUG-19 🆕 — Inhabilitar turno desde `/admin/reservas` falla con CORS 401

- **Sección:** §5 / `/admin/reservas`
- **Severidad:** Alta
- **Pasos:**
  1. Ingresar como admin (DNI 42542525).
  2. Ir a `/admin/reservas`.
  3. Seleccionar un turno libre (ej. Quincho · 28/09 · Noche).
  4. Completar el motivo y confirmar "Inhabilitar turno".
- **Esperado:** el turno queda bloqueado y desaparece de la vista del socio.
- **Obtenido:** `Failed to fetch` en el modal. Consola:
  ```
  club-atletico-api.onrender.com/admin/reservas/bloqueo: CORS — No 'Access-Control-Allow-Origin' header
  club-atletico-api.onrender.com/auth/login: 401 — La contraseña es incorrecta.
  ```
- **Causa probable:** el endpoint `POST /admin/reservas/bloqueo` es nuevo (ronda 6) y falta en la lista de orígenes permitidos del CORS de Render, o bien hay un problema de autenticación en ese endpoint específico. El 401 del login sugiere que el frontend está mandando la request sin el token de sesión del admin (¿el endpoint no está decorado con `require_roles(['admin_general'])`?).
- **Notas:** el endpoint existe (fue commiteado en la ronda 6) pero nunca se probó en el navegador contra producción. Verificar que: (a) la ruta esté en el router correcto con `include_router`, (b) el CORS de Render incluya la URL de producción, (c) el endpoint tenga el decorator de auth correspondiente.

---

### BUG-20 🆕 — Admin suspende reserva pagada sin aviso al socio ni comprobante de devolución

- **Sección:** §5 / `/admin/reservas`
- **Severidad:** Alta
- **Pasos:**
  1. Socio (DNI 77000001) tiene una reserva aprobada de quincho (Turno Día, pagada).
  2. Admin (42542525) va a `/admin/reservas`, hace click en esa reserva y confirma "Suspender".
  3. El turno se libera en el calendario del admin. ✅
  4. Revisar la cuenta del socio.
- **Esperado:**
  - El socio recibe una **notificación in-app** indicando que su reserva fue cancelada.
  - En `/mis-compras`, la orden pasa de "Aprobada" a "Cancelada" o similar.
  - Si el pago fue por transferencia: el sistema genera o avisa sobre la necesidad de una **devolución**. Idealmente un comprobante de cancelación o un mail con instrucciones.
- **Obtenido:**
  - No llega ninguna notificación in-app al socio.
  - `/mis-compras` del socio sigue mostrando la orden como "Aprobada".
  - El turno simplemente desaparece del calendario del socio, sin aviso alguno.
- **Notas:** este es un caso crítico para el MVP, especialmente si el socio ya pagó por transferencia y el admin cancela la reserva. Sin aviso ni flujo de devolución, el socio pierde el dinero sin enterarse. Propuesta:
  1. Al suspender una reserva pagada: notificación in-app obligatoria + actualizar estado de la orden a "Cancelada" en `/mis-compras`.
  2. Si el método de pago fue transferencia: modal de confirmación adicional en el admin preguntando si ya se realizó la devolución, y si corresponde enviar un mail al socio con instrucciones.
  3. Si fue efectivo: simplemente notificar que la reserva fue cancelada y que se le devuelve el efectivo en el club.

---

### Mejora-01 — Turno en carrito no se libera si el socio cierra sesión

- **Severidad:** Baja (comportamiento parcialmente esperado)
- **Observación:** si el socio DNI 77000001 tiene un turno en su carrito y cierra sesión sin vaciarlo, el turno queda bloqueado para otros socios. Solo se libera si vacía el carrito manualmente antes de salir.
- **Comportamiento actual:** otro socio (DNI 77000002) no puede reservar ese turno mientras esté en el carrito de 77000001.
- **Comportamiento esperado (sugerido):** al cerrar sesión, los turnos en carrito deberían liberarse automáticamente. O bien, un TTL corto (~15 min sin actividad) para las pre-reservas del carrito.
- **Notas:** esto genera pre-reservas huérfanas si el socio abandona sin vaciar (el mismo escenario de D2/E3). El DELETE del `bloqueo/{id}` que se agregó en la ronda 6 permite limpiarlas manualmente desde el admin, pero no es la solución ideal. Se puede manejar con el scheduler o con un hook de logout.

---

### Mejora-02 — Botón "Nueva reserva" en `/admin/reservas` no corresponde al sistema de turnos

- **Severidad:** Media
- **Observación:** el botón "Nueva reserva" del panel de admin pide fecha y hora libre, lo que no se corresponde con los turnos fijos del sistema (Día/Noche para quincho, turnos de 90 min para canchas).
- **Propuesta de rediseño:**
  - **Eliminar** el botón "Nueva reserva" actual.
  - **Para canchas:** en la lista de turnos del día seleccionado, al lado del botón "Inhabilitar", agregar un botón **"Reservar"** que abra un modal para asignarle el turno a un socio (buscar por DNI o nombre) y registrarlo directamente. Así le llega la confirmación al socio como si fuera una reserva normal.
  - **Para quincho:** misma lógica — desde la celda del turno libre en el calendario, poder asignarlo a un socio directamente.
  - El flujo de "alquiler cobrado en ventanilla" (sin socio específico o con cuenta Invitado) podría mantenerse como modal secundario desde esa misma celda.

---

### Concurrencia de socios en reservas — verificado

**Usuarios:** DNI 77000001 (PC) + DNI 77000002 (otra pestaña/browser)

| Caso | Resultado |
|------|-----------|
| 77000001 agrega Noche del 27/09 al carrito | ✅ Turno en carrito de 77000001 |
| 77000002 intenta agregar Noche del 27/09 | ✅ "Ese turno ya no está disponible. Elegí otro horario." |
| 77000001 vacía su carrito | ✅ El turno se habilita y 77000002 puede tomarlo |
| Turno en carrito de 77000001 sin vaciar + cierre de sesión | ⚠️ Mejora-01 — turno sigue bloqueado para 77000002 |

---

### Nuevos hallazgos para el índice

| # | Sección | Severidad | Resumen | Estado |
|---|---------|-----------|---------|--------|
| BUG-19 🆕 | 5 / admin/reservas | Alta | `POST /admin/reservas/bloqueo` falla con CORS/401 en producción | ❌ Abierto |
| BUG-20 🆕 | 5 / admin/reservas | Alta | Admin suspende reserva pagada sin aviso al socio ni flujo de devolución | ❌ Abierto |
| Mejora-01 | 5 / carrito | Baja | Turno en carrito no se libera al cerrar sesión | ⚠️ Abierto |
| Mejora-02 | 5 / admin/reservas | Media | Botón "Nueva reserva" no corresponde al sistema de turnos fijos | ⚠️ Abierto |

---

### Credenciales actualizadas post-ronda 7

| DNI | Contraseña | Mail | Estado |
|-----|-----------|------|--------|
| 42542525 | wewewewe | — | Admin general |
| 77000001 | wewewewe2 | tmtwifi0@gmail.com | Al día — pagado hasta abr 2027; reservas de quincho activas |
| 77000002 | wewewewe1 | merlina.cabrera@alu.ing.unlp.edu.ar | Menor (nac. 2015) |
| 77000003 | wewewewe1 | — (random) | Alta manual |
| 77000004 | wewewewe1 | random4@gmail.com | Menor; pagos de rondas anteriores |
| 77000005 | wewewewe1 | random5@gmail.com | Al día — primera cuota pagada (sept) |
| 77000006 | wewewewe1 | random6@gmail.com | Al día — reservas de quincho probadas en ronda 6 |
| 42542572 | wewewewe | cmerlina402@gmail.com | Moroso — 1 mes |
| 37889456 | wewewewe | — (random) | Moroso — varios meses |
| 36778899 | wewewewe | — (random) | Moroso — 15 meses |

---

### Qué sigue para ronda 8

- [ ] **BUG-19** (crítico): endpoint `POST /admin/reservas/bloqueo` — verificar CORS, router include y decorator de auth en producción
- [ ] **BUG-20** (crítico): flujo de cancelación de reserva pagada por admin — notificación al socio, actualización de estado en `/mis-compras`, y flujo de devolución si fue por transferencia
- [ ] **Mejora-02**: reemplazar "Nueva reserva" por flujo de asignación desde la celda del turno
- [ ] **Mejora-01** (opcional): TTL para pre-reservas de carrito o liberación al logout
- [ ] Retestear en navegador los casos que solo se verificaron en código: BUG-01, BUG-04, BUG-07, BUG-11, BUG-13, BUG-15, BUG-16, BUG-17, BUG-18, D6
- [ ] §5 reservas de canchas — no probado todavía
- [ ] §5 compras mixtas (cuota + alquiler + tienda)
- [ ] §3 socios morosos — flujo pago → aprobación → transición "al día" + QR
- [ ] `ReservaCalendar.jsx` — decidir si se borra (código muerto con window.confirm y precio hardcodeado)
- [ ] Decisión: cobro en ventanilla ¿debe mandar mail? (observación ronda 6)
- [ ] `models.TIPOS_NOTIFICACION` desactualizado — corregir en la próxima migración de Alembic

---

## Estado de la ronda 8 (correcciones Claude Code, 2026-09-11)

Responde a lo que encontró la corrida manual de la ronda 7. Todo commiteado y
**pendiente de push/deploy**. Nada de esto pasó todavía por el navegador salvo lo que
se indica como verificado en ejecución.

### Resumen

| # | Qué era | Estado |
|---|---------|--------|
| BUG-19 | "CORS" al inhabilitar un turno | 🔧 **causa raíz encontrada y corregida** — no era CORS: `id_producto` NOT NULL. **Reproducido y verificado contra el schema real.** |
| BUG-20 | Suspensión sin aviso al socio | 🔧 corregido — orden cancelada, notificación in-app, aviso de devolución al admin y en el mail |
| Mejora-01 | Turno del carrito no se libera al salir | 🔧 corregido — el TTL de 20 min ahora corre cada 15 min, no cada 4 hs |
| Mejora-02 | "Nueva reserva" no correspondía a turnos fijos | 🔧 implementada — se asigna desde la celda del turno |
| BUG-16/17 | Escáner: mes de ingreso y menor | ✅ **re-verificado: los 4 asserts pasan** |
| BUG-18 | Cambiar comprobante en `/mis-compras` | ✅ re-verificado en código |

### BUG-19 — el hallazgo que más cambia el diagnóstico

El reporte de la ronda 7 apuntaba a tres sospechosos (CORS de Render, `include_router`,
decorator de auth). **Los tres estaban bien**, y se verificaron uno por uno:

- `main.py` ya tiene `https://clubatleticoroberts.com` y `https://www.clubatleticoroberts.com`
  en `origins`.
- El `openapi()` de la app lista `POST /admin/reservas/bloqueo` y
  `DELETE /admin/reservas/bloqueo/{id_reserva}`: el router se incluye bien.
- Las dos rutas tienen `Depends(require_roles(...))`.

Lo que fallaba era un **INSERT contra una columna NOT NULL** (`id_producto`). El 500 de un
`IntegrityError` no manejado lo genera el `ServerErrorMiddleware`, que está **por fuera** del
`CORSMiddleware`, así que la respuesta sale sin `Access-Control-Allow-Origin` y el navegador
reporta un error de CORS que no existe.

**Vale la pena recordarlo para próximas rondas: en esta app, "CORS" en la consola sobre un
endpoint que otras veces anduvo casi siempre significa 500 en el backend.** Los logs de Render
tienen el traceback real.

El `401` de `/auth/login` que aparecía en la misma consola era una línea vieja de un intento de
login anterior, sin relación con el bloqueo.

### Verificado en ejecución

- **BUG-19, causa y fix:** contra el schema real, insertar la franja sin `id_producto` da
  `null value in column "id_producto" ... violates not-null constraint`; con `id_producto`
  entra bien. Corrido dentro de una transacción con `rollback`, sin dejar filas.
- **BUG-16/17:** los 4 asserts del bloque `ronda 5 · BUG-16/17` de `qa_seguridad.py` **pasan**.
  Ese bloque no toca base ni servidor (arma el socio con `SimpleNamespace` y llama directo a
  `_construir_respuesta_desde_orm`), así que se pudo correr sin levantar Postgres ni uvicorn.
  El cartel da `SOCIO NUEVO ✓` para el socio en su mes de ingreso y `SOCIO HABILITADO ✓` para
  el que ya pagó; el menor viaja con `es_menor=True` y su edad, y sin fecha de nacimiento no se
  asume nada.
- **El build del frontend pasa** (`npm run build`, 1890 módulos).
- **La app importa y registra todas las rutas** con los schemas nuevos.
- El template `reserva_suspendida.html` renderiza bien en los tres casos
  (transferencia / efectivo / sin dato).

### Qué NO se pudo verificar

- Nada por el navegador: no hay forma de operar la UI desde acá.
- La suite completa de `qa_seguridad.py` necesita Postgres local y **Docker no está levantado**
  en esta máquina. Solo se corrió el bloque de BUG-16/17, que no depende de la base.

### Hallazgo nuevo de esta ronda — falta el producto `Cancha 2`

Consultando el catálogo real, los productos de alquiler activos son:

| id | nombre | precio |
|----|--------|--------|
| 2 | `Quincho — Turno Día` | $15.000 |
| 3 | `Quincho — Turno Noche` | $20.000 |
| 7 | `Cancha 1` | $25.000 |

**No existe `Cancha 2`.** No es un bug de código: es un dato que falta en `/admin/productos`.
Consecuencias, para no perder tiempo diagnosticándolo en la ronda 8 manual:

- el socio **no puede reservar Cancha 2** desde `/socio/cancha` (la pantalla ya avisa
  "No hay un precio configurado para «Cancha 2»");
- el admin **no puede asignar** un turno de Cancha 2 (el modal dice "Falta «Cancha 2» en el
  catálogo");
- **inhabilitar** un turno de Cancha 2 sí funciona (un bloqueo no necesita precio).

👉 **Dar de alta `Cancha 2` en `/admin/productos` antes de probar §5 canchas**, con el nombre
exacto (es el que matchean el frontend y el backend).

### Archivos tocados

| Archivo | Qué |
|---------|-----|
| `backend/utils/reservas.py` 🆕 | Resuelve el producto de alquiler de un turno. Misma regla de nombres que el frontend. |
| `backend/routers/admin_reservas.py` | BUG-19 (`id_producto` en los dos INSERT), BUG-20 (orden cancelada + notificación + `metodo_pago`), Mejora-02 (producto y nombre derivados, `metodo_pago`, notificación, `DetalleOrden.id_reserva`) |
| `backend/schemas.py` | `metodo_pago` en `ReservaAdminListResponse` y en `SuspenderReservaResponse`; `CrearReservaManualPayload` con `metodo_pago` y con `nombre_responsable`/`id_producto` opcionales |
| `backend/scheduler.py` | Mejora-01: el TTL de pre-reservas también corre en el job de 15 min |
| `backend/mailer/...` | El mail de suspensión suma el bloque de devolución según el método, y usa el nombre lindo de la instalación |
| `frontend/src/utils/reservas.js` | `nombreProductoDeTurno()`, `productoDeTurno()`, `labelMetodoPago()` |
| `frontend/src/pages/AdminReservas.jsx` | `ModalAccionesTurno` y `ModalAsignarTurno` (reemplazan a `ModalNuevaReserva`), método de pago en el detalle, avisos de devolución |
| `frontend/src/pages/SocioCompras.jsx` | "Cancelada por el club" en ámbar en vez de "Rechazada" en rojo |

### Lo que NO se tocó, a propósito

Según el pedido de esta ronda:

- §5 canchas y compras mixtas — sin probar todavía, no se agregó complejidad.
- `ReservaCalendar.jsx` — sigue ahí; decidir si se borra en un commit aparte.
- `models.TIPOS_NOTIFICACION` — no hizo falta: `reserva_cancelada` y `reserva_confirmada` ya
  estaban en la tupla **y en el CHECK de la base** (migración `a7b8c9d0e1f2`). No hay migración
  nueva en esta ronda.
- El 40% hardcodeado en `AdminSocios.jsx`.
- Cobro en ventanilla sin mail — sigue sin mandar mail; la decisión sigue abierta. Lo que sí
  cambió es que ahora **manda notificación in-app**, que era el agujero más grande (el socio no
  se enteraba de nada).

### Qué sigue para la ronda 8 manual

- [ ] Deployar (backend a Render por push a `main`; frontend por GitHub Actions).
- [ ] **Dar de alta el producto `Cancha 2`** antes de tocar §5 canchas.
- [ ] BUG-19: casos 19-A…D.
- [ ] BUG-20: casos 20-A…D (probar sobre una reserva pagada por **transferencia**, que es el
      caso que dispara todos los avisos nuevos).
- [ ] Mejora-02: casos M2-A…D.
- [ ] Mejora-01: caso M1-A (necesita esperar ~35 min con el servidor despierto).
- [ ] Lo que sigue pendiente de rondas anteriores: retestear en navegador BUG-01, BUG-04,
      BUG-07, BUG-11, BUG-13, BUG-15, BUG-16, BUG-17, BUG-18.
- [ ] §5 reservas de canchas · §5 compras mixtas · §3 morosos (flujo pago → aprobación → QR).

---

## Rediseño visual — aplicación del manual de marca (2026-09-12)

Trabajo **solo visual** sobre el frontend: colores, tipografías, espaciados, jerarquía y
consistencia de identidad. Cero cambios de lógica, rutas, handlers, efectos, llamadas a la
API o backend. La verificación de eso está más abajo, en "Cómo se acotó el alcance".

Fuente: Manual de Marca Oficial v1.0 del club (docs 01 a 06).

### Decisión de diseño de fondo: cómo se cerró la paleta

El manual define una **norma cromática cerrada** de 4 tintas: Azul Roberts `#183F7C`,
Azul Camotí `#26348C`, Azul Francia `#1C1F2D` y Blanco `#FFFFFF`. La app, en cambio, estaba
escrita con las escalas por defecto de Tailwind (`blue`, `indigo`, `slate`, `teal`, `emerald`,
`purple`, `violet`, `rose`…) repartidas en ~22.600 líneas de JSX, sobre 55 archivos.

En vez de hacer un find-replace de colores archivo por archivo —que es exactamente donde se
rompen cosas— **se redefinieron las escalas de Tailwind en `tailwind.config.js`** para que
apunten a las tintas oficiales:

| Escala de Tailwind | Pasa a ser |
|---|---|
| `blue`, `sky` | Azul Roberts `#183F7C` |
| `indigo`, `violet`, `purple`, `fuchsia`, `cyan` | Azul Camotí `#26348C` |
| `teal` | Azul Francia `#1C1F2D` |
| `gray`, `slate`, `zinc`, `neutral`, `stone` | un único neutro frío (termina en Azul Francia) |
| `green`, `emerald`, `lime` | verde de estado, atenuado |
| `amber`, `yellow`, `orange` | ámbar de estado |
| `red`, `rose`, `pink` | rojo de estado |

Consecuencia buscada: `bg-blue-600 hover:bg-blue-700` —el patrón de botón primario que ya
usaba toda la app, en ~50 lugares— pasa a ser exactamente lo que pide el manual (fondo Azul
Roberts, hover Azul Camotí) **sin tocar una sola línea de JSX**. Por eso la escala de `blue`
tiene un punto no monótono a propósito: `blue-700` es Azul Camotí, no un tono más oscuro
del 600. Está comentado en el config.

Después de eso sí se hizo una pasada mecánica que renombró las clases residuales para que el
nombre diga la verdad (`bg-indigo-600` → `bg-camoti-600`, `text-teal-700` → `text-francia-700`,
`bg-slate-900` → `bg-gray-900`, etc.): **404 clases en 34 archivos**. Como las rampas de origen
y destino son el mismo objeto en el config, ese rename no cambia un solo pixel; solo hace el
código legible. Los alias viejos siguen definidos en el config como red de seguridad.

Barrido final: **cero clases fuera de la paleta cerrada** en `src/`.

### Verde: por qué sobrevivió

Hubo un conflicto real entre el brief de la sesión y el manual:

- El brief pedía "al día / activo / verificado → fondo derivado de `#183F7C`".
- El mockup del doc 06 (carnet de socio) muestra "● CUOTA AL DÍA" como **pastilla verde**.

Se resolvió con Merlina: **gana el verde, pero atenuado** (`#2F6B4F` en vez del verde
saturado de Tailwind). Motivo: en las tablas de admin hay que distinguir aprobada /
pendiente / rechazada de un vistazo, y si "aprobada" fuera azul chocaría con el azul de
acción primaria. El azul queda reservado para marca y acciones; verde/ámbar/rojo comunican
estado y nada más.

Corolario aplicado en varios lados: **los colores de categoría dejaron de robarle tintas a
los estados**. Antes "indumentaria" era naranja y "partido" era verde; ahora las categorías
usan los tres azules del manual.

### Tipografía

- Se importan **Playfair Display** (500/600/700/800 + itálica) e **Inter** (400/500/600/700)
  desde Google Fonts, con `preconnect`, en `index.html`.
- Inter queda como fuente de `body` (toda la UI, tablas, botones, datos).
- Playfair se aplica a **todos los `<h1>`** por regla base en `index.css`, y a los `<h2>`/`<h3>`
  cuyo tamaño **base** sea `text-lg` o mayor — **45 títulos de sección**, vía la clase
  `font-display`.
  · Por qué no a todos los `<h2>`: esta app usa muchos `<h2 className="text-xs uppercase
  tracking-wider">` como etiqueta de sección, y una serif de alto contraste a 12px se rompe.
  Esos quedaron en Inter, que es lo que pide el manual para elementos de interfaz.
- **La itálica quedó bloqueada** salvo en la clase `.matchday` de `index.css`, que es el único
  lugar habilitado (anuncios de partido y citas solemnes, regla estricta del manual). Hoy
  todavía no hay ninguna pantalla usándola.
- Números tabulares (`font-variant-numeric: tabular-nums`) en `th`/`td` y en los contadores,
  para que no bailen las columnas al refrescar.

### Reglas del escudo y de El Camotí que estaban incumplidas

Tres infracciones concretas al manual que se corrigieron:

1. **El escudo iba estampado directo sobre la foto del Hero** (doc 05: "NUNCA estampar el
   escudo sobre la fotografía… va siempre en marco o zócalo blanco perimetral"). Ahora va
   sobre pastilla blanca con margen.
2. **El escudo iba suelto sobre el fondo oscuro del header** (doc 02: "en soportes digitales
   con fondos oscuros, aplicar sobre pastilla perimetral blanca con margen 1X"). Igual.
3. **El escudo llevaba `drop-shadow-xl`** y El Camotí `drop-shadow-md` (doc 01: "Prohibido
   sombras o degradé"). Sombras eliminadas de ambos.

Además: El Camotí del footer estaba en Azul Camotí sobre el fondo Azul Francia, donde
prácticamente desaparecía. Va sobre pastilla blanca, que es lo que corresponde — las dos
tintas de El Camotí están definidas para fondos claros.

### Componentes modificados

**Tokens y configuración**

| Archivo | Qué se hizo |
|---|---|
| `frontend/src/styles/variables.css` | **Nuevo.** Las 4 tintas, las 2 fuentes, los neutros, los 3 estados y la forma (radio de tarjeta, radio de control) como variables CSS. Es la fuente de verdad legible. |
| `frontend/tailwind.config.js` | Rampas de marca (`roberts`, `camoti`, `francia`), remapeo de las escalas de Tailwind, `fontFamily` (`sans` = Inter, `display`/`serif` = Playfair), y una sola familia de sombras tintada con Azul Francia en vez de negro puro. |
| `frontend/index.css` → `src/index.css` | Base tipográfica, `.form-input` actualizado, y clases nuevas `.btn-primario` / `.btn-secundario` / `.card-car` / `.badge-car` / `.matchday` para código nuevo. Scrollbar y `::selection` a la paleta. |
| `frontend/index.html` | Import de Google Fonts + `<meta name="theme-color" content="#183F7C">`. |

**Shell y navegación**

| Archivo | Qué se hizo |
|---|---|
| `src/layouts/MainLayout.jsx` | Header pasa de `slate-900` genérico a **Azul Roberts** (es lo que muestra el mockup del doc 06). Drawer se queda en **Azul Francia**, que es la tinta que el manual reserva a fondos oscuros. Escudo sobre pastilla blanca y sin sombra. Botones del header a `white/10` con anillo de foco. Fondo de app a neutro 50. |

**Landing pública**

| Archivo | Qué se hizo |
|---|---|
| `src/components/landing/Hero.jsx` | Escudo sobre zócalo blanco (ver arriba). Titular a Playfair. Overlay de la foto pasa de negro plano a Azul Francia al 70%, acorde al criterio fotográfico del manual. CTAs de píldora a `rounded-xl`, con el secundario en outline blanco → invierte a azul en hover. |
| `src/components/landing/Historia.jsx` | Título a Playfair SemiBold. Tarjetas a `rounded-2xl` + sombra suave. Botón de píldora `border-2` a outline `rounded-xl`. |
| `src/components/landing/Galeria.jsx` | Ídem tarjetas y botón. |
| `src/components/landing/Beneficios.jsx` | Título a Playfair. Tarjeta de comercio a fondo blanco con borde sutil (antes gris sobre gris). Rubro en versalitas. |
| `src/components/landing/Sponsors.jsx` | Título a Playfair. **Cambio de criterio:** las tarjetas eran azul Roberts sólido y los logos de terceros (que vienen con sus propios colores y casi siempre están diseñados sobre blanco) quedaban peleando contra el fondo de marca. Ahora son tarjetas blancas con borde sutil que se tiñe de azul en hover. |
| `src/components/landing/Footer.jsx` | Ya estaba en `slate-900`, que ahora **es** Azul Francia — o sea que cumple el manual sin cambiar de clase. Nombre del club a Playfair. El Camotí sobre pastilla blanca. Links a mejor contraste. |

**Pantallas de acceso**

| Archivo | Qué se hizo |
|---|---|
| `src/pages/Login.jsx` · `Registro.jsx` · `RecuperarPassword.jsx` · `CambiarPasswordObligatorio.jsx` | Tratamiento común: fondo neutro, tarjeta con borde sutil + sombra suave, inputs con borde en reposo y **borde Azul Roberts en foco**, botón primario `rounded-xl` en Inter Bold con estado deshabilitado neutro (antes era gris azulado), y el mensaje de error en caja roja en vez de texto suelto. **Se agregó el escudo** en la cabecera de las tres pantallas públicas: no tenían ninguna marca del club. |
| | En Login y Registro, el botón "Solicitar reactivación" pasó de verde a azul: es una acción primaria, no un estado. |

**Carnet digital de socio** (`src/pages/SocioInicio.jsx`)

Es la sección con tratamiento propio en el manual (doc 06), y era la que más lejos estaba.
Se rehízo la tarjeta del QR como carnet:

- Cabecera blanca con **escudo + "Club Atlético Roberts" en Playfair + "Carnet oficial de socio"**.
- Franja de estado con tres variantes (habilitado / en verificación / con deuda) en verde,
  ámbar y rojo funcional — antes "en verificación" compartía el verde del estado OK.
- **Bloque de datos del socio nuevo**: nombre completo en **Playfair Bold**, y N.º de socio,
  DNI y vencimiento en **Inter tabular**, que es exactamente lo que especifica el doc 06.
  Estos cuatro datos ya venían en la respuesta de `/usuarios/me` que la pantalla **ya pedía**
  — no hay llamada nueva ni campo nuevo. Se agregaron tres formateadores de presentación
  puros (`formatearDNI`, `formatearNroSocio`, `formatearVencimiento`).
- **El Camotí como marca de agua** al 5,5% de opacidad, en el bloque de datos.
  Deliberadamente **no** detrás del QR: el escáner necesita contraste limpio.
- Color del QR: pasa de `#14532d` / `#991b1b` (verde y rojo viejos de Tailwind, hardcodeados)
  a **Azul Francia `#1C1F2D`** para el socio habilitado y rojo profundo para el moroso.

**Componentes compartidos**

| Archivo | Qué se hizo |
|---|---|
| `src/components/ConfirmDialog.jsx` | El botón de confirmar pasa de verde a **azul de marca**: confirmar es la acción primaria del diálogo; el verde queda para comunicar estado. La variante `peligro` sigue en rojo. |
| `src/components/admin/MetricCard.jsx` | Badge "Próximamente" a pastilla con anillo. Título de card a Inter Medium con más tracking. Número grande con cifras tabulares. |
| `src/components/admin/CategoriaOrdenBadge.jsx` | Las tres categorías conviven en la **misma tabla**, así que necesitaban tres tintas distinguibles: pasan a Roberts / Camotí / Francia (antes azul / violeta / verde azulado). Pastillas con anillo en vez de fondo saturado, en Inter Medium. |
| `src/components/ReservaCalendar.jsx` | `react-big-calendar` no toma clases de Tailwind, así que sus eventos van por estilo inline. Estaban hardcodeados en hex viejos y —lo importante— **"confirmada" se pintaba de rojo**, que en el resto de la app significa rechazo. Ahora confirmada = verde de estado, pendiente = ámbar. |

**Sistemas de color unificados entre pantallas**

Tres codificaciones que estaban definidas por separado en cada archivo, con colores distintos
entre sí, y que ahora comparten criterio:

| Qué | Dónde estaba duplicado | Cómo quedó |
|---|---|---|
| Categoría de producto/orden | `CategoriaOrdenBadge.jsx`, `AdminProductos.jsx`, `SocioShopping.jsx` | cuota = Roberts · alquiler = Camotí · indumentaria = Francia · otro = neutro |
| Tipo de evento deportivo | `JugadorEquipo.jsx`, `JugadorCalendario.jsx`, `TecnicoEventos.jsx` | partido = Roberts · entrenamiento = Francia · torneo = Camotí · institucional/otro = neutro (antes partido era verde y torneo violeta) |
| Avatares de plantel | `JugadorEquipo.jsx` | Eran 7 colores sueltos; dos colisionaban al cerrar la paleta y el rojo/ámbar se leían como error. Ahora son los 3 azules en 2 tintes cada uno. |

**Otras pantallas**

| Archivo | Qué se hizo |
|---|---|
| `src/pages/AdminInicio.jsx` | Las 4 cards de "Gestión operativa" son destinos de navegación equivalentes, no una leyenda: iban en 3 colores distintos (con dos repetidos). Ahora comparten un chip de marca, y el ámbar queda libre para las 2 cards que sí son "pendiente de revisión". |
| `src/pages/AdminEstadisticas.jsx` | Los gráficos son SVG dibujados a mano, sin Tailwind: los 10 colores hardcodeados (ámbar, azul, violeta, grises de Slate) pasan a la paleta. Series: Cuotas = Roberts, Compras = Camotí, Alquileres = Francia claro. |
| `src/pages/SocioPerfil.jsx` | **El medidor de fortaleza de contraseña estaba roto por la paleta cerrada**: `orange-400` y `yellow-400` caían en el mismo ámbar y los pasos "Débil" y "Regular" quedaban idénticos. Se abrió la escala dentro del ámbar (`amber-600` / `amber-400`). |
| `src/pages/SocioShopping.jsx` | Placeholder de producto sin foto: se saca el degradado (prohibido por el manual) y queda plano. |
| `src/pages/AdminScanner.jsx` | La línea de barrido del escáner pasa de verde a azul de marca. |
| `src/hooks/useExportarConvocatoria.js` | El PDF de convocatoria tenía su propio azul (`#0F1E50` / `#1E50A0`). Pasa a Azul Francia para el membrete y Azul Roberts para el encabezado de tabla — los PDF que salen del sistema ahora son de la misma marca que la web. |
| `src/pages/TecnicoEventos.jsx` | Filas seleccionables con estilo inline, a la paleta. |

**Normalización de forma (global, mecánica)**

- Un solo radio de tarjeta: los 2 `bg-white rounded-lg` sueltos pasaron a `rounded-2xl`
  (el resto de la app ya usaba 106 veces `rounded-2xl` — se alineó a la mayoría).
- Una sola escala de sombras, redefinida en el config y **tintada con Azul Francia** en vez
  de negro puro: se va el gris sucio de las sombras por defecto. Los 2 `shadow-2xl` sueltos
  bajaron a `shadow-xl`.
- Un solo borde sutil: **121 usos** de `border-gray-100` / `border-gray-50` (que sobre blanco
  eran prácticamente invisibles) pasaron a `border-gray-200`.
- Inputs: los que quedaban con `focus:border-blue-500` pasaron a **`blue-600` (Azul Roberts)**
  en foco, como pide el manual, y los `rounded-lg` a `rounded-xl`.

### Accesibilidad — una mejora parcial, y qué queda

El neutro `400` se oscureció respecto del gris original de Tailwind (`#9CA3AF` → `#8A92A1`).
Se usa **259 veces** para textos de ayuda y ahí no llegaba ni a 2,7:1 sobre blanco.

**Sigue sin cumplir AA**: queda en ~3,1:1. No se subió más para no aplastar el escalón contra
el neutro `500` (que sí cumple, 4,55:1) — son dos jerarquías distintas de texto secundario y
si se acercan demasiado se pierde la diferencia. Si en algún momento se quiere cumplir AA en
serio, el camino es subir esos textos de `text-gray-400` a `text-gray-500` pantalla por
pantalla, no seguir oscureciendo el token.

### Cómo se acotó el alcance

La regla de la sesión era: si hay que tocar un `.py`, un router, un hook, un contexto, un
endpoint, una query o un handler, no entra.

Verificación sobre el diff completo (52 archivos, +1024/−634): filtrando todo lo que es
`className` y comentarios, **las únicas líneas que cambiaron son**:

1. El valor por defecto de `bgClassName` en `InfiniteCarousel` (un string de clases CSS).
2. Las constantes de color de los SVG de `AdminEstadisticas`.
3. Un ternario que elige una clase de color en `AdminEstadisticas`.
4. Los tres formateadores de presentación del carnet.

Ni un `useState`, ni un `useEffect`, ni un `fetch`, ni un `onClick`, ni un `navigate`, ni una
ruta, ni un archivo de `backend/`. `npm run build` pasa limpio.

### Hallazgos al pasar (NO se tocaron — son bugs, no diseño)

Aparecieron leyendo el código. Quedan anotados acá para que se decidan aparte:

1. **`TecnicoPlanteles.jsx:600` — `setResultados` no está definido.** Se llama dentro del
   handler de asignar técnico, después de un POST exitoso. ESLint lo marca como `no-undef`:
   cuando se asigne un técnico y la llamada salga bien, eso tira `ReferenceError` en runtime.
   Es previo a esta sesión (está igual en `HEAD`).
2. **`ReservaCalendar.jsx` — `defaultView="week"s`.** Hay una `s` suelta después del atributo,
   que React interpreta como una prop booleana `s={true}` sobre `<Calendar>`. Inofensivo, pero
   es un typo.
3. `npx eslint src/` reporta 881 errores, de los cuales **858 son `react/prop-types`** — el
   proyecto nunca activó esa validación. Es ruido de base, no una regresión: conviene apagar
   la regla o asumirla, para que el lint sirva de algo.

### Lo que NO se hizo, a propósito

- **No se agregó copy nuevo.** El mockup del doc 06 propone para el Hero un bajada
  ("Portal oficial de socios, noticias, fixture…") y un eyebrow "PLATAFORMA OFICIAL CAR" que
  hoy la landing no tiene. Escribir texto para el club no es un rediseño visual; queda como
  sugerencia si lo querés.
- **No se tocaron los botones verdes de Aprobar / rojos de Rechazar** en las bandejas de
  admin. El brief pedía botón primario azul, pero ahí el par verde/rojo comunica el resultado
  de la acción y es una convención fuerte en pantallas de moderación. Si lo querés azul, es
  un cambio acotado y lo hacemos.
- **No se removieron los alias viejos del config** (`indigo`, `teal`, `emerald`…). Ya no los
  usa nadie en `src/`, pero quedan como red por si aparece una clase construida dinámicamente
  que no vi.
- No se tocó el backend, ni los templates de mail. Los mails siguen con su estilo propio: si
  se quieren alinear al manual, es otra sesión (y toca archivos de `backend/mailer/`).

### Qué conviene mirar en el navegador

El build pasa, pero esto es un rediseño: hay que verlo. Lo más sensible, en orden:

- [ ] **Landing** — escudo sobre el zócalo blanco del Hero (que no quede ni muy chico ni muy
      grande en mobile), y las tarjetas de sponsors ahora blancas.
- [ ] **Carnet del socio** (`/socio`) — es lo más rehecho. Verificar que **el QR siga
      escaneando bien** desde la puerta con el color nuevo, y que la marca de agua del Camotí
      no moleste.
- [ ] **Header en mobile** — el escudo ahora va dentro de una pastilla blanca; confirmar que
      no empuje la altura del header ni tape los íconos de campana/carrito.
- [ ] **Tablas de admin** (`/admin/verificaciones`, `/admin/socios`) — que aprobada / pendiente
      / rechazada se sigan distinguiendo bien con el verde atenuado.
- [ ] **Agenda de reservas** (`/admin/reservas`) — es la pantalla con más estados de color.
- [ ] **Medidor de contraseña** en `/perfil` — que los 5 pasos se vean distintos.
- [ ] Textos chicos en gris (`text-gray-400`) en general: ahora son un poco más oscuros.

---

## Rediseño landing — 2026-09-12

Segunda pasada sobre la landing pública, después del rediseño de marca del 11-09.
Alcance acotado a propósito: **solo componentes de `frontend/src/components/landing/`**.
No se tocó ningún `.py`, router, hook de negocio, contexto ni endpoint. La lógica de
fetch de Beneficios y Sponsors quedó idéntica (mismas URLs, mismo manejo de error,
mismo "si no hay datos, no renderiza la sección").

### Los dos bugs reportados

**BUG-21 — El escudo del Hero se veía como una pastilla blanca vacía.** ✅ Resuelto.

No era la ruta ni el `<img>`: el archivo cargaba perfecto. El problema es *cuál*
archivo. `Hero.jsx` apuntaba a `/escudo-car-1.png` (en `public/`), que es la versión
**monocroma blanca** del escudo — la que el manual reserva para fondos oscuros. Sobre
la pastilla blanca que el propio manual exige (doc 05: el escudo nunca va estampado
sobre la foto) quedaba blanco sobre blanco: se veía la pastilla y nada adentro.

Ahora usa `src/assets/escudo-car.PNG`, que es el escudo institucional a color.
Además pasó de ruta suelta en `public/` a `import`, así Vite lo versiona con hash
y no queda un escudo viejo cacheado en CloudFront. Verificado en el build: sale como
`dist/assets/escudo-car-BIaQnm-J.PNG`.

⚠️ `public/escudo-car-1.png` **sigue en el repo y no se borró**: es el escudo blanco
legítimo y puede hacer falta para un soporte oscuro. Si se vuelve a usar, que sea
sobre fondo oscuro, nunca sobre zócalo blanco.

**BUG-22 — Imágenes de Beneficios y Sponsors desaturadas.** ✅ Resuelto.

No había ningún filtro CSS, `opacity` ni `mix-blend-mode` — busqué los tres y no
aparecen en la landing. La causa era `InfiniteCarousel.jsx`: dibuja dos veladuras en
degradé (`w-12 sm:w-24`, del color de fondo de la sección a transparente) sobre los
bordes del riel, para tapar el corte donde el loop reengancha. Como el riel
auto-scrollea, las tarjetas pasan permanentemente por debajo de esas veladuras: en
desktop se comían ~96 px de cada tarjeta de los extremos, y en mobile (390 px) las dos
veladuras juntas tapaban ~1/4 del ancho visible. El efecto es exactamente "lavado".

Dos correcciones:

1. Beneficios y Sponsors **dejaron de usar el carrusel**. No lo necesitaban: son
   listados cortos que se leen mejor quietos. Beneficios es un riel con `scroll-snap`
   en mobile (el dedo manda, sin auto-scroll) y grilla de 2/3 columnas de `sm` para
   arriba. Sponsors es una grilla de 2/3/4 columnas sobre blanco puro. Sin veladuras,
   sin degradés y sin filtros: los logos y las fotos van a contraste pleno.
2. Las veladuras de `InfiniteCarousel` se achicaron de `w-12 sm:w-24` a `w-3 sm:w-8`.
   Alcanza para disimular el corte del loop y ya no desatura el contenido. Esto también
   arregla de paso las fotos de **Nuestra Historia**, que tenían el mismo lavado en los
   extremos (no estaba reportado, pero es el mismo bug).

### Cambios visuales por sección

- **Hero** — foto pasada a `<img>` + `object-position` para encuadrar a los jugadores.
  Overlay en dos capas tintadas con Azul Francia (antes era `bg-gray-950/70`, un plano
  que ahogaba el humo azul de tribuna) que carga arriba y abajo y se abre en el medio.
  Titular en Playfair Bold más grande, eyebrow y bajada nuevos en Inter. Botones más
  altos (mejor toque en mobile) y con `focus-visible` visible sobre fondo oscuro.
  Degradé de cierre al blanco para que el corte con "Nuestra Historia" no sea abrupto.
- **Historia** — eyebrow + filete, cuerpo más aireado y con más interlínea, fotos más
  grandes (verticales, no cuadradas). Se sacó el `hover:opacity-90` de las fotos.
- **Beneficios** — tarjetas blancas con borde sutil y sombra suave. Jerarquía del brief:
  rubro en versalitas chicas, nombre en Inter Bold, beneficio destacado en Azul Roberts.
  Fondo de sección `gray-50` para dar ritmo entre Historia (blanco) y Sponsors (blanco).
- **Sponsors** — grilla sobre **blanco puro** (varios logos vienen en PNG con
  transparencia y sobre gris se ensucian). Se sacaron las cajas con borde que competían
  con las marcas: quedó solo aire y un hover muy suave.
- **Footer** — fondo `francia-900`, que es `#1C1F2D` exacto. Pasó de bloque centrado a
  tres columnas (identidad / contacto / redes) con barra inferior. El Camotí se quedó
  sobre pastilla blanca a propósito: sus dos tintas oficiales están definidas para
  fondos claros y sobre el Azul Francia desaparecería. El escudo completo **no** va en
  el pie (va en cabecera), como pide el brief.

### Archivos modificados

```
frontend/src/components/landing/Hero.jsx              (escudo + overlay + tipografía)
frontend/src/components/landing/Historia.jsx          (aire y jerarquía)
frontend/src/components/landing/Beneficios.jsx        (reescrito: grilla + snap scroll)
frontend/src/components/landing/Sponsors.jsx          (reescrito: grilla sobre blanco)
frontend/src/components/landing/Footer.jsx            (columnas + Azul Francia)
frontend/src/components/landing/InfiniteCarousel.jsx  (veladuras de borde más finas)
```

`Landing.jsx` no se tocó. Sigue renderizando `<Calendario />`, que hoy es un componente
vacío (`export default function Calendario() {}`) y no pinta nada — es así desde antes,
no lo toqué para no cambiar comportamiento, pero conviene sacarlo o completarlo.

### Verificación hecha

`npm run build` pasa limpio (el aviso de chunk > 500 kB es previo y no tiene que ver con
esto). Además se levantó el build con `vite preview` contra un backend mock con 5
beneficios y 6 sponsors de prueba, y se revisó con Chrome headless a **390 px** y
**1440 px**: escudo visible sobre el zócalo, imágenes a contraste pleno en las dos
secciones, riel de beneficios sin tarjetas cortadas en mobile, footer legible.

Ojo con un detalle de la herramienta, por si alguien repite la prueba: Chrome headless
en Windows no baja de ~500 px de viewport, así que para ver 390 px reales hay que
renderizar la página dentro de un `<iframe>` de 390 px. Un screenshot con
`--window-size=390` engaña: muestra un layout de 500 px recortado y parece que hay
overflow horizontal cuando no lo hay.

### Pendiente / a mirar en el navegador

- [ ] **Landing en un celular real** — el `100svh` del Hero y el riel de Beneficios son
      lo más sensible al navegador móvil (barra de direcciones de Safari iOS).
- [ ] **Sponsors con logos reales** — la grilla se probó con logos mock. Si alguno viene
      muy apaisado o muy alto, revisar que `h-20 sm:h-24` + `object-contain` le quede bien.
- [ ] **Beneficios con muchos comercios** — se probó con 5. Con 15+ la grilla de desktop
      se va a hacer larga; puede querer un "ver todos" o un límite.
- [ ] La foto del Hero es de 720×900: en desktop 1440 se ve blanda. Si hay un original
      más grande, reemplazarlo (es solo cambiar `src/assets/hero-bg.PNG`).

### BUG-23 — Logos de sponsors invisibles (2026-09-12, mismo día)

Reportado como "sigue el lavado de imágenes después del rediseño". **No era CSS.**

Auditado `Sponsors.jsx` punto por punto: no tiene `opacity`, ni `grayscale`, ni
`brightness`, ni `contrast`, ni `saturate`, ni `mix-blend-mode`, ni `filter` inline,
ni usa ya `InfiniteCarousel`, y la sección y las tarjetas eran `bg-white`. Todo limpio.

La causa está en los archivos que subió el club. Bajé los 5 logos del bucket público
y los medí pixel a pixel:

| id | sponsor | lienzo | bbox del arte | % alto | píxeles casi blancos |
|----|---------|--------|----------------|--------|----------------------|
| 3 | Liga Padres de Familia | 1080×1350 | 555×278 | 20.6% | 100% |
| 4 | Nutridel ("Vaca") | 1080×1350 | 794×242 | 17.9% | 100% |
| 5 | Amacar | 1080×1350 | 447×254 | 18.8% | 100% |
| 6 | TMT | 1080×1350 | 444×398 | 29.5% | 100% |
| 2 | La Paz | 1080×1350 | 438×317 | 23.5% | 100% |

Dos problemas de origen, no de código:

1. **Los 5 son arte blanco sobre fondo transparente.** El 100% de los píxeles con alfa
   tiene luminancia > 235. Sobre una tarjeta blanca no se ven lavados: no se ven. Es
   exactamente el mismo caso que BUG-21 (el escudo blanco del Hero), pero en assets
   subidos desde el admin.
2. **Son exports de Instagram, no archivos de logo.** Lienzo 4:5 con el arte chiquito y
   centrado: ocupa entre 18% y 30% del alto. Con `object-contain` lo que entraba en la
   caja era, sobre todo, margen transparente, y el logo quedaba diminuto.

**Solución aplicada** (todo dentro de `Sponsors.jsx`, no se tocó nada más):

- Las placas pasaron a **Azul Francia (`bg-francia-900` = `#1C1F2D`)**, tinta oficial del
  manual, así que la sección sigue dentro de la paleta cerrada. El fondo de la *sección*
  sigue siendo blanco puro: lo oscuro es la placa de cada logo.
  ⚠️ Esto contradice el pedido de "card blanca y nada de grises", pero con estos archivos
  una card blanca garantiza que el logo siga invisible. Es la única forma de que se lean
  hoy.
- La imagen se ajusta **por ancho** dentro de una placa `aspect-[2/1]` con `overflow-hidden`
  y centrado absoluto: el sobrante vertical se recorta y, como el arte está centrado en el
  lienzo, lo único que se corta es transparencia. Sin `object-cover` (deformaría), sin
  filtros, sin veladuras.
- Grilla estática 2 / 3 / 4 columnas (mobile / `md` / `lg`), borde sutil y `p-6` efectivo.

Verificado con Chrome headless a 390 px y 1440 px **contra los logos reales de producción**
(no mocks): los 5 se leen nítidos — lpf, Nutridel, Amacar, tmt-wifi, La Paz.

**Pendiente para el club (es lo que arregla esto de verdad):**

- [ ] Pedir a cada sponsor el logo **a color o en versión oscura**, recortado al arte
      (sin el lienzo 4:5 de Instagram), en PNG con transparencia o SVG.
- [ ] Cuando estén, volver la placa a fondo blanco: en `Sponsors.jsx` cambiar
      `bg-francia-900 border-white/10` por `bg-white border-gray-100`. Es esa línea sola;
      quedó comentado en el archivo.
- [ ] Evaluar una validación al subir logo desde `/admin/sponsors` que avise si la imagen
      es casi toda blanca (mismo chequeo de luminancia). Evita que esto vuelva a pasar.
