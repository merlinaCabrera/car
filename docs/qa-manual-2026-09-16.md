# QA Manual — Club Atlético Roberts

> **Inicio: 2026-09-16** | Continúa desde `docs/qa-manual-2026-09-08.md` (cerrado)
> **Entorno:** producción — https://www.clubatleticoroberts.com + API `club-atletico-api.onrender.com` + Neon
> **Tester:** Merlina

Este documento **no replica el historial del anterior**. Arranca con el estado de lo que
migra y con lo que hay que probar. Las secciones que ya funcionaban bien en la corrida
anterior se omiten a propósito: retestearlas cuesta tiempo y no mueve la aguja.

**Para el diagnóstico de cualquier bug viejo, leer la ficha en `qa-manual-2026-09-08.md`**
(sección "Detalle de hallazgos", BUG-01 a BUG-27). Acá se los referencia por número; la
causa raíz está allá, y varias costaron dos o tres rondas de encontrar.

---

## Antes de empezar — cuatro cosas que bloquean la corrida

| # | Qué | Por qué bloquea |
|---|---|---|
| 1 | **`alembic upgrade head`** contra Neon | ✅ **Listo y verificado.** La base ya está en la última revisión (`bc78e9102a34`), incluyendo `whatsapp_club`, el fixture, y las tablas y columnas de streaming / entradas para invitados. No hay que correr ninguna migración. |
| 2 | **Confirmar `FRONTEND_URL` en Render** = `https://www.clubatleticoroberts.com` | Si falta, `{link_pago}` sale como `http://localhost:5173/socio/cuotas` en **cada** WhatsApp y **cada** mail del aviso masivo. Es BUG-07, pendiente desde la ronda 3. **Verificar antes de M5**, que le escribe a morosos reales. |
| 3 | **Dar de alta el producto `Cancha 2`** | ✅ **Listo y verificado.** Creado en Neon (ID 8: 'Cancha 2', precio $25.000, activo). Bloqueo despejado. |

⚠️ **El aviso masivo (M5) le escribe a los morosos reales del club.** No hay modo de
prueba y el envío no se puede cancelar a mitad de camino. Probar primero el botón de
WhatsApp (que no manda nada: solo abre el chat) y recién después el mail.

---

## Estado inicial — lo que migra del documento anterior

Todo lo de abajo está **corregido o implementado en código y sin confirmar por una
persona usando la app**. Esa es la deuda real de este ciclo.

| # | Área | Estado al migrar | Sección |
|---|------|-----------------|---------|
| Mejora-06 | Unificación `/admin/comercios` → `/admin/productos` | 🆕 Nuevo (Gemini) — sin verificar | §A.1 |
| Mejora-07 | Navbar flotante + píldora "Administrador ▾" | 🆕 Nuevo (Gemini) — sin verificar | §A.2 |
| Mejora-08 | Recorrido mobile (5 pantallas) | 🆕 Nuevo (Gemini) — sin verificar | §A.3 |
| Mejora-04 | WhatsApp deep link en morosos (texto ajustado el 16-09) | 🆕 Nuevo — sin verificar | §B.2 |
| Mejora-05 | Mail masivo a morosos | 🆕 Nuevo — sin verificar | §B.3 |
| — | Campo "Número de WhatsApp del club" | 🆕 Nuevo — sin verificar | §B.1 |
| — | Escáner offline (caché del padrón + banner + sesión sin señal) | 🆕 Nuevo — sin verificar | §C |
| — | Streaming PPV: Fixture + Player Blindado (YouTube) + Concurrencia=1 | 🆕 Nuevo (Gemini, 16-09) — sin verificar | §I.1, §I.6 |
| — | Entradas virtuales para invitados sin cuenta (Magic Link + persistencia) | 🆕 Nuevo (Gemini, 16-09) — sin verificar | §I.3, §I.6 |
| — | Panel de accesos en eventos (`[🎟️ Accesos]`): emisión no-socios/morosos y WhatsApp | 🆕 Nuevo (Gemini, 16-09) — sin verificar | §I.2, §I.3, §I.4 |
| — | Aprobación de pagos por transferencia para streaming en 1 clic | 🆕 Nuevo (Gemini, 16-09) — sin verificar | §I.5 |
| — | Asistente Virtual IA ("CAMOTE" / Chatbot Gemini Flash + In-App reset + seguridad por rol) | 🆕 Nuevo (Gemini, 16-09) — sin verificar | §J |
| BUG-19 | Inhabilitar turno en `/admin/reservas` (falso CORS/401) | 🔧 Corregido en código | §D |
| BUG-20 | Admin suspende reserva pagada sin avisar al socio | 🔧 Corregido en código | §D |
| BUG-27 | Modal "Inhabilitar turno" manda a un botón que no existe | 🔧 Corregido (ronda 12) | §D |
| Mejora-02 | "Asignar a socio" / "Inhabilitar turno" | 🔧 Corregido en código | §D |
| BUG-16 | Escáner no distingue "mes de ingreso" de "al día" | 🔧 Corregido en código | §E |
| BUG-17 | Escáner no indica si el socio es menor de edad | 🔧 Corregido en código | §E |
| Mejora-01 | TTL de pre-reservas (20 min, job de 15 min) | 🔧 Corregido | §F.1 |
| BUG-18 | Reemplazar comprobante de reserva en `/mis-compras` | 🔧 Corregido en código | §F.4 |
| BUG-24 | Carrito sucio al reactivar socio | 🔧 Corregido en código | §F.5 |
| BUG-25 | Falta filtro "Admin general" en `/admin/socios` | 🔧 Corregido en código | §F.5 |
| Mejora-03 | Auto-baja: aviso in-app en vez de `alert()` | 🔧 Corregido en código | §F.5 |
| BUG-01, 04, 05, 07, 09, 11, 13, 15 | La cola vieja de las rondas 3 y 4 | 🔧 Corregido en código | §F.6 |
| BUG-26 | `TIPOS_NOTIFICACION` atrás del `CHECK` real | 🔧 Corregido (ronda 12) | §G |

---

## Credenciales de prueba

| DNI | Contraseña | Mail | Rol / Estado |
|-----|-----------|------|-------------|
| 42542525 | wewewewe | — | Admin general (Merlina Cabrera) |
| 14000113 | wewewewe | tapia@afa.com.ar | Personal administrativo (Claudio Tapia) |
| 77000009 | wewewewe1 | portero@caroberts.com | Admin temporal / Portero escáner puro (Portero Escaner) |
| 77000001 | wewewewe3 | tmtwifi0@gmail.com | Socio puro — al día (pagado hasta abr 2027) |
| 77000002 | wewewewe1 | merlina.cabrera@alu.ing.unlp.edu.ar | Socio — menor (nac. 2015) |
| 77000005 | wewewewe1 | random5@gmail.com | Socio — al día |
| 77000006 | wewewewe1 | random6@gmail.com | Socio — al día, reservas de quincho |
| 42542572 | wewewewe | cmerlina402@gmail.com | Socio puro — moroso 1 mes (Travis Kelce) |
| 37889456 | wewewewe | — | Socio puro — moroso varios meses (Valentina Fernandez) |
| 36778899 | wewewewe | — | Socio puro — moroso 15 meses (Facundo Méndez) |

✅ **Usuario `admin_temporal` (portero) creado y verificado:** DNI `77000009` / `wewewewe1`. Cuentas morosas y socios al día sanitizadas (roles espurios de staff removidos). Listo para probar §C, §E y guards de RBAC.

---

## §A — UX nueva (Gemini, 16-09)

Los tres frentes están **verificados en código** (ver "Estado de la ronda 13" del
documento anterior). Lo que sigue es lo que hay que mirar con los ojos.

### A.1 Catálogo unificado — `/admin/productos` (Mejora-06)

| Caso | Ítem | Resultado |
|------|------|-----------|
| A1-A | `/admin/comercios` redirige a `/admin/productos` (y el botón "atrás" no queda en loop — es `<Navigate replace>`) | — |
| A1-B | La pantalla muestra los 4 desplegables: Productos, Comercios, Sponsors, FAQ | — |
| A1-C | Abrir/cerrar cada desplegable funciona y no pisa el estado de los otros | — |
| A1-D | Comercios: alta, edición y baja siguen funcionando desde el bloque nuevo | — |
| A1-E | El panel "Recordatorio de cuota" sigue arriba y funcional (sobrevivió a la reescritura) | — |
| A1-F | Ningún ítem del menú apunta todavía a `/admin/comercios` | — |

### A.2 Navbar flotante y píldora Administrador (Mejora-07)

| Caso | Ítem | Resultado |
|------|------|-----------|
| A2-A | Navbar con bordes redondeados, `backdrop-blur` y sombra flotante | — |
| A2-B | La píldora "Administrador ▾" aparece **solo** para `admin_general` | — |
| A2-C | Click → popover con Mi perfil, Dashboard, Historial | — |
| A2-D | El popover tiene el separador "Vista de socio" con Carrito y Notificaciones | — |
| A2-E | Badges numéricos correctos en Carrito y Notificaciones dentro del popover | — |
| A2-F | El puntito rojo de la píldora aparece cuando hay carrito o notificaciones sin leer | — |
| A2-G | Click fuera del popover → se cierra | — |
| A2-H | Navegar desde el popover → se cierra | — |
| A2-I | Un socio sin rol de admin ve los accesos directos de siempre, sin píldora | — |
| A2-J | **Hamburguesa de admin general: exactamente 7 ítems** — Inicio, Socios, Verificaciones, Alquileres, Eventos, Planteles, Catálogo. Nada más | — |
| A2-K | Un `personal_administrativo` (no admin general) sigue viendo **su** menú, no el curado | — |
| A2-L | El navbar flotante no tapa contenido en 360px de ancho | — |

### A.3 Recorrido mobile (Mejora-08) — probar en 360px y en celular real

| Caso | Ítem | Resultado |
|------|------|-----------|
| A3-A | `Registro.jsx` — pantallas de éxito y de reactivación centradas | — |
| A3-B | `Reservas.jsx` — grilla de días en 2 columnas hasta 440px, 3 después; los turnos tienen área táctil cómoda | — |
| A3-C | `TecnicoAsistencias.jsx` — tarjetas en mobile, tabla en desktop, sin scroll horizontal | — |
| A3-D | `TecnicoPlanteles.jsx` — inputs de categoría en una columna, sin solaparse | — |
| A3-E | `SocioShopping.jsx` — el botón no parte el texto en la grilla de 2 columnas | — |

---

## §B — Recordatorio de cuota: WhatsApp y mail masivo

⚠️ **El texto cambió el 16-09.** Los casos M4/M5 del documento anterior quedaron viejos:
el mensaje ahora dice cuántas cuotas se deben, el mes va **sin el año** y el monto **sin
centavos** cuando son cero. Esta es la versión vigente.

**Plantilla de fábrica actual:**

> Hola {nombre}! Te escribimos desde el club. Te recordamos que tenés {meses} cuota(s)
> pendiente(s) desde {mes} por ${monto}. Podés transferir al alias {alias} o pagar online:
> {link_pago}. ¡Gracias!

Estado de la configuración leído en Neon el 16-09: `alias_transferencia =
'clubatleticoroberts.mp'` ✅ · `plantilla_recordatorio = NULL` (usa la de fábrica) ·
`whatsapp_club` = columna nueva, sin cargar.

### B.1 Configuración — `/admin/productos` → panel "Recordatorio de cuota"

| Caso | Ítem | Resultado |
|------|------|-----------|
| M4-A | El panel abre y muestra la plantilla de fábrica con el aviso "todavía nadie editó el mensaje" | — |
| M4-B | El alias ya cargado aparece en el campo y en la vista previa | — |
| M4-C | Guardar una plantilla sin `{monto}` o sin `{mes}` → 422 con mensaje entendible | — |
| M4-D | "Restaurar" vuelve al texto de fábrica y el botón queda deshabilitado | — |
| M4-L 🆕 | **`{mes}` vs `{meses}`**: la lista de variables muestra las 6 y la nota de abajo advierte que son dos distintas. Escribir `{meses}` donde va `{mes}` **pasa la validación** — el mensaje sale con un número donde iba el nombre del mes. Confirmar que la vista previa lo deja ver antes de guardar | — |
| M4-M 🆕 | Campo **"Número de WhatsApp del club"**: se carga, se guarda y persiste tras recargar | — |
| M4-N 🆕 | El campo muestra la nota *"El mensaje se envía desde el WhatsApp que tengas abierto en tu dispositivo. Este número es solo un recordatorio interno."* | — |
| M4-O 🆕 | El número cargado **no** aparece en el mensaje ni cambia el deep link (es informativo) | — |
| M4-P 🆕 | Vaciar el campo y guardar → queda vacío, no rompe nada | — |
| M4-Q 🆕 | Guardar el panel con `personal_administrativo` → **403** (el PATCH es solo `admin_general`) | — |

### B.2 WhatsApp deep link — `/admin/socios` (Mejora-04)

| Caso | Ítem | Resultado |
|------|------|-----------|
| M4-E | El botón aparece **solo** en morosos activos (no en al día, no en becados, no en dados de baja) | — |
| M4-F | Socio sin teléfono → ícono gris, deshabilitado, tooltip "Sin teléfono registrado" | — |
| M4-G | Socio con teléfono → abre WhatsApp con el texto ya escrito | — |
| M4-R 🆕 | **`{meses}`** muestra la cantidad correcta de cuotas adeudadas | — |
| M4-S 🆕 | **`{mes}`** sale sin el año: "septiembre", no "septiembre 2026" | — |
| M4-T 🆕 | **`{monto}`** sale sin centavos: "$5.000", no "$5.000,00" | — |
| M4-U 🆕 | Un socio **menor** (descuento 40%): si el total tiene centavos reales, se muestran | — |
| M4-H | El monto coincide con lo que dice la fila y con `/socio/cuotas` de ese socio | — |
| M4-V 🆕 | Un moroso de **más de 12 meses**: "desde julio" sin año se entiende gracias al `{meses}` que va al lado. Si no se entiende, es una decisión a revisar (ver ronda 13) | — |
| M4-I | Teléfonos en formatos raros (`02355 15 123456`, `+54 9 2355…`, `(2355) 15-…`) → abre el chat correcto | — |
| M4-J | Teléfono ilegible → error 409 visible, sin pestaña en blanco colgada | — |
| M4-K | **En celular** (es el uso real): el link abre la app de WhatsApp, no la web | — |
| M4-W 🆕 | El `{alias}` del mensaje es `clubatleticoroberts.mp` | — |
| M4-X 🆕 | El `{link_pago}` apunta a producción, **no** a `localhost:5173` (depende del bloqueo #2) | — |

### B.3 Aviso masivo por mail — `/admin` (Mejora-05)

| Caso | Ítem | Resultado |
|------|------|-----------|
| M5-A | El botón pide confirmación antes de mandar | — |
| M5-B | Al confirmar: loading, y después "Aviso enviado a N socios" con los conteos | — |
| M5-C | Los mails llegan y el cuerpo es **el mismo texto** que el de WhatsApp | — |
| M5-D | Un socio con comprobante **pendiente de aprobación** no recibe el mail y aparece contado aparte | — |
| M5-E | Un moroso **sin email** no rompe el envío (queda contado aparte) | — |
| M5-F | Disparar el botón 4 veces en una hora → la cuarta da **429** | — |
| M5-G | Queda en `/admin/auditoria` como `AVISO_MAIL_MASIVO_CUOTA` con los conteos | — |
| M5-H 🆕 | Doble clic en el botón no dispara dos envíos (guarda con `useRef`, igual que BUG-03) | — |
| M5-I 🆕 | Con el padrón real el request tarda cerca de un minuto: la UI no da timeout ni parece colgada | — |

⚠️ Para M5-C/D hay que **fabricar el caso**: en la foto del padrón del 16-09 no había
ningún moroso con pago en verificación (0 de 13), y el socio de prueba 77000001 no está
moroso. O se usa un moroso real, o se deja a uno en mora a propósito desde `/admin/socios`.

---

## §C — Escáner offline (`/admin/escaner`)

Implementado el 15-09. **Nada de esto se vio funcionando.** Se prueba con el modo avión
del celular; el escritorio sirve solo para la mitad de los casos.

Caché: `localStorage['escaner_cache']`, refresco cada 15 min desde `GET /admin/escaner/cache`.

| Caso | Ítem | Resultado |
|------|------|-----------|
| C-A | Con señal: banner **verde** "En línea" | — |
| C-B | Modo avión → el banner pasa a **ámbar** (caché < 2 h) | — |
| C-C | Ingresar DNI offline → sale la misma tarjeta, con el chip "OFFLINE · datos de hace X" | — |
| C-D | El color del resultado (verde / rojo) **no** cambia por estar offline | — |
| C-E | **QR offline** → avisa que no se puede validar y ofrece pasar a DNI manual (el `qr_token` rota y no se cachea, es a propósito) | — |
| C-F | DNI que no está en la caché, sin señal → "Sin conexión — verificar manualmente" | — |
| C-G | `localStorage` vacío + sin señal → banner **gris**, "sin caché disponible" | — |
| C-H | Caché de más de 2 h → banner **rojo** con la advertencia | — |
| C-I | Volver la señal → "Conexión restaurada — actualizando…" y vuelta a verde | — |
| C-J | **Recargar la página sin señal → la sesión se mantiene**, no va a `/login` (es el fix de `car_user_profile`; sin esto el modo offline servía solo con la pestaña abierta) | — |
| C-K | Bloquear la pantalla del celular, esperar, volver → la sesión sigue viva | — |
| C-L | Con Render dormido (cold start): la validación en vivo cae a la caché a los 3 s, no se queda colgada | — |
| C-M | Un `admin_temporal` (portero) puede usar el escáner offline; un `invitado` **no** puede pedir `/admin/escaner/cache` | — |
| C-N | El escáner offline **no** ensucia `/admin/auditoria` con cientos de filas | — |
| C-O | Prueba en la puerta real, de noche, con poca luz (arrastra de la ronda 4) | — |

---

## §D — Admin reservas (BUG-19, BUG-20, BUG-27, Mejora-02)

| Caso | Ítem | Resultado |
|------|------|-----------|
| M2-A | **No** aparece el botón "Nueva reserva manual" en el header | — |
| M2-B | Un turno libre abre "Asignar a un socio" e "Inhabilitar turno" | — |
| M2-C | "Asignar a un socio": buscar por DNI/nombre y registrar la reserva | — |
| M2-D | El precio sale del catálogo según el turno, no de una lista escrita a mano | — |
| 19-A | "Inhabilitar turno" con motivo → funciona, **sin** el falso CORS/401 (era un `NOT NULL` de `id_producto`) | — |
| 19-B | El turno inhabilitado desaparece de `/socio/reservas` | — |
| 19-C | Si algo falla, el error que muestra el navegador es el real, no "Failed to fetch" | — |
| 19-D | El turno inhabilitado se ve como tal en la agenda del admin | — |
| 27-A | El modal "Inhabilitar turno" manda a «**Asignar a un socio**», no al botón que ya no existe | — |
| 20-A | Admin suspende una reserva confirmada → el socio recibe notificación in-app `reserva_cancelada` | — |
| 20-B | `/mis-compras` del socio la muestra como "**Cancelada por el club**", en **ámbar** (no rojo) | — |
| 20-C | El modal del admin **advierte antes de confirmar** si el turno se había pagado por transferencia | — |
| 20-D | El saldo a favor se acredita y el mail sale | — |
| M2-E | El socio asignado a mano recibe su notificación in-app | — |

---

## §E — Escáner en la puerta (BUG-16, BUG-17)

| Caso | Ítem | Resultado |
|------|------|-----------|
| 16-A | Socio en su mes de ingreso → "**SOCIO NUEVO ✓**" en verde | — |
| 16-B | La tarjeta suma el chip "Mes de ingreso — 1ª cuota pendiente" | — |
| 17-A | Socio menor de edad → la respuesta indica `es_menor` y la edad | — |
| 17-B | Socio **sin** fecha de nacimiento cargada → no se asume nada, no dice "menor" ni "mayor" | — |

---

## §F — Cola de retesteo que arrastra

Lo de acá está corregido hace rondas y nunca se confirmó en el navegador. Ordenado de
más barato a más caro.

### F.1 — Mejora-01: TTL de pre-reservas

| Caso | Ítem | Resultado |
|------|------|-----------|
| M1-A | Socio pone un turno en el carrito y cierra sesión → el turno se libera solo a los ~20-35 min (el job de 15 min ahora también lo mira, no solo el de 1 h) | — |

⚠️ Con Render dormido el job no corre. Si el turno no se libera, mirar primero
`/health` antes de anotar un bug.

### F.2 — §5.4 Expiración sin pago (nunca probado)

| Caso | Ítem | Resultado |
|------|------|-----------|
| F2-A | Reserva sin comprobante + pasa la hora de inicio → se libera | — |
| F2-B | Reserva con orden **aprobada** → **no** se libera | — |
| F2-C | Las 11 pre-reservas huérfanas de D2 (`bloqueada` con `id_orden = NULL`) se liberan solas en la primera corrida del scheduler | — |

### F.3 — §5.6 Cancelación del socio (nunca probado)

| Caso | Ítem | Resultado |
|------|------|-----------|
| F3-A | El socio cancela desde `/socio/reservas` → el turno queda liberado | — |
| F3-B | La confirmación es un modal in-app, no el `confirm()` del navegador | — |
| F3-C | El turno liberado vuelve a aparecer disponible para otro socio | — |

### F.4 — BUG-18: comprobante de reserva en `/mis-compras`

| Caso | Ítem | Resultado |
|------|------|-----------|
| 18-A | Una reserva **con** comprobante ofrece "Cambiar comprobante" | — |
| 18-B | El comprobante reemplazado se ve enseguida, sin esperar la aprobación (es BUG-15, misma causa) | — |

### F.5 — §8 Panel admin (BUG-24, BUG-25, Mejora-03)

| Caso | Ítem | Resultado |
|------|------|-----------|
| 24-A | Dar de baja a un socio con turnos `bloqueada` sin orden → las franjas se sueltan enseguida, no quedan ocupando la agenda hasta el TTL | — |
| 24-B | El socio reactivado **no** vuelve con el carrito viejo (el carrito ahora tiene dueño en `localStorage`) | — |
| 24-C | Dos socios distintos en el mismo navegador no se heredan el carrito | — |
| 24-D | Logout → el carrito no sobrevive | — |
| 25-A | El selector de `/admin/socios` tiene la opción "**Admin general**" | — |
| 25-B | No se confunde con "Administrativos" (`personal_administrativo`), que ya estaba | — |
| M3-A | Un admin que intenta darse de baja a sí mismo → aviso **in-app** rojo, no `alert()` del navegador | — |
| M3-B | Cuando la acción falla, el `ConfirmDialog` se cierra y el aviso queda visible (antes quedaba detrás del overlay) | — |

### F.6 — La cola vieja de las rondas 3 y 4

Lo más viejo y lo menos mirado. Cada ficha está en el documento anterior.

| Caso | Ítem | Resultado |
|------|------|-----------|
| 01-A…E | BUG-01: un socio con rol admin **desactivado** ya no aterriza en `/admin` (faltaba `es_activo` en `RolResponseSimple`) | — |
| 04-A…C | BUG-04: el inicio del socio, `/socio/cuotas` y el escáner dicen **lo mismo** (las tres copias del cálculo se unificaron en `utils/cuotas.js`) | — |
| 05-A | BUG-05: "Ver comprobante" funciona para el admin en `/admin/verificaciones`, en el modal y en el historial de `/admin/socios` | — |
| 07-A…E | BUG-07: **el mail `orden_aprobada_cuota` sale** por los cuatro caminos que aprueban (era el `autoflush=False`) | — |
| 09-A | BUG-09: el cobro en ventanilla queda como "efectivo", no como "transferencia" | — |
| 11-A/B | BUG-11: editar la fecha de nacimiento **no** deja un `CAMBIO_ROLES` en auditoría ni notifica "te sacaron: socio" | — |
| 13-A | BUG-13: no queda ningún `confirm()` nativo en el camino del socio ni en "Vaciar carrito" / "Aprobar Pago Completo" | — |
| 15-A/B | BUG-15: el comprobante reemplazado se ve enseguida en la tarjeta y en el modal | — |

---

## §G — Regresión automatizada (BUG-26)

Bloqueado por Docker, no por código. La suite arrastra **sin ejecutarse** los bloques de
BUG-24 (ronda 10) y BUG-26 (ronda 12).

| Caso | Ítem | Resultado |
|------|------|-----------|
| G-A | Rearmar la base de QA con `bootstrap_db` y correr **`qa_seguridad.py` entera** (29+ escenarios) | — |
| 26-A | Asignar una beca no da 500 en una base creada por `bootstrap_db` | — |
| 26-B | La notificación `beca_actualizada` queda registrada | — |
| 26-C | **Verificación manual de una sola vez:** `alembic revision --autogenerate` contra producción **no** propone tocar `chk_notificacion_tipo` (si lo propone, angostaría el `CHECK` de 12 valores a 9 — no aplicarla) | — |

⚠️ Si la base de QA se armó **antes** del fix de la ronda 12, la regresión va a fallar y
tiene razón: esa base tiene el `CHECK` viejo. Rearmarla primero.

---

## §H — Nunca cubierto

Secciones que no se tocaron en ninguna de las 13 rondas.

### H.1 — §5.7 Saldo a favor

| Caso | Ítem | Resultado |
|------|------|-----------|
| H1-A | Suspensión por lluvia → se acredita saldo en `usuarios.saldo_a_favor` | — |
| H1-B | El socio ve su saldo en `/socio` y en el checkout | — |
| H1-C | Aplicar el saldo como descuento en el checkout → el total baja y el saldo se descuenta | — |
| H1-D | Saldo mayor al total de la orden → no queda saldo negativo | — |
| H1-E | Ajuste manual desde `/admin/socios` con motivo obligatorio → queda en `audit_log` | — |

### H.2 — §5 Compras mixtas (split-order)

| Caso | Ítem | Resultado |
|------|------|-----------|
| H2-A | Una orden con cuota + alquiler + producto se aprueba y ejecuta las tres acciones | — |
| H2-B | D6-E: la orden mixta aparece bien en `/mis-compras` y en `/socio/cuotas` | — |

### H.3 — §3 Flujo moroso completo

| Caso | Ítem | Resultado |
|------|------|-----------|
| H3-A | Socio moroso paga → orden `pendiente_verificacion` | — |
| H3-B | `/socio` y `/socio/cuotas` dicen "**Pago en verificación**" (BUG-02 de la auditoría: el QR sigue inhabilitado, pero explicado) | — |
| H3-C | Admin aprueba → el socio pasa a "al día" | — |
| H3-D | El QR del socio cambia a verde **en el momento** | — |
| H3-E | `/admin/socios` refleja el cambio | — |

### H.4 — Módulos sin testear del todo

Tienda, módulo deportivo, personal administrativo, comercios y sponsors siguen en
🔲 **sin testear** según la tabla de `CLAUDE.md`. No hay casos escritos todavía: se
agregan cuando les toque el turno.

---

## §I — Transmisiones en Vivo (Streaming Pay-Per-View) y Fixture (Gemini, 16-09)

Todo lo de esta sección es **código nuevo implementado el 16-09**. Requiere que estén
corridas las migraciones `e7b8c9d0f1a2` y `bc78e9102a34`.

### I.1 — Configuración del Partido y Stream (`/admin/eventos` o `/tecnico/eventos`)

| Caso | Ítem | Resultado |
|------|------|-----------|
| I1-A | Alta/Edición de evento: campos de fixture visibles (Rival, Condición: Local/Visitante/Neutral, Goles Local, Goles Rival) | — |
| I1-B | Casilla "Habilitar Transmisión en Vivo" despliega configuración de PPV (Plataforma, Video ID / URL, Estado, Precio, Socio Gratis, Pública) | — |
| I1-C | Guardar evento con transmisión activada → persiste sin errores y muestra el badge `PPV <estado>` en la tarjeta | — |
| I1-D | La tarjeta del evento muestra los botones `[Ver Stream ↗]` y `[🎟️ Accesos]` cuando tiene transmisión | — |

### I.2 — Panel de Gestión de Accesos (`ModalAccesosTransmision.jsx`)

| Caso | Ítem | Resultado |
|------|------|-----------|
| I2-A | Click en `[🎟️ Accesos]` abre el modal con título del partido, rival, precio y estado actual | — |
| I2-B | Métricas en vivo cargan correctamente (Espectadores online, Entradas emitidas, Recaudación verificada, Pagos pendientes) | — |
| I2-C | Selector rápido de estado: cambiar entre `Programada`, `🔴 EN VIVO`, `🟡 Pausada` y `Finalizada` actualiza el estado en backend y en la tarjeta | — |
| I2-D | Botón "Refrescar" recarga las métricas y la lista de espectadores sin cerrar el modal | — |

### I.3 — Emisión Manual de Entrada a Hincha No-Socio (Sin Cuenta)

| Caso | Ítem | Resultado |
|------|------|-----------|
| I3-A | Pestaña "Emitir Entrada Manual" → opción "Hincha No-Socio (Sin Cuenta)" | — |
| I3-B | Intentar emitir sin email → validación bloquea con mensaje claro | — |
| I3-C | Cargar Email + Nombre + Teléfono (ej: `2355 123456`) + Motivo → click en "Emitir Entrada y Generar Link" | — |
| I3-D | Aparece tarjeta verde de éxito con el Magic Link (`.../en-vivo/:id?ticket=XYZ`) | — |
| I3-E | Botón "Copiar Link" copia al portapapeles y da feedback visual ("¡Copiado!") | — |
| I3-F | Botón verde "Enviar por WhatsApp" abre `wa.me/5492355123456?text=...` con el mensaje pre-redactado conteniendo el saludo y el link | — |
| I3-G | Si no se cargó teléfono, "Enviar por WhatsApp" abre `wa.me/?text=...` permitiendo elegir el contacto en WhatsApp Web/móvil | — |

### I.4 — Emisión Manual de Entrada a Socio Moroso / Sin Cuota al Día

| Caso | Ítem | Resultado |
|------|------|-----------|
| I4-A | Opción "Socio del Club (Moroso o Sin Cuota)" → buscador en vivo filtra por nombre/apellido/DNI llamando a `GET /transmisiones/buscar-usuarios` | — |
| I4-B | El resultado distingue con badge si el socio está `Al Día` (verde) o `Moroso / Con deuda` (rojo) | — |
| I4-C | Seleccionar un socio moroso → muestra tarjeta de resumen, precarga teléfono si lo tiene cargado en el perfil y permite editar motivo | — |
| I4-D | Emitir entrada → desbloquea el reproductor para su cuenta de usuario (`id_usuario`) y genera Magic Link | — |
| I4-E | **Verificar que NO altere el estado financiero**: `mes_cubierto_hasta` del socio no cambia y su deuda de cuotas sociales se mantiene intacta | — |

### I.5 — Pestaña "Accesos y Espectadores" y Aprobación de Transferencias

| Caso | Ítem | Resultado |
|------|------|-----------|
| I5-A | La pestaña lista todas las entradas emitidas (socios e invitados) con su estado de pago | — |
| I5-B | Filtros rápidos: `Todos`, `🟢 En Vivo`, `⏳ Pendientes`, `Pagados`, `Cortesía` filtran la lista al instante | — |
| I5-C | Buscador de texto en la lista filtra por nombre, email o token | — |
| I5-D | Entradas con pago por transferencia en estado pendiente muestran badge ámbar y botón verde `[✓ Aprobar Pago]` | — |
| I5-E | Click en `[✓ Aprobar Pago]` → llama a `PATCH /transmisiones/entradas/:id/aprobar`, pasa a verificado y actualiza la recaudación | — |
| I5-F | Cada fila tiene botones para copiar link, enviar por WhatsApp y botón rojo para revocar entrada (con confirmación) | — |

### I.6 — Experiencia del Espectador y Player Blindado (`/en-vivo/:idEvento`)

| Caso | Ítem | Resultado |
|------|------|-----------|
| I6-A | **Socio al día** entra a `/en-vivo/:id` logueado (con streaming gratis para socios) → reproductor se carga directamente sin paywall | — |
| I6-B | **Socio moroso** sin entrada entra logueado → ve el paywall informando que debe comprar entrada o regularizar cuota | — |
| I6-C | **Socio moroso** con entrada asignada entra logueado → reproductor se desbloquea directamente | — |
| I6-D | **Hincha no-socio** entra por Magic Link (`/en-vivo/:id?ticket=XYZ`) sin cuenta → reproductor se desbloquea de inmediato | — |
| I6-E | **Resiliencia F5**: Hincha no-socio refresca la página (F5) o cierra la pestaña y vuelve a entrar → la sesión persiste en `localStorage` y NO le pide pagar de nuevo | — |
| I6-F | **Player Blindado anti-clics**: En el reproductor de YouTube, click en el título, avatar de canal o botón "Compartir" está bloqueado por la máscara invisible | — |
| I6-G | Click derecho sobre el reproductor no abre el menú contextual de YouTube (`contextMenu` prevenido) | — |
| I6-H | Botón de pantalla completa oficial en la barra superior expande el reproductor a pantalla completa nativa correctamente | — |
| I6-I | **Control de concurrencia = 1**: Abrir el mismo Magic Link o la misma cuenta en dos navegadores/dispositivos distintos → a los 30s del heartbeat, la pestaña anterior se pausa con cartel de "Sesión iniciada en otro dispositivo" | — |

---

## §J — Asistente Virtual IA ("Camotito" / Chatbot con Gemini Flash)

El asistente flotante está disponible en toda la web (esquina inferior derecha) y funciona tanto con Google Gemini Flash (`thinkingBudget: 0` para respuesta ultrarrápida en 1-2s) como con un motor de contingencia por reglas ante indisponibilidad o límite de cuota (HTTP 429). Dispone de segmentación estricta de permisos por rol, reinicio in-app sin cuadros del navegador, protección contra filtración de rutas privadas y transición automática entre sesiones anónimas y de socio.

### J.1 — Identidad Visual, Presencia y Apertura del Widget

| Caso | Ítem | Resultado |
|------|------|-----------|
| J1-A | **Botón flotante disparador**: Ubicado en la esquina inferior derecha (`bottom-5 right-5 z-40`), con icono de mensaje limpio y sin emojis | — |
| J1-B | **Tooltip de escritorio**: Al pasar o posarse sobre el botón cerrado, muestra el botón pastilla `"Consultas con Camotito"` (nunca "Camote" ni "Camotero") | — |
| J1-C | **Apertura del widget**: Al hacer clic en el botón, se despliega suavemente la ventana del chat y el cursor hace auto-focus en el campo de texto | — |
| J1-D | **Encabezado minimalista**: La cabecera muestra **únicamente** el logo circular con el Camotí (`camoti-azul.PNG`) y el texto **Camotito**. No contiene badges ("Asistente CAR"), ni subtítulos ("En línea..."), ni globos rojo/azul | — |
| J1-E | **Botón de cierre**: El botón `[✕]` cierra la ventana manteniendo el estado abierto/cerrado en `sessionStorage` | — |
| J1-F | **Adaptación Mobile**: En pantallas móviles (< 640px), la ventana se expande ocupando la pantalla de forma responsiva sin desbordes horizontales ni tapar la barra del navegador | — |
| J1-G | **Prohibición estricta de emojis**: Toda la interfaz del widget, sugerencias rápidas, saludos y respuestas generadas están 100% libres de emojis | — |

### J.2 — Reinicio In-App (Sin alertas emergentes del navegador)

| Caso | Ítem | Resultado |
|------|------|-----------|
| J2-A | **Disparador de reinicio**: Al presionar el botón de reinicio (icono circular al lado de la ✕), **NO** se abre el alert/confirm nativo del navegador (*"localhost dice..."*) | — |
| J2-B | **Banner de confirmación integrado**: Se despliega una barra in-app en la parte superior del chat con el texto *"¿Reiniciar la conversación?"* y botones `[Sí, reiniciar]` y `[Cancelar]` | — |
| J2-C | **Acción Cancelar**: Clic en `[Cancelar]` cierra el banner inmediatamente y preserva el historial de chat intacto | — |
| J2-D | **Acción Confirmar**: Clic en `[Sí, reiniciar]` borra el historial del storage (`car_chatbot_historial_v4_*`), limpia los mensajes y recarga el saludo inicial con las sugerencias correspondientes al rol actual | — |

### J.3 — Consultas Frecuentes (Sugerencias Contextuales por Rol)

| Caso | Ítem | Resultado |
|------|------|-----------|
| J3-A | **Etiqueta oficial**: La cabecera de las sugerencias muestra estrictamente el texto **"Consultas frecuentes:"** (sin "de un toque" ni adornos) | — |
| J3-B | **Chips para Visitante Anónimo**: Al entrar sin cuenta, se muestran chips públicos: `Próximo partido y stream`, `Cuota social y alias`, `Cómo hacerme socio`, `Alquiler de canchas y quincho`, `Contactar por WhatsApp` | — |
| J3-C | **Chips para Socio Autenticado**: Al entrar con cuenta de socio, las sugerencias se adaptan a su portal: `Próximo partido y stream`, `Mis cuotas y pagos`, `Reservar cancha o quincho`, `Comercios con descuento`, `Tour guiado por la app`, `Contactar por WhatsApp` | — |
| J3-D | **Ejecución al toque**: Clic en cualquiera de las sugerencias envía automáticamente la consulta al asistente y desactiva los botones mientras procesa | — |
| J3-E | **Disparador del Tour Guiado desde el Chatbot**: Al hacer clic en el chip `Tour guiado por la app` o pedirle al bot *"quiero el tour guiado"*, se cierra la ventana del chat, se emite el evento global `car:tour-iniciar` y se lanza el tour interactivo en `/socio?tour=1` | — |

### J.4 — Seguridad, Permisos y Control de Enlaces por Rol (Auditoría de Rutas)

| Caso | Ítem | Resultado |
|------|------|-----------|
| J4-A | **Visitante Anónimo consulta por Cuotas**: El bot informa valor oficial y alias (`clubatleticoroberts.mp`), pero **NUNCA** da link a `/socio/cuotas`. Ofrece enlaces a `[Iniciar sesión](/login)` o `[Completar solicitud de socio](/registro)` | — |
| J4-B | **Visitante Anónimo consulta por Canchas/Quincho**: Explica las instalaciones, pero **NUNCA** da links a `/socio/reservas` ni `/socio/cancha`. Invita a iniciar sesión en `[Iniciar sesión](/login)` o asociarse en `[Completar solicitud de socio](/registro)` | — |
| J4-C | **Visitante Anónimo consulta por Cobro Manual / Admin**: El bot **deniega** el acceso por no tener sesión y **NUNCA** entrega rutas `/admin/*`. Invita a iniciar sesión como administrador o consultar por WhatsApp | — |
| J4-D | **Socio Autenticado consulta por Cuotas/Canchas**: El bot entrega links válidos a su panel de socio (`[Consultar cuotas](/socio/cuotas)`, `[Reservar instalaciones](/socio/reservas)`) | — |
| J4-E | **Socio Autenticado consulta por Admin**: El bot reconoce que es socio y le **prohíbe** enlaces a `/admin/*` | — |
| J4-F | **Administrador consulta por Gestión**: El bot reconoce su rol administrativo y le provee los accesos a `[Administración de socios](/admin/socios)` y `[Verificaciones de pagos](/admin/verificaciones)` | — |
| J4-G | **Guardia de Navegación Frontend (Doble Blindaje)**: Si por cualquier motivo un usuario no autenticado hace clic en un botón hacia `/socio/*` o `/admin/*`, el frontend lo intercepta y redirige a `/login?redirect=...`. Si un socio común hace clic en `/admin/*`, lo redirige a `/socio` | — |

### J.5 — Transición Dinámica de Sesión (Visitante ➔ Registro / Login ➔ Socio)

| Caso | Ítem | Resultado |
|------|------|-----------|
| J5-A | **Aislamiento de Storage**: Las conversaciones se almacenan bajo claves independientes (`car_chatbot_historial_v4_anonimo` vs `car_chatbot_historial_v4_socio_{id}`) | — |
| J5-B | **Flujo cruzado (Visitante a Socio)**: Un usuario abre el chat como visitante, pregunta por cuotas, luego va a `/login` e inicia sesión como socio → Al abrir o mirar el chatbot, este detecta el cambio de sesión automáticamente vía `useAuth()`, saluda con su nombre personal (`"Hola Carlos. Soy Camotito..."`), carga sugerencias de socio y **no sobreescribe ni contamina** su conversación con la del invitado | — |
| J5-C | **Flujo inverso (Logout)**: El socio cierra sesión → el chatbot conmuta automáticamente al estado de visitante anónimo con el saludo general | — |

### J.6 — Formato Visual de Enlaces y Comunicación

| Caso | Ítem | Resultado |
|------|------|-----------|
| J6-A | **Botones estilo píldora y cero corchetes `[...]`**: Los links a rutas internas se renderizan como botones legibles con flecha `[Nombre amigable] →` (ej: `Ver transmisión en vivo →`), sin duplicación de etiquetas ni mostrar corchetes `[...]` o barras técnicas sueltas como `(/en-vivo)` | — |
| J6-B | **WhatsApp condicionado a pedido explícito**: Eliminado el pie de mensaje reiterativo de WhatsApp. El botón/enlace a WhatsApp aparece única y exclusivamente cuando el socio pregunta explícitamente por contactar a Secretaría o hablar por WhatsApp | — |
| J6-C | **Formato markdown enriquecido**: El texto soporta negritas (`**texto**`) y código monospaciado (`` `alias` ``) con estilos visuales limpios | — |

### J.7 — Inteligencia Artificial, Latencia y Fallback Resiliente

| Caso | Ítem | Resultado |
|------|------|-----------|
| J7-A | **Respuesta Ultrarrápida (`thinkingBudget: 0`)**: Con Gemini Flash conectado, las respuestas se generan en 1 a 2 segundos sin demoras de razonamiento profundo | — |
| J7-B | **Fallback automático ante límite de cuota (HTTP 429)**: Si se agotan los tokens gratuitos de la API de Gemini, el backend conmuta al instante al motor por reglas sin arrojar error 500 | — |
| J7-C | **Fallback ante ausencia de API Key**: Si `GEMINI_API_KEY` está vacía, el motor de reglas responde preguntas sobre cuotas, alias, partidos, streaming, canchas, comercios y FAQs con datos directos de Neon | — |
| J7-D | **Envío por teclado y auto-scroll**: Presionar `Enter` envía el mensaje; `Shift+Enter` genera salto de línea. El scroll se desplaza suavemente al final tras cada respuesta | — |
| J7-E | **Supresión de viñetas crudas en fallback**: Al solicitar el menú o ante consultas no reconocidas (o saludos como *"como estas?"*), el chatbot devuelve texto limpio y despliega los chips interactivos de sugerencias, nunca una lista de viñetas Markdown con links técnicos | — |

---

## §K — Tour Guiado Interactivo del Socio ("Guía con Camotito")

El tour interactivo de 14 pasos acompaña a los nuevos socios para que conozcan las funciones esenciales del portal. Diseñado especialmente para mobile-first con posicionamiento inteligente anti-recortes y menú hamburguesa ítem por ítem.

### K.1 — Disparador y Alcance del Tour

| Caso | Ítem | Resultado |
|------|------|-----------|
| K1-A | **Primer ingreso exclusivo**: Solo se abre automáticamente si la URL contiene `?bienvenida=1` tras cambiar la contraseña obligatoria en el primer ingreso | — |
| K1-B | **Socios recurrentes inmunes**: Al ingresar por `/login` común, el tour **NO** se inicia automáticamente | — |
| K1-C | **Disparador a demanda vía Chatbot**: Un socio puede iniciar el tour cuando lo desee haciendo clic en el chip *"Tour guiado por la app"* o escribiendo *"quiero el tour guiado"* en el chat con Camotito | — |
| K1-D | **Limpieza de `/perfil`**: La tarjeta estática inferior de la guía interactiva fue retirada de `SocioPerfil.jsx`, unificando todo el soporte en Camotito | — |

### K.2 — Secuencia de los 14 Pasos

| Caso | Ítem | Resultado |
|------|------|-----------|
| K2-A | **Paso 1 - Carnet Digital (`#tour-carnet`)**: Spotlight sobre la tarjeta con el QR dinámico que rota cada 55s | — |
| K2-B | **Paso 2 - Cuotas y Pagos (`#tour-cuotas`)**: Estado de cuenta y botón de pago rápido por transferencia | — |
| K2-C | **Paso 3 - Beneficios en Comercios (`#tour-beneficios`)**: Acceso a descuentos en comercios locales | — |
| K2-D | **Paso 4 - Campanita de Notificaciones (`#tour-notificaciones`)**: Alertas in-app de cuotas aprobadas y reservas | — |
| K2-E | **Paso 5 - Carrito de Compras (`#tour-carrito`)**: Acceso a la orden unificada de indumentaria y accesos | — |
| K2-F | **Paso 6 - Botón Menú Principal (`#tour-menu-btn`)**: Explicación del menú lateral y apertura automática | — |
| K2-G | **Paso 7 - Menú: Inicio (`#tour-item-inicio`)**: Resumen general y carnet | — |
| K2-H | **Paso 8 - Menú: Transmisión en Vivo (`#tour-item-stream`)**: Partidos PPV en directo | — |
| K2-I | **Paso 9 - Menú: Gestión de Cuotas (`#tour-item-cuotas`)**: Subida de comprobantes y meses cubiertos | — |
| K2-J | **Paso 10 - Menú: Reserva Salón/Quincho (`#tour-item-salon`)**: Turnos para eventos familiares | — |
| K2-K | **Paso 11 - Menú: Reserva Canchas (`#tour-item-canchas`)**: Canchas de fútbol sintético | — |
| K2-L | **Paso 12 - Menú: Tienda Oficial (`#tour-item-tienda`)**: Camisetas y merchandising | — |
| K2-M | **Paso 13 - Menú: Mis Compras (`#tour-item-compras`)**: Estados de entrega y comprobantes | — |
| K2-N | **Paso 14 - Menú: Configuración (`#tour-item-perfil`)**: Edición de datos personales y teléfono | — |

### K.3 — Experiencia Visual y Mobile

| Caso | Ítem | Resultado |
|------|------|-----------|
| K3-A | **Posicionamiento inteligente anti-recorte**: En pantallas móviles, si el elemento está arriba, el diálogo se sitúa en `bottom: 12px`; si está abajo, en `top: 12px` con `max-h-[44vh]` scrolleable | — |
| K3-B | **Spotlight con seguimiento de scroll interno**: Al scrollear dentro del menú hamburguesa, el recuadro blanco sigue fluidamente cada enlace sin desfasarse | — |
| K3-C | **Cierre limpio**: Al hacer clic en *"Omitir"*, presionar `Escape` o finalizar el tour, el menú lateral se repliega automáticamente | — |

---

## Sesión Pre-QA Automatizada — 2026-09-17

> **Fecha:** 2026-09-17 | **Ejecutor:** Antigravity (Suite Automatizada Pre-QA)  
> **Entorno:** Base de datos Neon (`ep-calm-bread-acnpvt59-pooler.sa-east-1.aws.neon.tech`) + FastAPI TestClient + Frontend Vite / ESLint  
> **Herramienta:** `python -m backend.scripts.pre_qa_test`

### 1. Resumen Ejecutivo
Se ejecutó una batería integral de pruebas previas al QA manual para garantizar la integridad del código, modelos de datos, seguridad, compilación y comportamiento del Asistente Virtual IA ("Camotito") y el Tour Guiado:

| Área evaluada | Pruebas | Resultado | Estado |
|---|:---:|:---:|:---:|
| **Frontend Linter (ESLint)** | Análisis estático global | 0 errores, 0 warnings | ✅ PASS |
| **Frontend Build (Vite)** | 1900 módulos transpilados | Build exitoso (10.80s) | ✅ PASS |
| **Infraestructura & Conectividad** | Root API, `/health`, Alembic Head | 3/3 aprobadas | ✅ PASS |
| **Configuración & Catálogo BD** | Cuota $5.000, Menores $3.000 (40%), Vencimiento día 10, Alquileres | 9/9 aprobadas | ✅ PASS |
| **Seguridad Criptográfica & JWT** | Bcrypt hash/verify, creación y decoding de token | 4/4 aprobadas | ✅ PASS |
| **Chatbot Asistente "Camotito"** | Saludo, chips, cálculo de cuotas, aranceles, blindaje de rutas por rol, cero emojis, menú interactivo y tour | 31/31 aprobadas | ✅ PASS |
| **Streaming PPV & Eventos** | `/transmisiones/partido-actual`, `/deportivo/eventos`, tablas BD | 9/9 aprobadas | ✅ PASS |
| **TOTAL GENERAL** | **56 casos automatizados** | **56 / 56 (100% PASS)** | ✅ **ESTABLE** |

---

### 2. Hallazgos detectados y remediados en esta ronda

#### BUG-28 — 15 Errores de ESLint por variables e imports no utilizados
- **Sección:** Frontend · **Severidad:** Baja (Linter) · **Hallado:** 2026-09-17
- **Qué pasaba:** `npm run lint` fallaba con 15 errores por variables/iconos no utilizados en `ModalAccesosTransmision.jsx` y `TransmisionEnVivo.jsx`.
- **Remediación:** Se limpiaron todos los imports sin uso y se agregó la visualización visual del banner de error en `ModalAccesosTransmision` si `errorMetricas` está presente.
- **Estado:** ✅ Resuelto.

#### BUG-29 — WhatsApp del club en BD sin valor inicial (`None`)
- **Sección:** Base de Datos / Configuración · **Severidad:** Media · **Hallado:** 2026-09-17
- **Qué pasaba:** La columna `configuracion_global.whatsapp_club` contenía `NULL`, obligando a usar siempre el fallback estático del código.
- **Remediación:** Se inicializó en `2355 123456` en la base de datos de desarrollo.
- **Estado:** ✅ Resuelto.

#### BUG-30 — Formateo en palabras de importes en respuestas de Gemini ("tres mil pesos", "cuarenta por ciento")
- **Sección:** Chatbot · **Severidad:** Media (UX) · **Hallado:** 2026-09-17
- **Qué pasaba:** Al consultar por cuotas de menores, el modelo generaba texto redactando los números con letras en lugar de cifras monetarias claras.
- **Remediación:** Se añadió en `backend/routers/chatbot.py` la directiva explícita de estilo numérico ("expresar siempre cifras y porcentajes con números y símbolos tipo $5.000, $3.000, 40%").
- **Estado:** ✅ Resuelto.

#### BUG-31 — Fallback del chatbot enviando listas crudas de viñetas Markdown en lugar de opciones interactivas
- **Sección:** Chatbot · **Severidad:** Media (UX) · **Hallado:** 2026-09-17
- **Qué pasaba:** Ante consultas no mapeadas o saludos como *"como estas?"*, la regla de fallback del chatbot devolvía una lista de viñetas de texto markdown plano (`- Próximo partido y stream...: Ver transmisión...`).
- **Remediación:** Se actualizaron las reglas del chatbot para responder con saludo cordial sin viñetas y adjuntar el array estructurado `sugerencias`, el cual es renderizado en `ChatbotFlotante.jsx` mediante la botonera de chips interactivos.
- **Estado:** ✅ Resuelto.

#### MEJORA-10 — Renombre oficial a "Camotito" e invalidación de almacenamiento previo
- **Sección:** Chatbot / Identidad · **Severidad:** Baja (Consistencia) · **Hallado:** 2026-09-17
- **Qué pasaba:** El asistente utilizaba variaciones entre "Camote" y "Camotero", persistiendo en cachés viejas de navegador.
- **Remediación:** Se unificó la identidad bajo el nombre único **"Camotito"** en backend, frontend, tooltips y sanitizadores. Se migró la clave de sesión a `car_chatbot_historial_v4` para purgar estados antiguos.
- **Estado:** ✅ Implementado.

#### MEJORA-11 — Tour Guiado de 14 Pasos Ítem por Ítem e Integración en el Chatbot
- **Sección:** Onboarding / UX · **Severidad:** Media (Feature) · **Hallado:** 2026-09-17
- **Qué pasaba:** El socio no contaba con una inducción interactiva a su portal en su primer ingreso, o dependía de una tarjeta estática en `/perfil`.
- **Remediación:** Se implementó `TourGuiado.jsx` (14 pasos recorriendo carnet QR, cuotas, beneficios, notificaciones, carrito y el menú lateral ítem por ítem), con posicionamiento inteligente anti-recorte en mobile y activación exclusiva en primer ingreso (`?bienvenida=1`) o a demanda desde las opciones de consulta de Camotito.
- **Estado:** ✅ Implementado.

---

## Sesión QA Extendido Automatizado — 2026-09-17 (Previo al QA Manual)

> **Fecha:** 2026-09-17 | **Ejecutor:** Antigravity (Suite Automatizada QA Extendido)  
> **Entorno:** Base de datos Neon (`ep-calm-bread-acnpvt59-pooler.sa-east-1.aws.neon.tech`) + FastAPI TestClient + Frontend ESLint  
> **Herramienta:** `python -m backend.scripts.extended_qa_test`

### 1. Resumen Ejecutivo
Para garantizar que el QA manual del 16/09/2026 se desarrolle con total fluidez y sin bloqueos de datos, se creó y ejecutó un script de verificación exhaustivo de **69 pruebas automatizadas** que cubre los módulos críticos, datos maestros en Neon, roles puros y casos bordes de la aplicación:

| Módulo / Frente evaluado | Pruebas | Resultado | Detalle clave |
|---|:---:|:---:|---|
| **1. Bloqueos Previos, Infra & Catálogo** | 14 / 14 | ✅ PASS | API root 200, DB online, Head `bc78e9102a34`, Cuota $5.000, 40% menor, WhatsApp oficial, Cancha 1, Cancha 2 (desbloqueado), Quinchos |
| **2. Matriz de Roles & RBAC (Seguridad)** | 11 / 11 | ✅ PASS | Admin General entra a todo; Personal Administrativo accede a usuarios/reservas/productos pero 403 en dashboards sensibles; Socio entra a su portal y 403 estricto a `/admin/*`; Portero (`admin_temporal`) accede a caché pero 403 a usuarios y dashboard |
| **3. §B — Recordatorios de Cuota & WhatsApp** | 9 / 9 | ✅ PASS | `/admin/pagos/morosos` 200 con payload estructurado; normalización robusta de teléfonos argentinos (`02355 15...`, `+54 9...`, `(2355)...` -> `5492355...`); cálculo de mora para 0, 1 y 5 meses |
| **4. §C & §E — Escáner Puerta & Padrón Offline** | 6 / 6 | ✅ PASS | `/admin/escaner/cache` 200 (153 socios indexados); validación DNI socio al día (77000001) verde; menor (77000002) verde con chip; moroso (36778899) rojo no habilitado |
| **5. §D — Agenda de Reservas e Instalaciones** | 3 / 3 | ✅ PASS | `/admin/reservas` 200; consultas de disponibilidad de turnos para Cancha 1 y Cancha 2 responden sin error 500 |
| **6. §I — Streaming Pay-Per-View & Accesos** | 4 / 4 | ✅ PASS | `/transmisiones/partido-actual` 200; `/transmisiones/buscar-usuarios` filtra socios por nombre/DNI y cataloga certeramente estado financiero (`Al Día` vs `Moroso`) |
| **7. §J & §K — Camotito & Tour Guiado** | 12 / 12 | ✅ PASS | Info inicial con identidad "Camotito", chips según rol, disparo de tour (`/socio?tour=1`), consulta de aranceles y alias bancario, cero emojis y sin viñetas crudas |
| **TOTAL EXTENDIDO** | **69 / 69** | **✅ 100% PASS** | **Cero fallos, cero regresiones, base de datos Neon y servicios 100% validados** |

---

### 2. Acciones y Desbloqueos realizados antes de la corrida

#### Desbloqueo Bloqueo #3 — Alta del producto `Cancha 2`
- **Problema:** En Neon solo figuraba `Cancha 1` (ID 2), impidiendo testear la sección §5 de alquiler de canchas.
- **Acción:** Se insertó en la tabla `productos_servicios` el registro ID 8: `nombre='Cancha 2'`, `categoria='alquiler'`, `precio_actual=25000.00`, `es_activo=True`.

#### Creación del usuario Portero Escáner puro (`admin_temporal`)
- **Problema:** No existía en la base una cuenta con rol único `admin_temporal` para verificar el redireccionamiento estricto a `/admin/escaner` y el rechazo en `/socio`.
- **Acción:** Se dio de alta en Neon el usuario DNI `77000009` (contraseña `wewewewe1`, nombre *Portero Escaner*), vinculado exclusivamente al rol `admin_temporal`.

#### Sanitización de cuentas de prueba con acumulación de roles de staff
- **Problema:** Cuentas como Travis Kelce (`42542572`) o Valentina Fernández (`37889456`) tenían asignado el rol `personal_administrativo` además de `socio`, desvirtuando las pruebas de morosos en la interfaz del socio.
- **Acción:** Se sanearon sus registros en `roles_usuarios`, dejándolos exclusivamente con el rol `socio`.

#### Corrección defensiva en extracción de IP (`backend/routers/qr_auth.py`)
- **Problema:** Al interactuar clientes de test o proxies enviando hosts no estándar (`testclient`), el backend arrojaba error de sintaxis PostgreSQL en el tipo de dato `INET`.
- **Acción:** Se ajustó la función `_extraer_ip()` para mapear `testclient` a `127.0.0.1`, blindando el registro de auditoría de escaneos.

---

## Cómo usar este documento

- **Una ronda = una pasada.** Al terminar, se agrega una sección "Estado de la ronda N"
  al pie con qué se corrigió y qué quedó.
- **No borrar filas.** Un caso que pasó se marca ✅ y se deja; así la próxima ronda sabe
  qué no hace falta repetir.
- **Cada hallazgo, una ficha.** El índice de arriba es solo el resumen.
- **Antes de diagnosticar un bug de cuotas, pagos, mails o roles**, leer la ficha
  correspondiente en `qa-manual-2026-09-08.md`. Varias causas raíz de allá son
  contraintuitivas y ya costaron rondas enteras.


